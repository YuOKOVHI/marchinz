# 010_MarchinZ 引き継ぎ書 2026-07-20夜

新しいセッションの最初の1通に、この下の「新セッションへの貼り付け用テキスト」を
そのまま貼ってください。以降はこの文書が正本です。

## 現在地

- 本番・ローカルとも `7dd4a33` = **v1.39.24** で一致。作業ツリークリーン
- 今日1日で v1.39.1 → v1.39.24 まで進行。push は6回に分けて実施、すべて本番反映確認済み
- 前の引き継ぎ書は `引き継ぎ書_2026-07-20.md`(朝〜昼)。本書はその続き(昼〜夜)

---

## 今日やったこと(昼〜夜、ざっくり)

### 1. 全ページ・全コードレビュー → v1.39.5〜1.39.6

UI/UXデザイナー+シニアエンジニアの二人格でレビュー。AA未達56件を色トークン2つの
修正でほぼ解消。トースト基盤新設(alert 22箇所を置換)、タップ標的44px、
`[hidden]`ガードを本体にも追加。

### 2. Switcher 実素材テスト → 書き出しバグを3件発見・修正

優さんの実機(Timeline 1-3.mov、2〜3GB×3本)で初めてテスト。3つのバグが
連鎖して見つかった:

- **①「デコードが進みません」**(v1.39.9で修正): `frameAt`の時刻基準が絶対/相対で
  食い違い、開始位置の分だけ二重に進んでいた
- **② NotReadableError**(v1.39.13でリトライ導入、後に真因判明): 2タブ同時書き出しに
  よるメモリ枯渇だった(懸念1参照)
- **③ RangeError: Array buffer allocation failed**(v1.39.14で対症→v1.39.15で根治):
  `fastStart: "in-memory"`が真因。ディスク直接書き込みにしても効いていなかった

**②③の共通点**: シニアエンジニア協議で判明。20:38に別タブの2つの書き出しが
「同じ秒」に別種のエラーで同時死しており、実体は同じメモリ枯渇だった
(v1.39.18で Web Locks により全タブ通して書き出し1本に制限)。

### 3. Switcher UI改善 → v1.39.10〜1.39.12, 1.39.16〜1.39.19

- カメラ切替を「いま映っているカット」に統一、タップ2回で変更
- 手ブレを単独条件で失格に(従来は`flipRatio`とのANDで穴があった)
- 保存先ダイアログは容量が大きいときだけ(700MB基準)
- 書き出し中の残り時間を直近速度で計算(従来は序盤の助走に引きずられ過大表示)
- 「映像分析中」/「書き出し中」の出し分け+400msスピナー
- 「できました」→「書き出す準備ができました」等、文言の明確化

### 4. iOS書き出し対応 → v1.39.20〜1.39.21

iPhoneはディスク直接書き込みができずメモリに全部載せるため、上限を超えると
無警告でタブごと落ちる。始める前に断るガードを追加。ビットレートは
投稿先の実効レート(Instagram実質5Mbps)に合わせて5Mbpsに。

**優さんの要望「マーチングショウ8分30秒をiPhoneで書き出したい」は未解決。**
現状の5Mbps/260MB上限では6.7分が限界。`iPhone書き出し8分半_検討メモ_2026-07-20.md`
に選択肢と協議結果を記録済み(下記「持ち越し」参照)。

### 5. 会員種別の書き出し上限を新設 → v1.39.22

Switcher: 取り込み13分(登録)、書き出しは登録8分30秒/ゲスト5分(ショウ8分基準)。

### 6. MarchinZ Vlog 大改修 → v1.39.23〜1.39.24

優さんの12項目の指示を一括実装:
- ヘッダーのログイン状態が反映されない不具合を修正(全4ツール共通、sitechrome.js)
- クリエイター一覧を2×2グリッドに、全バッジ削除、Vlogに描き起こしイラスト
- 雰囲気選択を単体着地ページ→作業領域内の1セクションに統合。順序変更
- 「見てほしい人」をテキスト→7択+その他プルダウンに
- 「映像」→「インサート映像(Bロール)」に改称、インタビューを先に配置、
  撮影ガイドを折りたたみ化+アイコン付与(17項目)
- 「音」→「BGM」、選択肢の並べ替え
- Artlist紹介文修正(＋2ヶ月無料の正しい文言に)
- 「組み立てる」→「Vlog自動編集」。**押しても無反応だった不具合を修正**
  (onclick未結線)。ただし自動編集エンジン自体は未実装のため「近日公開予定」と
  正直に案内するのみ(下記「持ち越し」参照)
- 会員種別ごとの素材上限を全面改定(ゲスト/登録で動画尺・インサート本数・
  ロゴ・写真・完成尺)
- 「MarchinZ Vlog（開発中）」表記を全4箇所に統一

---

## 持ち越し・未解決(優先順)

### 1. Vlog自動編集エンジンが未実装 ★最重要

`tools/vlog/js/` に `planner.js` が存在しない。Phase 0(素材の取り込み・状態管理・
UI)のみ完了で、素材をどう並べて動画にするかのロジックが丸ごと無い。
「組み立てる」ボタンは今回**正直に案内するだけ**に直したが、本体機能は
ゼロから実装が必要。過去の引き継ぎ書にあった設計の要点:

- Phase 1.5「書き出し貫通スパイク」を先に(データ構造を確定させてから)
- planner の割当は「ブロック→素材ID+使う区間」方式(3者レビューの一致要求)
- テロップ・Bロール被せ・末尾トリムより頭トリム優先、など未反映の提案あり
  (旧引き継ぎ書「レビューで出た未反映の提案」節を参照)

### 2. iPhoneでショウ8分30秒を書き出せるようにする

`iPhone書き出し8分半_検討メモ_2026-07-20.md` に選択肢A〜D、シニアエンジニア
協議の結論(A'案: OPFS直書き+音声チャンク化)を記録済み。要点:

- fast経路(iOS 18.4+)に入れる端末は定義上OPFSも使えるはず(バージョン包含関係)
- 実装未着手。5項目(OPFSターゲット分岐/共有フロー/後始末/音声チャンク化/
  デコーダキュー水位)、半日弱の見込み
- 唯一の本質的リスク: `share({files:[OPFSのFile]})`が実機でメモリ死しないか。
  ここだけは優さんのiPhoneでしか確かめられない

### 3. Switcher実素材テストの残り

v1.39.18(Web Locksでの多重書き出し防止)以降、**優さんによる実機での
再テストが未実施**。特に確認したいこと:

- 1本ずつ(2タブ同時にしない)書き出せば最後まで通るか
- 手ブレ除外(v1.39.11)でカットが減りすぎていないか
- カメラ切替UI(v1.39.12・タップ2回)の操作感

### 4. Vlogの新しい上限のうち「写真」はUIが無い

`maxVlogPhotos`(登録8枚/ゲスト不可)を`limits.js`に値だけ定義したが、
Vlogには一般写真をインサート素材として使う機能自体がまだ無い(ロゴ画像とは別)。
将来この機能を作る時のための先行定義であることを明記済み。

### 5. 音声エンコードのメモリ保持(優先度低)

`encodeAudioPcm`が書き出し尺ぶんのFloat32(977秒で約196MB)を保持する実装は
未改修。v1.39.15のfastStart修正でメモリに余裕ができたため急ぎではないが、
iPhone対応(持ち越し2)に着手するなら同時に直す価値がある。

---

## 教訓・ハマりどころ(今日の追加分)

- **2タブ同時作業でメモリを取り合う**。Chromeは同一サイトのタブを同じ
  プロセスに載せる。「別々のエラーに見えても実は同じ原因」の実例が今日出た
  (RangeErrorとNotReadableErrorが同じ20:38に同時発生)。エラーメッセージの
  字面だけで原因を決めつけない
- **`fastStart: "in-memory"`は「ディスクに書く」設定と別物**。ターゲットを
  ディスクにしても、この指定が残っている限りメモリに全部溜め続ける。
  ライブラリのオプション名は额面通りに信じず、ドキュメントか実装を見る
- **無制限(Infinity)を画面にそのまま出さない**。`maxVlogInserts`等を
  `unlimited?Infinity:...`にした際、「あとInfinity本」と文字通り表示される
  regressionを引き起こした。`isFinite()`でガードする習慣をつける
- **`git show HEAD:`でのgrep一致は`</span>`等のタグを跨ぐと引っかからない**。
  「MarchinZ Vlog（開発中）」を`<span>MarchinZ Vlog</span>（開発中）`の形で
  入れたため、文字列としては連続していてもgrepの単純一致では0件になった。
  ブラウザでの実測(textContent)が正。grep一致数だけで安心しない
- **push確認ルールを新設**(CLAUDE.md必須ルール0番・deploy-guardスキルにも反映)。
  「1度の許可は1度の実行にしか使えない」。今日「一旦pushして」の後に
  6回連続pushした反省から

---

## 運用ルール(継続+更新)

- **push/デプロイは毎回かならず確認を取る**(2026-07-20新設・最重要)。
  コミットは都度作ってよいが、pushだけは優さんの明示的な許可を待つ
- push前は deploy-guard スキル必須 + `git show HEAD:<file>` で実体裏取り +
  bundle バックアップ。push はまとめて1回(Netlifyクレジット節約)
- Node無し。構文確認はブラウザで実読み込みする。スクリプト優先編集
- iOS Safari最優先(375px)。CLAUDE.md必須ルール8(UI/UXの設計思想そのものを
  iPhone前提で最適化)
- 検証は「ユーザーが実際にする操作」で行う。プレビューサーバはDrive配下を
  配信できないのでscratchpadへcp

## 関連メモリ・文書

`marchinz-project` / `switcher-director-update` / `marchinz-mobile-safari-first` /
`marchinz-deploy-flow` / `css-hidden-attribute-trap` / `drive-sync-commit-revert`

- `引き継ぎ書_2026-07-20.md`(今日の朝〜昼。Switcher実機バグ発見までの経緯)
- `Switcher書き出し問題_2026-07-20.md`(3つのエラーの詳細な原因分析・検証記録)
- `iPhone書き出し8分半_検討メモ_2026-07-20.md`(iOS対応の選択肢とA'案の設計)
