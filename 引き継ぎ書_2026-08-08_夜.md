# 引き継ぎ書 2026-08-08 夜

**本番 v2.8.0 (Beta) 稼働中（push済）。ローカルは v2.8.1 が作業中（未コミット5ファイル）。**
このセッションは Claude Code から Codex への移行と、Safari容量問題の調査が中心でした。

---

## いちばん大事なこと

### Safari容量問題の主犯が、ほぼ確定しました

2026-08-08 の実機診断（v2.4.0の管理者診断）と、外部のバグ報告が一致しました。

```
実機: FHD素材3本を通常取り込み・分析
  取り込み前 5.81 GB → 後 9.36 GB（+3.55 GB＝素材合計と一致）
  OPFS は前後とも 0 B / プロキシ作成なし / Safari完全終了後も 9.36 GB のまま
```

これは **WebKit の未修正バグ** と同じ症状です。

**https://bugs.webkit.org/show_bug.cgi?id=318572**（NEW・未修正 / iOS 26・Safari 26 / rdar://181340765）
> Photos picker を通したファイルは、そのサイズぶんが "Documents & Data" へ
> **永久にリークし、強制再起動でも回収されない**（`never reclaimed (survives force reboot)`）。
> 最終的に **picker が無言で失敗しはじめる**。

つまり:

- **①取り込みの内部コピーが主犯**。サイト側のコードでは消せない — この結論は正しかった
- **②OPFSの蓄積は、少なくともこの試行では無関係**（0 B）。掃除の対策は効いている
- 以前立てた**「プロキシの1時間据え置きが再発原因」という仮説は、この試行では否定**された
  （プロキシは1本も作られていなかった）。ただし4K素材では別の話になりうるので破棄はしていない

### 次の一手（いちばん価値がある）

バグ報告は **Photos picker（フォトライブラリ）経由**に限定して書かれています。
MarchinZ の入力は `accept="video/mp4,video/quicktime,.mp4,.mov,.m4v"` なので、
iOSでは「フォトライブラリ」と「ブラウズ（ファイルApp）」の両方が出ます。

**同じ素材で①フォトライブラリ / ②ファイルApp を分けて測ってください。**
②で漏れないなら、UIで「ファイルAppから選んでください」と誘導するだけで効きます。
これが「取り込みコピーを減らす工夫は未着手」だった項目に、初めてついた当たりです。**未検証。**

もうひとつの食い違いも未解決です:

| | |
|---|---|
| 2026-08-06 の観測 | Safari完全終了で解放された |
| 2026-08-08 の観測・バグ報告 | 終了しても戻らない |

経路（フォトライブラリ/ファイルApp）の違いで説明できるかもしれません。

詳細はすべて **`docs/SAFARI_STORAGE_ISSUE.md`** にあります（コミット `7112806`）。**まずこれを読んでください。**

---

## いま途中のもの（未コミット5ファイル）

```
index.html                                版番 2.8.0 → 2.8.1
tools/switcher/index.html                 ?v= を 2.8.1 へ
tools/switcher/js/storage-diagnostics.js  ★本体の変更
tools/switcher/js/ui.js                   1行
tools/qa/runner.html                      見張り1本を追加
```

**変更の趣旨**: 診断で「①基準値」を記録する前は、**OSのファイル選択を開かせない**ようにした。
理由は、iOSのpickerは**開いた時点で**動画容量ぶんの内部コピーを作りうるため。
onchange のあとに「基準値がありません」と止めても、容量はもう戻らない。だから入口で止める。

- `D.allowPicker()` を新設し、共通の選択入口より前で判定する
- 「② 選択だけを試す」ボタンは基準値が無いと `disabled` + 理由バッジ
- QAランナーにその見張りを1本追加

**この変更はWebKitバグの性質と整合的**です（pickerを開くだけで漏れる）。
コミットするかは優さんの判断。動作確認が済んでいれば版番はもう 2.8.1 に上がっています。

---

## このセッションでやったこと

### 1. Claude Code → Codex 移行（完了）

- `CloudeCode_u` 配下の **77プロジェクト**すべてに `CODEX_HANDOVER.md` と `AGENTS.md` を作成
- Claude Codeスキル **16件**を `~/.codex/skills/` へ移行（`.system` は無変更）
- `Codex_u/CODEX_PROJECT_DIRECTORY.md`（78行）と `CODEX_SKILL_DIRECTORY.md` を作成
- `Codex_u/AGENTS.md` の末尾に探索手順を追記
- **MarchinZ は `CursorLogs` 配下で移行スキャンから漏れていた**ので、
  `AGENTS.md` / `CODEX_HANDOVER.md` を作り、索引の先頭に登録した
- `Codex_u/CODEX_START_PROMPTS.html` + `Codexプロンプト集を開く.command`（22種のコピペ用）

### 2. 完全バックアップ（検証済み）

```
~/Movies/MarchinZ_FullBackup_2026-08-07/     2.1GB / 43,464ファイル
├── MarchinZ-repo-full.bundle   Git全履歴（復元テスト済み: 546コミット・4タグ）
├── repo/                       作業ツリー完全コピー
├── related/                    MarchCut / MzMosaic / ReAngle / MzPromo / VinaDelMar ほか
├── knowledge/                  設計判断と踏んだ罠の記録58件
├── skills/                     移行済みCodexスキル16件
└── README_復元手順.md
```

**Drive の外**に置いてあります（同期の巻き戻し対策）。**Driveの中へ移動しないでください。**
作成後に v2.3.0 時点のものなので、**いまの v2.8.x は含まれていません**（要・取り直し）。

### 3. 新しいスキル2本

- `test-teeth` — 試験に歯があるか（変異でFAIL確認・操作で測る・受け取り経路・fixture独立）
- `repro-first` — 実機報告を正とするデバッグ（受け取り経路 + 検証環境チェック表）

### 4. QAと push前チェックの軽量化

- QAランナーを2段に: **標準283件・約70秒** / `?full=1` で全件316件・約130秒
- `push_check.py` を新設（積みコミット・Drive巻き戻り・版番整合・QAの段を1コマンドで判定）
- 配信は `tools/qa/serve.sh`

---

## この環境の鉄則（必ず守る）

- **`git push` は本番自動デプロイ。毎回、優さんの明示確認を得る。1度の許可＝1度の実行**
  （Netlifyクレジット制。過去に上限到達でサイトが一時停止した）
- **push命令を受けたら版番のマイナーを上げてパッチを0に**（2.8.x → 2.9.0）
- **変更したJSは `?v=` をバンプ**し、`index.html` の `data-mz-version` も上げる
- **Google Drive同期がコミットを巻き戻す**。push前に `git show HEAD:<file>` で実体確認
  （`push_check.py` が自動でやる）
- **`body[data-mz-tool]` を書き換えると登録特典が黙って消える**（スコープは `data-mz-tool-scope`）
- QAは **desktopの窓**で回す（モバイル判定だと会員×端末の門が実力どおり落ちる）
- 一括編集は **Pythonスクリプト1本 + assert**

---

## 次にやるべきこと（優先順）

1. **フォトライブラリ経由 / ファイルApp経由の切り分け**（上記・いちばん効く）
2. 未コミットの v2.8.1 を確定させるか判断する
3. `docs/SAFARI_STORAGE_ISSUE.md` の「2026-08-06 と 08-08 の食い違い」を解消する
4. **バックアップを v2.8.x で取り直す**（いまのは v2.3.0 時点）
5. 実機確認の残り（引き継ぎ書_2026-08-07_夜.md の7項目のうち未消化ぶん）

## 未解決のまま残っていること

- **「被写体」の初期値は仕様上いまも初回「指定なし」**（軽い判定は固定/手持ちしか測っていない）
- OGPがツール別に分かれていない
- `reangle/index.html` だけ og:* と description が無い
- Vlogゲートが3実装に分裂
- **PlugDock と RemoteDesk は Claude Code 専用ツール**。Codex移行後に残すか要判断
- **VinaDelMar の経理集計**は Claude Code のセッションログを読む実装。移行後は動かない可能性
