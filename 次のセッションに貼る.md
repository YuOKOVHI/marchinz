010_MarchinZ の続き。まず 引き継ぎ書_2026-08-01_夜.md を読んでから着手して。

## いまの状態
- 本番 v1.64.0 稼働中（https://marchinz.netlify.app）
- 手元 v1.65.0（おまかせ専用の全画面 + 音の読み込み%）は**未push**
- QA は tools/qa/runner.html を 114件。着手前に1回通して現状を確認して

## 直近の経緯（要点だけ）
映像ツールを「書き出す道具」から「見る道具」へ寄せる改革を進めている。
最初の画面は2段: ①作る動画の種類（縦型/ワイプ/自動スイッチング）→ ②おまかせ/こだわり。
おまかせは動画を入れたら**タップ0回で書き出しまで自走**し、専用の全画面
（大きなプレビュー・進み具合・いま何をしているか・あと何分）だけを見せる。
決め打ちは 60秒 / 盛り上がり / おすすめ音声 / 傾き自動 / MarchinZカラー。

## 最優先でやりたいこと
1. **v1.65.0 の push**（許可を取ってから。1度の許可は1度の実行にしか使えない）
2. **iPhone実機での確認**（優さんの端末が要る。ここが本丸）
   - おまかせが最後まで走り、実際にMP4が書き出せるか（一度も成功を確認できていない）
   - 3本同時再生が10分持つか（「見る道具」化の前提。崩れたら改革は白紙）

## 守ってほしい進め方
- push と本番デプロイは**毎回かならず確認を取る**（Netlifyのクレジット制）
- Drive同期がコミットを巻き戻す。push前に `git show HEAD:<file>` で実体を見る
- 一括置換は件数を assert し、報告前に git blob で数える
- 本番の挙動は**本番の配信物**（curl）で測る。手元のコードで判断しない
- 直したら QA を通し、新しい見張りは「バグを戻すと落ちる」ことを変異で確認する
- 版番の書き換えは計23箇所（root index.html ×2 + tools/switcher/index.html ×21）
