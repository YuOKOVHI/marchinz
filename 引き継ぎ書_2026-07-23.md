# 引き継ぎ書 2026-07-23（MarchinZ）

## 現在地

- 本番: **v1.39.61**（https://marchinz.netlify.app/）
- ローカル HEAD: **v1.39.63**
- **未push が2件**（下の「最優先」参照）
- 作業ツリーと HEAD は一致（差分なし）

---

## 最優先：未pushの2件

```
f1fb967  v1.39.62  ファンの近道をモーメントに(3タイプとも近道1つ)
e14d8e3  v1.39.63  Switcher大改修(画質3モード/縦型配置/進捗/UI刷新)
```

どちらもブラウザ検証済み・コミット済み。**push は優さんの許可を毎回取ること**（1許可＝1実行）。
push 後は必ず `python3 check_production.py` で本番を確認する。

---

## 今日やったこと（v1.39.48 → 63）

### ユーザータイプ（ファン／プレイヤー／スタッフ）
- 3択でTOP・メニュー・下部タブバーの並びを最適化。**既定はファン**、既存ユーザーもファン
- ラベルは **ファン／保護者・プレイヤー／団体・スタッフ／クリエイター**（広報レビューで「保護者」「団体」の居場所が無い指摘を受けて改称）
- アカウントメニューの近道: ファン=モーメント / プレイヤー=MarchinZ Days / スタッフ=Switcher
- **Firestoreルールに `user_type` を追加してデプロイ済み**（忘れると無音で保存拒否）

### イベント登録の大改修
- **URLファースト**（URL欄を先頭＋大きな「URLから自動入力」）
- 読み取りエンジンv2: **文字コード検出**（旧版はUTF-8決め打ちでShift_JISが全滅）、JSON-LDの期間/会場/時刻、日付候補の複数提示、SNS URLへの案内
- 新項目: 終了日・会場名・開演時間（**rulesデプロイ済み**）
- 確信度を3段階で表示（自動／自動・要確認／推測）
- SSRFがリダイレクトで抜ける穴を塞いだ（`redirect:"manual"`＋1ホップずつ検査）

### YouTubeリスト不表示の根本対応
- **原因は表示側の欠陥**。データだけのpushはNetlifyがビルドせず、補完役の実行時リフレッシュを **#youtube ページが聴いていなかった**（7/18から潜在）
- 対策: 再描画リスナー追加／botは引けないとき前回データを保持／bot内で検証／`fix_channel_url.py` でURL変更を1コマンド化
- **知識の訂正: 名簿の正本は `site-nav.js`**。CSV/inlineはbot生成キャッシュ

### 本番ヘルスチェック（新設）
- `check_production.py` ＋ `.github/workflows/check-production.yml`（毎朝6時JST）
- ページ生存（**SPAフォールバックがあるので固有マーカーで判定**）・版一致・全チャンネルのロゴ・rawの生存
- **Netlifyクレジットは消費しない**（読むだけ・commitもpushもしない）

### Switcher 高速化
- 書き出し: **映るカメラだけデコード**（`neededIds`＋lazy skip）、HWエンコーダ明示、並行化
- 解析: WebCodecs逐次デコード化＋色統計の相乗り、シークは`reset()`
- **効果の実測（正直な数字）**: 書き出しはデコード量が約1/3。解析は8:30の実ケースで**約1.05倍**（サンプル間隔4秒＞HOP3秒で毎点シークになるため）
- 殺しスイッチ: `MC.exporter.SKIP_MIN = Infinity` / `MC.visual.forceSeek = true`

### Switcher UI/UX
- 素材カード縦積み（サムネは切り取らず枠内）、おまかせは解像度と長さだけ
- フロートプレビューは**下スクロール時のみ**
- 素材投入直後に「分析◯分、書き出し◯分かかりそうです」（**実測学習型**）
- **画質3モード**: SNS用720p（既定）／1080p／高画質（パソコン限定）
- 縦型に**カメラ配置（上/中/下）**の確認・入れ替え
- 「MP4を書き出す」→「動画を書き出す」、「音を取り出し」→「音を分析」
- 93%張り付きの解消（音声処理の実進捗で90→97%を埋める）

---

## 次にやること

1. **未push 2件を push**（許可を取ってから）→ `check_production.py` で確認
2. **iPhone実機での確認**（優さん）
   - 高速化の体感（見積り表示は1回使うと実測を学習する）
   - 画質SNS(720p)の見え方
   - 縦型のカメラ配置UI
3. **Switcher UI/UX の続き**（優さん「大幅に再構築」の残り）
   - 今回はリスタイル＋情報整理まで。画面構成そのもの（ウィザード化等）は未着手
4. 保留中の論点
   - Switcherの「YouTube向けの横型」という説明文（既定が720pになったので要検討）
   - ゲストがクリップ毎の微調整（±1s/役割/出番/撮り方）を触れる件（仕様確認待ち）

---

## 運用ルール（厳守）

- **push・本番デプロイは毎回かならず確認を取る。1度の許可は1度の実行にしか使えない**
  - 理由: Netlify はビルドクレジット制（7/12に上限到達でサイト停止の実績）
  - コミットは都度作ってよい。**止めるのは push だけ**
- **deploy-guard スキルを必ず通す**: バンドル退避 → md5を数秒あけて2回 → `git show HEAD:file` で裏取り → `git pull --rebase` → push
- **Drive がコミットを巻き戻す**ことがある。作業ツリーの grep だけを信じない
- **Firestoreルールのデプロイは push とは別**: `firebase deploy --only firestore:rules`
- iOS Safari 最優先。検証は 375px から

---

## ハマりどころ（再発防止）

- **`mll_profiles` に項目追加したら rules の allowlist 3箇所も直す**。忘れると無音で保存拒否
- **inlineデータを読む処理は `mz:data-refreshed` を必ず聴く**。初期化時の一度読みだとbot更新が反映されない
- **`display:flex` を持つ要素は `hidden` 属性が効かない**。`[hidden]{display:none!important}` を当てる
- **DOMのtextContentだけ見て検証しない**。白文字×白背景を2回見逃した。**computed styleで実測する**
- **このブラウザペインは非表示だと rAF・IntersectionObserver・CSS transition・scrollイベントが止まる**。
  時間計測は `performance.now()`、待ちは `dequeue` イベント駆動、判定関数は直接呼ぶ
- **ローカルサーバは新しいポートで**（SW＋HTTPキャッシュで旧JSが配られる）
- **VideoDecoder は tkhd 回転を適用しない**（`<video>` は適用済み）。縦撮り素材で全滅する

---

## 私（Claude）の反省

**目的ではなく途中の成果物ばかり検証していた。** CSVが直った・CIが緑・コミット実体OK…と proxy を確認する一方で、
「本番の画面に出ているか」を見ていなかった。IK ALUMNI の不表示はそれで数日気づけなかった。
ヘッダー3回失敗も、白×白バッジも、同じ型（描画を実測しなかった）。

**次から**: データやデプロイが絡む作業は、**本番URLを実際に叩いて対象が出ているまで「完了」と言わない**。
派生ファイルを触る前に「誰が生成し誰が読むか」を1分で確認する（「CSVが正本」という誤りをコミットに永久に残した）。
