# MarchinZ Switcher 書き出し問題 — 経過と懸念(2026-07-20)

上位モデルでの再検討用。**私(Opus 4.8)は実素材を読めない**ため、すべての修正は
「ログからの推論」と「模擬による検証」に留まる。実機で確かめたのは優さんのみ。
その境界をこの文書では明示する。

## 前提

| 項目 | 値 |
|---|---|
| 環境 | macOS / Chrome 150 / Mac Chrome |
| 素材 | `~/Desktop/自動整理/Timeline 1-3.mov` |
| 尺・解像度 | 1920x1080 / 1074.6s・1380.6s・1412.4s(18〜23分) |
| サイズ | 各 2.2GB / 2.8GB / 2.9GB |
| 音声 | LPCM 48kHz 2ch 24bit(自前リーダー経路) |
| 同期結果 | offset 94.2040s(conf 25.2)/ 315.0861s(conf 25.5) |
| 自動トリム | salute: 381.6s 〜 1359.2s(約977秒 = 16分半) |
| 書き出し方式 | fast(WebCodecs)/ H264:true AAC:true |

**重要**: 同期オフセットが 94秒・315秒と大きく、トリム開始が 381.6秒。
この「開始が0ではない」条件が、以下のバグを次々に表面化させた。

---

## 報告されたエラーと対応(時系列)

### ① v1.39.4 時点 — 「デコードが進みません: Timeline 3.mov」(縦型)

```
Error: デコードが進みません: Timeline 3.mov
  at MC.exporter.VideoPipe.frameAt (exporter.js:101)
```

**原因(確定)**: `VideoPipe.frameAt` の時刻基準の食い違い。
- 呼び出し側は `local = t - clip.offset`(クリップ先頭からの**絶対**秒)を渡す
- frameAt は最初に届いたフレームの timestamp を `firstTs` として引き、そこからの**相対**時間で比較
- `init(tIn - c.offset)` で途中(381.3秒)から始めるため `firstTs ≈ 381.3秒`
- 結果、要求位置からさらに同じだけ先(762.6秒)へ進もうとしていた

**対応**: v1.39.9。`demux.samples()` が返す `cts` はトラック内の絶対時刻なので、
`firstTs` を引く正規化そのものが不要だった。削除して絶対時刻で突き合わせる。
あわせて guard を「進捗があればリセット」に変更。

**検証**: 模擬(30fps / 開始381.3秒 / 尺1412秒)で裏取り。
- 修正前: 1コマ目の要求で **762.3秒地点**を返す(381秒ズレ)。11,448枚を無駄デコード
- 修正後: 381.3秒ちょうど。デコード40枚

**実機**: ✅ 解消を確認(次のテストで別エラーへ進んだ)

> トリム開始が0のときだけ偶然動くバグ。v1.39.2 の「おまかせに自動トリムを組み込んだ」
> 変更で表面化した。同じ原因で switch モードの「書き出しが真っ黒」も説明できる
> (offset を持つクリップが二重進行で素材の終端を超え、eof 後の最後のフレームを保持)。

### ② v1.39.12 — NotReadableError(縦型)

```
The requested file could not be read, typically due to permission problems
that have occurred after a reference to a file was acquired.
```

ログ上、同期・salute・colormatch までは通り、**書き出し開始直後**(3本の PCM 再読み込みの後)に発生。

**原因**: ❌ **未確定**。候補は2つ。
1. 2〜3GB×3本の並行読みによる一時的な失敗
2. macOS の TCC(プライバシー保護)— 素材が `~/Desktop/` 配下にある

**対応**: v1.39.13(推測に基づく対症療法)
- demux の読み取り6箇所を `MC.readSlice()` に集約し、150/300/450ms 空けて最大4回リトライ
- 失敗時は日本語メッセージへ変換(従来は DOMException の英文がそのまま画面に出ていた)
- 読み直しのたびに「ファイル名 @開始〜終了 (エラー名)」をログへ
- 書き出し冒頭で `preflightFiles()` を通し、素材が今も読めるかを先に確認

**検証**: 模擬のみ(2回失敗→3回目成功でリトライ通過、常時失敗で日本語化を確認)

**実機**: ❌ **未検証**

### ③ v1.39.12 — RangeError: Array buffer allocation failed(スイッチング)

```
RangeError: Array buffer allocation failed
  at new ArrayBuffer
  at Ue.g (mp4-muxer.min.js)
  at Rt.finalize (mp4-muxer.min.js)
  at MC.exporter.exportMP4 (exporter.js:458)
```

**原因(ほぼ確定)**: muxer が `ArrayBufferTarget` で完成MP4を丸ごとメモリに保持。
```
977秒 × 12Mbps ÷ 8 ≒ 1.47GB   ← 1つの ArrayBuffer を確保できずに失敗
```
書き出し自体は 19:49:10 〜 19:52:13 と3分走っており、**最後の finalize で落ちている**
= 映像・音声のエンコードは通っていた。

**対応**: v1.39.14
- 保存先を先に選ばせ、`FileSystemWritableFileStreamTarget` でディスクへ直接書く
  (同梱の mp4-muxer 5.2.2 が対応)
- `showSaveFilePicker` はユーザー操作の直後でないと拒否されるため、進捗表示や
  デコードを始める前、書き出しボタンの onclick 冒頭で取得
- AbortError(選択をやめた)は静かに中断
- 非対応環境(iOS Safari 等)は従来の ArrayBufferTarget へフォールバック
- 途中失敗時も writable を閉じる
- 直接書き込みでは blob を持たないため完了表示を分岐(`showDone` が `res.blob.size` 前提で落ちる問題も同時に修正)

**検証**: 模擬のみ(muxer の API 存在、showDone の両経路、推定サイズ 1.47GB の算出)

**実機**: ❌ **未検証**

---

## 同時に入れた変更(書き出し以外)

| 版 | 内容 | 実機 |
|---|---|---|
| v1.39.8 | 音声の初期選択を「おすすめ」に(表示と選択が食い違っていた) | 未検証 |
| v1.39.10 | 「3つまとめて選べます」を明示(機能は元からあった。UIの問題) | 未検証 |
| v1.39.11 | 手ブレを単独条件で失格(従来は `flipRatio > 0.45` との AND で穴があった) | ✅ ログで動作確認 |
| v1.39.12 | カメラ切替を「いま映っているカット」へ / カット⇄ディゾルブ・前と結合を削除 | 未検証 |
| v1.39.13 | Vlog を全員に公開 | ✅ 表示確認 |
| v1.39.14 | プレビューとカットの連動 / カメラはタップ2回 | 未検証 |

手ブレ除外の実機ログ(19:44:35):
```
384.1s〜 直前のカットを延長（人が写っていない×2 / 手ブレ×1）
401.4s〜 直前のカットを延長（人が写っていない×2 / 急パン・激ブレ×1）
→ 最終的に 137カット(ディゾルブ68)。除外しすぎではなさそう
```

---

## 今の懸念点(再検討してほしい順)

### 懸念1: ②の原因が未確定のまま対症療法を入れている ★最重要

`NotReadableError` に対してリトライを入れたが、**原因が TCC(権限)なら再試行では越えられない**。
エラー文の "permission problems that have occurred after a reference to a file was acquired"
は権限側を示唆しており、リトライは的外れの可能性がある。

一方で「解析(同期・salute・colormatch)は通っている」= 最初は読めていた。
途中から読めなくなる理由として権限は考えにくく、並行読み・リソース枯渇の線もある。

**検討してほしいこと**
- 素材を `~/Movies/` など Desktop 以外へ移して再現するか(切り分けの決定打)
- Chrome の File System Access API で `showOpenFilePicker` から取得したハンドルなら
  権限が安定するのではないか(現在は `<input type=file>` の File)
- 3本同時に開くのをやめ、逐次化・チャンク縮小で回避できないか
- そもそも縦型(v3)は3本を毎フレーム読む。ここが構造的に重い

### 懸念2: ③の直接書き込みに新たな失敗経路を作った可能性

- `showSaveFilePicker` を onclick 冒頭で呼ぶが、**その前に await が入るとジェスチャーが失効**する。
  現在は冒頭に置いたつもりだが、実機で「ユーザー操作が必要」と拒否されないか未確認
- 書き出し中にユーザーがファイルを消す/移動すると `writable.write()` が失敗する経路が増えた
- 途中キャンセル時、**書きかけの壊れた MP4 がディスクに残る**。閉じてはいるが削除はしていない
- iOS Safari は従来経路のままなので、**長尺では相変わらず落ちる**(未対応)

### 懸念3: 実素材での検証が私にはできない

ブラウザペインは大容量ファイルを扱えず、すべて模擬で確かめている。
模擬は「私が想定した条件」しか再現しないため、**想定外の条件を見落としている可能性がある**。
特に:
- LPCM 24bit の自前リーダー経路(`viaRawPcm`)は実素材でしか通らない
- 3本同時デコード時の実メモリ使用量を測っていない
- 977秒 × 30fps = 29,310フレームの長時間ループでの累積(フレーム解放漏れ等)

### 懸念4: 修正が積み上がって相互作用が読めなくなっている

今日1日で v1.39.7 〜 v1.39.14 の8版を入れた。うち書き出し経路に触れたのは3版。
**それぞれ単体では検証したが、組み合わせた状態での通し確認をしていない**。
特に v1.39.9(frameAt)と v1.39.14(直接書き込み)は同じ `exportMP4` の中にある。

### 懸念5: 縦型とスイッチングで症状が違う理由を詰めていない

- 縦型: NotReadableError(3本を毎フレーム読む)
- スイッチング: メモリ確保失敗(1本ずつだが尺が長い)

同じ書き出し関数なのに出るエラーが違う。**縦型の方が読み取り負荷が高い**という仮説は立つが、
検証していない。縦型が③の修正後も NotReadableError で落ちるなら、懸念1の線が濃くなる。

---

## シニアエンジニア監査の結果(v1.39.15・同日追記)

上記の懸念を受けて実施したコード監査で、**懸念2と③の評価が大きく変わった**。

### 発見1: `fastStart: "in-memory"` が③の真因だった

muxer 生成に `fastStart: "in-memory"` が指定されていた。これは「全チャンクを
メモリに溜めて finalize で一括書き出し」の指定で、**RangeError の真の原因**。
v1.39.14 のディスク直接書き込みは、この指定が残っている限り**実質無効**だった
(ターゲットがどこであれ、メモリに1.5GBを保持し続ける)。
→ `fastStart: false` に変更。mdat を先に書き moov を末尾に置くため、
チャンクは到着のたびにディスクへ流れる。v1.39.15 で修正済み。

### 発見2: 失敗時の後始末が「壊れたMP4を作る」側だった

FS Access API は swap ファイル方式で、`close()` は部分データの**コミット**。
懸念2に書いた「書きかけが残る」は誤りで、実際は逆 — 従来の後始末(close)が
壊れた書きかけを実ファイル化してしまう実装だった。`abort()` に変更。
OPFS で実測: 1MB 書いた後に abort → 実ファイルは 0 bytes(破棄成功)。

### 発見3: ②の有力仮説を TCC から「File 参照の失効」へ更新

素材フォルダ名が **「自動整理」**。優さんは CamSort / IngestBay 等の
自動整理ツールを持っており、**選択後にファイルが移動・変名・変更されると
Chrome の File 参照が失効し、遅れて NotReadableError になる**。
「解析は通ったのに書き出しで落ちる」という時系列とも整合する。
TCC 説は「最初の読みが通っている」事実と噛み合わないため後退。
→ エラーメッセージを「選び直してください」の案内に変更済み。
確定には実機での再現(自動整理を止めて書き出す)が必要。

### 検証方法(実素材なしでの限界を1段埋めた)

模擬オブジェクトではなく、**実際の Mp4Muxer + VideoEncoder で30フレームの
本物の書き出し**を回した。OPFS の本物の FileSystemWritableFileStream
(実運用と同一クラス)で finalize まで通し、生成 MP4 が <video> で再生できる
ことまで確認。ArrayBufferTarget フォールバックも同一バイト数で成功。

### 残る懸念(更新後)

- 懸念1(②の原因)は仮説が更新されたが**未確定のまま**。決定打は実機:
  v1.39.15 で再発したら、自動整理ツールを止める/素材を選び直して再試行
- 懸念3(実素材の通し検証不可)は不変。29,310フレームの長時間ループと
  実メモリは実機でしか分からない
- 音声エンコードが977秒分の Float32(約375MB)を保持する実装は未改修。
  fastStart 修正でメモリに余裕ができたため優先度は下がったが、記録しておく
- iOS Safari の長尺は引き続き未対応(realtime 経路は別実装のため影響なし)

## 次の一手(提案)

1. **v1.39.15 で両モードを実素材テスト**。結果で分岐:
   - 両方通る → 解決。残るは音声メモリと iOS 長尺(優先度低)
   - 縦型だけ NotReadableError → 自動整理ツールを止める/素材を選び直して再試行。
     それでも出るなら素材を Desktop 外へ移して切り分け
   - 保存先ダイアログが出ない/拒否される → ジェスチャー失効(監査では問題なしを確認済み)
2. ログに出る `読み直し n/4` と `preflight` の有無で、権限か競合かを判定
3. 必要なら**縦型の読み取りを逐次化**する実装を検討(3本同時をやめる)

## 関連メモリ

`marchinz-project` / `switcher-director-update` / `resolve-mov-demux` /
`marchinz-deploy-flow` / `drive-sync-commit-revert`
