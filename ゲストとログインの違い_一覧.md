# ゲスト／ログインの違い 一覧

**基準バージョン: v1.39.38（2026-07-21）**

このファイルは「いま実際にどう動くか」を実コードから起こした一覧です。
プラン説明ページや告知の下敷きとして使えます。

## 正本の場所（値を変えるときはここ）

| 対象 | ファイル | 備考 |
|---|---|---|
| ツールの上限（全部） | `tools/shared/limits.js` | ここが唯一の正本。3ツールが読む |
| ログイン状態の受け渡し | `auth.js` → `localStorage` | 本体がログイン時に印を書き、ツールが読む |
| 端末側の上限 | `tools/switcher/js/exporter.js` | メモリ由来。プランとは別枠 |

ツールは Firebase SDK を読まない設計（端末内完結・軽量が売り）のため、
本体 `auth.js` が `localStorage` に書いた印（`mz_member_v1` / `mz_admin_unlimited_v1`）を
`limits.js` が読んで判定しています。

---

## 1. クリエイターツール

### Switcher（自動スイッチング）・ReAngle（正面補正）

| 項目 | ゲスト | ログイン | 管理者・手元環境 |
|---|---|---|---|
| 取り込める動画の長さ | 5分 | **13分** | 無制限 |
| 書き出せる長さ（IN〜OUT） | 5分 | **8分30秒** | 無制限 |
| 写真の枚数 | 1枚 | **5枚** | 無制限 |

**取り込みが13分あるのに書き出しが8分30秒なのは意図的です。**
複数カメラの回し始めのズレ（実測で最大5分超）を吸収したうえで、
ショウ全体（約8分）を切り出せるようにするための差です。

### Privacy（顔モザイク）

| 項目 | ゲスト | ログイン |
|---|---|---|
| 動画の長さ | 10分 | 10分（同じ） |
| モザイク作業できる範囲 | 60秒 | 60秒（同じ） |
| 写真の枚数 | 1枚 | **5枚** |

動画尺に差がないのは、全編ではなく「選んだ範囲だけ」を処理する方式のためです。

### Vlog（インタビュー構成ツール）

> **現在は管理者以外ページを開けません（開発中）。** 下の値は公開後に効きます。

| 項目 | ゲスト | ログイン |
|---|---|---|
| 素材1本の長さ | 5分 | **10分** |
| インタビュー | 1人 | **3人** |
| インサート映像（Bロール） | 4本 | **8本** |
| ロゴ画像 | 使えない | **1枚** |
| BGM | 3曲 | 3曲（同じ） |
| 完成の長さ | 3分まで | **5分まで** |
| 写真（挿入素材） | 使えない | 8枚（UI未実装の先行定義） |

完成尺の下限は全員 3分01秒（`vlogMinSec: 181`）。
これは MarchinZ の YouTube 一覧に載せられる下限で、下回っても書き出しは止めず画面で知らせるだけ。

---

## 2. 本体サイト

| 機能 | ゲスト | ログイン |
|---|---|---|
| 大会動画・YouTube一覧の閲覧 | ○ | ○ |
| メディア（note記事）の閲覧 | ○ | ○ |
| メトロノーム・チューナー | ○ | ○ |
| Board（掲示板）の閲覧 | 一部 | 全部 |
| └ 「雑談・質問」カテゴリ | **見えない** | 見える |
| └ 投稿・返信・いいね | × | ○ |
| イベント（大会案内）の閲覧 | ○ | ○ |
| └ イベントの登録 | × | ○ |
| モーメント・ノートの投稿 | × | ○ |
| MarchinZ Days（練習記録・目標・連続記録） | × | ○ |
| マイリスト（動画・チャンネルの保存） | × | ○ |
| マイページ・プロフィール | × | ○ |

---

## 3. 押さえておきたい点

**機能自体は塞いでいません。** ゲストでも透かし入りで書き出せます。
制限しているのは長さ・枚数だけで、「使えない」のではなく「短く試せる」設計です。

**iPhone・iPadには端末側の上限が別にあります。**
完成MP4を丸ごとメモリに載せるしかないため、約8分41秒が物理的な限界です
（3.8Mbps × 260MB から算出）。**プランの上限と端末の上限は、厳しい方が効きます。**

v1.39.38 で、この2つのうち厳しい方を「おまかせ完了の時点」で判定し、
超えていれば「INから8分30秒に詰める」ボタンを出すようにしました。
書き出しボタンを押すまで黙っていると「15分待って書き出せません」になるためです。

**上限は目安であって、秘密を守る仕組みではありません。**
端末の負荷と誤操作を防ぐためのもので、長尺・大量の素材はブラウザのメモリを
使い切って落ちることがあります。

**管理者・手元環境（localhost / file:// / 私用IP）は上限なしです。**
本番ドメインでは決して `unlimited` になりません。

---

## 4. 値を変えるときの注意

- `limits.js` の1箇所を変えれば3ツールに反映されます（二重管理しない）
- ただし**画面の文言は別**です。`renderPlanNote()` と各ツールの
  `data-limit-note` / `data-limit-note-member` も揃えてください
- iOSのビットレート（`exporter.js`）を上げると、書き出せる尺が縮みます。
  「案内している上限が実際に書き出せること」を必ず確認してください
  （v1.39.36以前、5Mbpsで6分56秒しか出せないのに8分30秒と案内していました）
