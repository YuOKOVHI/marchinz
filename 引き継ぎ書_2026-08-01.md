# MarchinZ 引き継ぎ書（2026-08-01）

前の版: [引き継ぎ書_2026-07-31_夜.md](引き継ぎ書_2026-07-31_夜.md)

## 現在地

```
本番        v1.55.0
手元        v1.58.0   コミット12本が未push   QA 100/100 PASS
```

**本番にまだ出ていないもの**（優さんの指摘を直した分を含む）
- 「下ごしらえ」等の擬人化・料理の比喩の全廃
- 「落ちても続きから戻れます」の案内の全廃（計4か所）
- 「最初と最後を自動カット」「演奏の範囲を探す」の撤去
- ロゴの説明の位置と文言（「消せません」の廃止）
- 製品改革の実装4件（下記）

## この回でやったこと

### 優さんの指摘3件（2026-07-31 夜）

| 指摘 | 対応 |
|---|---|
| 「下ごしらえ」って何。文章が不快 | 料理・擬人化の比喩を全廃。下ごしらえ→音楽の解析、聴いています→解析しています、見比べています→調べています、整いました→分析が終わりました |
| 落ちたら結局戻れないから案内を消して | 4か所すべて撤去。仕組みは残すが約束はしない |
| 「はじめとさいごを探す」が意味不明・進捗なし・進まない | UIごと撤去。3つの指摘が全部当たっていた（重複・p.frozenで固定表示・同じ検出が3回）。検出結果をキャッシュ（実測: 3回呼んでも読み直し1回） |

### ロゴ（2026-08-01）

`layout.js:170,181,210` はプレビューと書き出しで**同じ関数**を呼ぶ。
ロゴは工程3から画面に出ているのに、説明は工程7の書き出し画面にあった。場所が違った。
- 説明をプレビューの直上へ（素材があるときだけ・17pxの1行）
- 「（消せません）」を廃止 → 「右上の MarchinZ ロゴは、完成した動画にも入ります」

### 製品改革（PM+シニアエンジニア+UI/UXデザイナー の3人協議に基づく）

提案書: [映像ツール_製品改革提案_2026-08-01.md](映像ツール_製品改革提案_2026-08-01.md)
仕様書: [映像ツール仕様書_2026-08-01.md](映像ツール仕様書_2026-08-01.md)

中心の転換は「**書き出す道具から、見る道具へ**」。
根拠: 最頻の用事（練習直後の反省会）にMP4は要らないのに、
iPhoneで最短3分・通常9分・まるごと25分待たせている。

| 段 | 内容 | 状態 |
|---|---|---|
| 0 | iPhone実機で3本同期→10分再生 | **未実施（優さんの端末が要る）** |
| 1 | 区間の色帯 | 完了 |
| 2 | 同期後すぐ再生画面へ | 未着手 |
| 3 | 傾きのゲート撤廃 | **判断待ち** |
| 4 | 書き出しを「いま見ている場面から」 | **判断待ち** |
| 5 | ファーストビューを「動画を選ぶ」へ | 完了 |
| ＋ | 見えていないカメラを再生しない | 完了 |

**見えていないカメラを再生しない**（段0のリスクを自分で下げた）
`play()` が表示中でない本まで回していた。`visibleClips()` を新設し、
何が映るかは `MC.neededIds(t)`（書き出しと同じ関数）を正本にした。
肝は `driftFix` ─ 0.5秒ごとに止めた本を起こし直すので、そこを直さないと効かない。
実測: 常時3本 → 平均約2.0本 / 解析前は1本。

**区間の色帯**
`sections.js` の5分類をシークバーへ描き、タップで頭出しできるようにした。
代表ラベルは `MC.sections.label()` の1箇所だけで決める
（UIが独自にスコアを比べると、カット割の判断と帯の見た目がずれる）。

**ファーストビュー**
起動時の3枚のモードカードを撤去。既定は自動スイッチング、
種類は「← 種類を変える」でいつでも。
★ `mode` を保存対象へ追加した ─ それまで保存しておらず、
入口から外すと「縦型を選んだのに次は横型」になるところだった。

## 次にやること

### 優さんの判断が要る3件

1. **傾きのゲートを外すか**
   3人の提案は「外す」。ただし 07-28「本人の目で1本ずつ確認する」と
   07-31「傾きは、やっぱり単独でページを」に衝突する。**推奨: 現状維持**
2. **開始位置の6候補を色帯へ統合するか**
   3人の提案は「統合」。超大型改修時の「スタート時点のオプションを5つ表示」に
   衝突するが、色帯が入ったいま同じ情報が重複している。**推奨: 統合**
3. **v1.58.0 を push するか**

### 実機でしか分からないこと

- **段0: 3本同期して10分再生し続けられるか**（改革の前提。崩れたら白紙）
- 8分素材の書き出し（一度も成功を確認していない）
- 20分素材の取り込み

## 注意（変わらず有効）

- **push と本番デプロイは毎回かならず確認を取る。1度の許可は1度の実行にしか使えない**
- Drive 同期がコミットを巻き戻す。push 前に `git show HEAD:<file>` で実体を見る
- 一括置換は途中で失敗すると後続が丸ごと未適用になる。件数を assert し、
  報告前に git blob で数える
- **本番の挙動は本番の配信物（curl）で測る。** 手元の修正済みコードを測って
  「直っている」と誤認した実例がある（2026-07-31 の無限再帰）
- このMacに node/npm は無い。テストはブラウザで `tools/qa/runner.html`
- 版番の書き換えは計22箇所（root `index.html` ×2 + `tools/switcher/index.html` ×20）

## QAの穴（正直な記録）

- 本物の `exportMP4Parts` を runner から走らせられない
  （iframe内でOPFSのwriter Workerが応答せずスイート全体が固まる）
- 試験データの落とし穴を1件記録: 素朴なLCGで作った「雑音」は実際には鋸波で、
  調波性 harm=0.529 → percussion 0.309 となり判定が「静か」に落ちた。
  コードではなく試験データの欠陥。決定的な xorshift32 へ差し替えた
