# MarchinZ (010_MarchinZ) プロジェクトルール

## バージョニング（2026-07-23 優さん指示・厳守）

版番は `data-mz-version` と各 `?v=` / `data-mz-app-v` を semver `major.minor.patch` で管理する。

- **push（本番デプロイ）命令を受けたとき** → **マイナーを上げ、パッチを 0 にする**
  例: 1.39.85 → **1.40.0**。その版を push する。
  = マイナー番号は「本番リリース回数」を表す。
- **push しないコミット（ローカルの積み上げ）** → **パッチを上げる**
  例: 1.39.79 → 1.39.80 → 1.39.81 …
  = コミットは都度作ってよい。止めるのは push だけ（deploy-guard 参照）。

現在地(2026-07-23): 本番 = 1.39.79 / ローカル = **1.41.0**(優さん指定で1.40を飛ばした)。次のローカルコミットで 1.41.1、次の push 命令でマイナー++。

版番を書き換える箇所（Switcher 系）:
- ルート `index.html` の `data-mz-version`
- `tools/switcher/index.html` の `data-mz-app-v` と全 `?v=`
- 他ツールの該当 `?v=`

## 運用

- push・本番デプロイは毎回かならず確認を取る。1度の許可は1度の実行にしか使えない（Netlify クレジット制）
- Drive 同期がファイル・コミットを巻き戻す。作業ツリーの grep を信じず、git blob を権威にする（deploy-guard の安全ワークフロー）
- **口調は敬語・呼び方は優さん**。間違ったときは謝り、次からの対策を書く（詳細は `AGENTS.md`）
- **他AIへの依頼文は1クリックコピー**（言語タグなしのコードブロック1つ）。詳細は `AGENTS.md` の「他AIへの依頼は1クリックコピーで出す」
- **POV（奏者視点）動画リストを触るときは、先に `docs/POV_LIST_GUIDE.md` を読む**。
  手で検索して拾うのは禁止（`scout_pov_videos.py` を通す）。団体名が他分野と衝突して
  無関係な動画が混ざる罠がある（Pacific Crest＝ロングトレイル、Troopers＝eスポーツ 等）。
  新着は毎日 06:00 JST に Actions が見張り、候補が出た日だけ Issue が立つ

## 検証・QA（2026-08-07 優さん指示「必要最低限に」）

- QAランナーは2段。**標準（既定）** = 重い実コーデック7群（③-d ③-e ④ ⑥ ⑱ ㉘ ㉟）を
  飛ばす（283件・実測約70秒） / **`?full=1`** = 全件（316件・約130秒）
- ふだんの再検証 = 関連 `?g=` か標準。**push前 = `python3 push_check.py` を回す**
  （積みコミット・Drive巻き戻り・版番整合を機械チェックし、QAの段も判定する。
  エンジン = tools/shared・switcher/js の exporter/visual/sync/media を触っていたら `?full=1` 必須）
- 配信は `tools/qa/serve.sh`（/private/tmp/mzqa へ rsync + :8931。`serve.sh stop` で停止）
- QAは **desktop の窓で回す**。モバイル判定（UA/タッチ）だと会員×端末の門の試験が
  実力どおり落ちる（ランナー最上部に警告が出る）
