# MarchinZ 引き継ぎ書 2026-07-19(ツール大改修+実素材で「落ちる」根治セッション)

## 現在地(このまま次セッションの前提にしてOK)

- ローカル main = `49af351`(**未push 1件**)。origin/main = 本番 = `caea908`
- 未pushの `49af351` = 実MOV(数GB)でタブごと落ちる問題の根治(下記)。push待ち
- 場所: `マイドライブ/CursorLogs/010_MarchinZ` / 本番: https://marchinz.netlify.app / GitHub: `YuOKOVHI/marchinz`
- 前回の引き継ぎ書: `引き継ぎ_2026-07-18b.md`

## 今回やったこと(コミット13個、caea908までは本番反映済み)

### 制限・公開範囲
1. 取り込み上限を3段階化: ゲスト(動画10分/写真1枚) < 登録ユーザー(20分/5枚) < 管理者ログイン・ローカル(無制限)。auth.jsがlocalStorageへ印(`mz_admin_unlimited_v1`/`mz_member_v1`)、`tools/shared/limits.js`が判定
2. **Privacyは一般公開を写真専用に**(動画の顔検出精度が実用未満のため)。動画はコードを全部残したまま、`videoAllowed()`(=MZ_LIMITS.unlimited)の関門で管理者+ローカルのみ「動画を選ぶ〈管理者テスト用〉」ボタンが出る

### Privacy(顔モザイク)大改修
3. 顔検出AI(約3MB)の遅延ロード化(ページを開いただけでは落ちてこない。初回転送3.5MB→0.15MB)
4. **顔検出をYuNetハイブリッドへ全面刷新**: 旧BlazeFaceは自撮り用で51px未満全滅→YuNet(ORT wasm)は21px級まで。実写比較 21人→27人。ORTは**1.18.0固定**(1.19+はiOS17でOOM死)。iOS16.4とiPadデスクトップUAはSIMD無効化
5. シーン/カメラ切替検出(MAD+ヒストグラム合議)→追跡を捨てて即高精度再検出。再検出中は全面ピクセレートの安全ベール(ctx.filterはiOS Safari非対応のため縮小拡大方式)
6. プロUI/UXデザイナー+シニアエンジニアのダブルレビュー→Critical/High全対応(書き出し中の検出並走による汚染、さかのぼりバッファ<検出間隔の未マスク窓、タップ抑制のシーン限定化、VideoFrameリーク等)

### Switcher
7. 動画1/2/3の個別スロットUI(3本上限)。モード別制約: 縦型動画=縦9:16固定+分割のみ/自動スイッチング=switch・wipeのみ+正方形廃止
8. 波形同期のFFTをyield分割(メインスレッド最大ブロック82ms化=iOS強制終了対策)
9. カメラMOVの「mp4a」コーデック名補正+ASC自前生成(AAC用)

### 共通・運用
10. **進捗表示コンポーネント `tools/shared/progress.{css,js}`(window.MZP)**: 確定/件数/不確定/凍結の4型。凍結型は「この間、数秒だけ画面が止まります」と事前予告+`MZP.paint()`で描画保証。完了は緑チップで残す。下部固定ミニバー(スクロール対策)。全重処理に配線済み
11. 2日おき自動更新: 新着の顔ぶれ指紋(sha256)が同じならGeminiを呼ばずファイルも書き換えない(無駄コミット根絶)

### 実素材で「絶対落ちる」を根治(49af351・未push)
12. 優さんの実素材 `~/Desktop/自動整理/Timeline 1-3.mov`(Resolve書き出し、H.264+LPCM、2.2〜2.9GB、17〜23分)でタブ死を再現→2原因を特定し根治:
    - **moovがファイル末尾**: demux初期化がmdat 2.2GBを読んでハング → トップレベルbox自前走査+moov直読み+mdatヘッダ16Bだけ食わせて正規スキップ。初期化227msに
    - **LPCM音声が5,157万サンプル**(1フレーム=1サンプル): mp4boxが全展開してメモリ死+WebCodecsにlpcmデコーダ無し → **moovからPCM trakを外科切除**してmp4boxへ、音声はチャンク表(2,147個)の自前リーダーで生読み(`src.pcm`/`pcmChunks()`/`pcmToFloat()`)
    - Switcherに同期用(8kモノ)+書き出し用(48kステレオ→AAC)のPCM直読み実装。Privacy/ReAngleにもdemux改修を移植(落ちないが書き出し音声は未対応=映像のみ)
    - **実測**: 3本の波形同期完走(T2=-220.85s 信頼度22 / T3=-315.09s 信頼度33、長い素材ほど早く録画開始で整合)。音声抽出25〜33秒/本、メモリピーク203MB

## ⚠ ハマりどころの教訓(次セッション向け)

- **QuickTime lpcm version2のstsdは `sizeOfStructOnly` を含む**: rate(f64)@base+24, ch@+32, bits@+40, flags@+44(base=version位置)。+20/+28/+36で読むと全部ゴミ→PCM判定が空振り→手術されず死ぬ(1回踏んだ)
- **mp4boxはmoovを食わせた瞬間に全トラックのサンプル配列を展開する**。数千万サンプルのトラックは食わせる前に切除するしかない
- **ブラウザペインのタブはhidden化してrAFが止まる**ことがある→プレビュー検証はMessageChannelポンプ(`MZ.yield`)+シーク駆動で。実機では問題ない
- **jsc**(`/System/Library/Frameworks/JavaScriptCore.framework/Versions/Current/Helpers/jsc`)でmp4box.jsを単体実行してデバッグできる(console/windowのポリフィル要。UTF-8パス不可→/tmpへコピー)
- ブラウザ検証でJSを更新したら **`script src="...?r=時刻"` の動的注入**で確実に差し替える(fetch cache:reloadだけでは実行中コードは変わらない)
- classic script内の`import()`は**スクリプトURL基準**で解決→`new URL(..., location.href)`でドキュメント基準に
- try内のconstはfinallyから見えず、`frames`は**window.frames**(グローバル)に解決される罠(1回踏んだ)
- 大容量ファイルのブラウザテストは`fetch→blob`不可(2GB超)→**Range対応サーバ+遅延slice Fileモック**(scratchpad/rangeserver.py)で

## 未着手・次にやるなら

1. **push**(`49af351`)— deploy-guard手順で
2. **実機確認(最優先)**: Timeline 3本を**実UIで**(ローカルで開けばファイル選択から全部動く。エンジンは検証済みだがUI統合は未)。iPhone実機での Privacy 写真フロー/YuNet速度/進捗表示の体感
3. Privacy動画の精度判断: 管理者テストで実マーチング動画を確認→ダメなら精度改善 or 写真専用継続
4. Privacy/ReAngleの書き出し音声PCM対応(現状は映像のみになる。SwitcherのencodeAudioPcmを移植すれば済む)
5. iPhoneで検出が固まる場合の切り札: `ort.env.wasm.proxy = true`(Worker実行、要実機検証)
6. 収益化Tier2統計の実データ実行(サービスアカウントJSON、優さん作業)/A8アフィリ未実装

## 運用ルール(継続)

- push前: `git ls-remote origin refs/heads/main`で実体確認→`~/Movies/marchinz-bundles`へbundle→`git pull --rebase`→push。**deploy-guardスキル必須**。push成否はツール出力でなくremote実体で裏取り
- 毎コミット後 `git show HEAD:<file>` でblob実体検証(Drive巻き戻り罠)
- スクリプト優先編集(一括編集はPython1本+assert)。Node無し(Python 3.9+venv)
- カラー絵文字禁止(Font Awesomeモノクロ)。iOS Safari最優先(375px)
- プレビューはscratchpadへcp→配信(Drive直接配信不可)。大容量はRange対応サーバ(rangeserver.py)

## 参考

- 前回: `引き継ぎ_2026-07-18b.md`(ツール3本改修+ツール捏造報告の教訓)
- テスト素材: `~/Desktop/自動整理/Timeline 1.mov`(2.2GB) / `Timeline 2.mov`(2.83GB) / `Timeline 3.mov`(2.9GB)
