# MarchinZ 映像ツール 仕様書

版: 2026-08-01（改訂2） / 対象コード: リポジトリ `CursorLogs/010_MarchinZ`、作業ツリー v1.58.0（本番稼働は v1.55.0）

この文書は**第三者のレビューを受けるために**書いた。
できるだけコードから直接起こし、確認していない事柄は「未検証」と明記する。
数値・定数は実ファイルから引いた値で、記憶で書いたものは無い。

---

## 0. レビューして欲しいこと

1. **設計の妥当性** — とくに書き出しパイプライン（§5）と、上限モデル（§3）
2. **見落としている失敗モード** — iOS Safari のメモリ制約下での挙動
3. **自動カット割の判定ロジック**（§6）が、マーチングの映像として妥当か
4. **§10 の既知の穴**が、致命的かどうかの判断

---

## 1. 全体像

### 1.1 何のためのツールか

マーチングバンド／吹奏楽団体の「ランスルー（通し練習）」を、複数人がスマホで
撮った動画から1本の視聴可能な映像に仕立てる。想定ユーザーは中高生と顧問で、
映像編集の経験は無いものとする。

### 1.2 ツール構成（4本 + 共有）

| ツール | 役割 | 規模 |
|---|---|---|
| **Switcher** | 複数カメラを音で同期し、自動でカットを割って1本のMP4に | JS 約9,800行 |
| **Privacy** | 顔に自動モザイク | JS 約2,400行 |
| **ReAngle** | 斜めから撮った映像を正面視点へ透視補正 | JS 約2,000行 |
| **Vlog** | 素材を枠に流し込んで団体Vlogを作る | JS 約3,400行 |
| **shared/** | 工程バー・進捗・上限・MP4デマックス・セッション保護 | JS 約1,900行 |
| **qa/** | ブラウザで走る回帰テスト（100件） | HTML 1本 |

以降は主に **Switcher** を記述する。他3本は共有部分を通じて同じ制約を持つ。

### 1.3 技術的な前提（動かせない制約）

- **ビルド工程が無い。** 素の HTML + JS をそのまま配信する。開発機に node/npm が無い
- **すべて端末内で処理する。** 動画をサーバへ送らない。これは訴求点であり要件
- **配信は Netlify の静的ホスティング。** `git push origin main` = 本番デプロイ
- **第一の対象は iPhone Safari 16+。** 検証は 375px 幅を基準にする
- 依存は WebCodecs / OPFS / mp4-muxer / ONNX Runtime(顔検出) / fft.js

### 1.4 主要な外部API

| API | 用途 | 代替の有無 |
|---|---|---|
| `VideoDecoder` / `VideoEncoder` (WebCodecs) | デコード・H.264エンコード | 無し（非対応端末は書き出し不可） |
| OPFS `createSyncAccessHandle` | 完成MP4の逐次書き込み | iOSでは**Worker内でしか使えない** |
| `showSaveFilePicker` | パソコンでのディスク直書き | 無ければOPFSへ |
| `AudioContext.decodeAudioData` | 音声抽出のフォールバック | WebCodecs音声抽出が第一 |

---

## 2. 画面の流れ（7工程）

`tools/shared/journey.js` が全ツール共通の工程バーを描く。Switcher の工程は
`tools/switcher/js/ui.js` の `MC.ui.STEP_RANK` / `initJourney()` に定義がある。

### 2.0 入口（2026-08-01 に作り替え）

以前は起動時に「作る動画を選ぶ」の3枚のカード（縦型 / ワイプカメラ /
自動スイッチング）を出していた。**これは製品の内部構造をそのまま質問にしたもので、
まだ何も見せていないユーザーに出力形式を先に決めさせていた。**
縦型かどうかは共有先で決まる話であり、撮った直後には分からない。

現在は起動したらすぐ「動画を選ぶ」。既定は自動スイッチング。
種類は上部の「← 種類を変える」からいつでも変えられる。

- `MC.S.mode` を **localStorage に保存する**（2026-08-01 に追加）。
  それまで保存していなかった ─ モード選択が毎回の必須画面で、
  そこで選び直していたため誰も気づかなかった。入口から外した以上、
  覚えないと「縦型を選んだのに次に開いたら横型」になる
- 保存済みのモードがあれば `chooseMode(saved, {silent:true})` で復元し、
  保存済みの preset / layout / border を上書きしない

```
1 動画を選ぶ  →  2 傾きを直す  →  3 同期と分析  →  4 音声を選ぶ
                                                        ↓
      7 書き出し  ←  6 仕上げ設定  ←  5 長さと開始位置
```

| id | 表示名 | 完了の条件 | 備考 |
|---|---|---|---|
| `mat` | 動画を選ぶ | 1本以上入っている | 最大3本 |
| `tilt` | 傾きを直す | **全本を本人がOKした** | 自動検出は全廃。1画面に1本 |
| `sync` | 同期と分析 | 波形同期が成功 | 動画1本ならスキップ |
| `audio` | 音声を選ぶ | 「この音で進める」を押した | 音のある動画が2本以上のときだけ |
| `length` | 長さと開始位置 | 「この長さで進める」を押した | 長さ3択 + 開始位置6候補 |
| `polish` | 仕上げ設定 | （任意。そのままでも進める） | 色・フィルターのみ |
| `export` | 書き出し | MP4が出力された | |

### 2.1 「1画面1操作」

現在の工程のパネルだけを表示し、他は `display:none` にする（`applySteps`）。
2026-07-23〜27 は「済んだ工程は1行に畳む」折衷案だったが、実測で
**押せる要素29個・ページ高2039px（画面の2.9倍）**になり、
「動画を書き出す」ボタンが同じ画面に2つ出ていたため捨てた。

### 2.2 傾き工程（設計判断が分かれうる点）

- 自動水平検出は**全廃**した。誤検出を本人が見抜けないため
- 1画面に1本だけ出し、動画1 → 動画2 → 動画3 と送る
- 全本をOKするまで先へ進めない（ゲート）
- 調整幅は **±3度・0.1度刻み**。±5度は画面が15%切れて実用外だった（実測）
- 補正は隅が出ないようズームするので周囲が切れる。**何%切れるかを明示する**

> レビュー観点: まっすぐ撮れている最頻ケースでも N 回タップを強制する。
> この摩擦は「本人の目で確認させる」価値に見合うか。

---

## 3. 上限モデル（2軸）

正本は `tools/shared/limits.js` 1ファイル。他所に条件のコピーを置かない。

### 3.1 二枚の門

```
   取り込み（入口）              書き出し（出口）
   maxSourceSec                 maxExportSec
   = 端末の技術的な天井          = 会員種別 × 端末のプランの壁
   スマホ 20分 / パソコン 40分    ゲスト 59秒
                                会員×スマホ 180秒
                                会員×パソコン 600秒
```

**入口で会員種別を見ない。** 見ると「長い動画は取り込めません」と門前払いになり、
「どこを何分使うか」を選ぶ機会そのものが消える。長く撮っておいて後で切り出す、
という本来の使い方ができなくなる。

### 3.2 端末判定

```js
const mobile = /iPhone|iPad|iPod|Android|Mobile/i.test(ua) || iPadDesktopUA;
const bigDevice = !mobile;
```

iPadOS はデスクトップUAを名乗るため `maxTouchPoints` で別途判定する。

### 3.3 書き出しの長さ（3択）

| id | 表示 | 秒 | 補足 |
|---|---|---|---|
| `short` | ショート | 59 | SNSに投稿しやすい長さ |
| `mid` | ミドル | 180 | 3分に収める長さ |
| `full` | まるごと | 510 | 演奏全体（実尺は `maxExportSec` で頭打ち） |

上限を超える選択肢は「鍵つき」で表示する。**使える選択肢は実尺を、
使えない選択肢は解除後の尺を出す**（全部が「59秒」と表示される欠陥を修正済み）。

---

## 4. 音の解析

### 4.1 音声の取り出し

`audio.js`。8kHz モノラル Float32 に落とす（32KB/秒）。
WebCodecs で抽出し、失敗したら `decodeAudioData` へ。
長尺は窓（`WIN_SEC`）で切って読む。

### 4.2 同期 — GCC-PHAT（`sync.js`）

- 位相変換相互相関。振幅の差に強く、拍手や歓声など広帯域の音で合いやすい
- 二段構え: 粗い窓で当たりを付け、その周辺を細かく見る
- 信頼度 `conf` を返し、低い本には「手動調整をおすすめします」と出す
- 失敗時は窓を変えて再試行する（先頭3分など）

### 4.3 拍（`beats.js`）

- スペクトル流束 → 自己相関 → 拍グリッド
- 窓 512 / ホップ 128 サンプル（64ms / 16ms）@8kHz
- 探索範囲 60〜200 BPM
- **自己相関ピークを放物線補間**して `lagF`（小数ラグ）にする
- グリッドは `lagF` ずつ進み、オンセットが強い位置へ減衰付きで引き寄せる
  （`ALPHA = 0.5`、`envMed` を超えたときだけ）

> 実測（128.7BPM / 8分素材）: 補間前 129.31BPM・最大ドリフト0.233秒 →
> 補間後 128.90BPM・最大ドリフト0.032秒

### 4.4 区間分類（`sections.js`）

ホップ0.5秒・窓1.0秒で、RMS・オンセット密度・スペクトル重心・調波性から
5種に分類する。

| ラベル | 意味 | 想定する画 |
|---|---|---|
| `quiet` | 静か | バラード。ディゾルブ向き |
| `solo` | 薄いテクスチャ + 調波的 | 単独奏者のアップ |
| `percussion` | 打撃的・調波性低 | バッテリーのグループショット |
| `pit` | 高重心 + 調波的 + オンセット密 | ピット（鍵盤打楽器）画角 |
| `tutti` | 音圧が高い | 全奏。引きの絵 |

**この分類は 2026-08-01 に UI へ格上げした。** それまでは自動カット割の内部に
しか無く、ユーザーからは一度も見えていなかった（他に無い強みなのに）。

- シークバーの下に**区間の色帯**として描く（1秒刻み・同じ性格は1本にまとめる）
- 帯をタップするとその場面へ飛ぶ
- 凡例は**その曲に実際に出てきた性格だけ**を並べる
- 代表ラベルの決定は `MC.sections.label()` の**1箇所だけ**。
  UI側でスコアを直接比べない（カット割の判断と帯の見た目がずれるため）

しきい値の出どころ:

| 性格 | しきい値 | 出どころ |
|---|---|---|
| `feature`（ソロ・ソリ） | 0.45 | `director.js:126,199` に準拠 |
| `pit` | 0.45 | `director.js:133` に準拠 |
| `quiet` | 0.55 | `director.js:272` に準拠 |
| `percussion` | 0.40 | **帯のための独自値**（director は重みとして使い、しきい値を持たない） |
| `tutti` | 0.60 | **帯のための独自値**（同上） |

独自値の2つは実素材で調整すること。

### 4.5 演奏区間の検出（`salute.js`）

DMのサリュート（敬礼）とアナウンスで演奏前は静か、という前提を使う。

- RMS包絡（100msホップ → 1秒平滑）
- 開始 = 「5秒以上の静けさ（下位25%点未満）の後、3秒以上続く高エネルギー（60%点超）」
- 終了 = 静寂〜ピークの間に置いた閾値を最後に超えた位置
- 前提が崩れる素材（曲中から始まる等）は簡易検出へ落ちる
- **結果は音声クリップ単位でキャッシュする**（id/名前/サイズ/オフセットが鍵）

> 2026-07-31 まで、この検出が「おまかせ」の中で2回、UIのボタンで3回目が
> 走っていた。毎回音声を全尺で読み直すため長尺で数分無言になっていた。
> UIのボタンは撤去し、キャッシュを入れた（実測: 3回呼んでも読み直し1回）。

---

## 5. 書き出しパイプライン（最重要）

`tools/switcher/js/exporter.js`（1,942行）。

### 5.1 二つの経路

```
exportMP4(onProgress, saveHandle, opts)
  │
  ├─ 分割が使える？（下記4条件すべて）→ exportMP4Parts()
  │     ・?nopart が付いていない
  │     ・ディスク直書き(saveHandle)ではない
  │     ・OPFS が実際に書けると実測済み
  │     ・書き出し尺 > 135秒（PART_MIN_TOTAL）
  │
  └─ それ以外 → 従来の一気書き（短尺・PC直書き・OPFS無し端末）
```

**設計上の要点:** 分割へ入るかどうかは**引数**で渡す。モジュール変数に持たせると、
呼ばれた側の初期化で印が消えて無限再帰する（§10.1 に実害の記録）。

### 5.2 分割書き出し

| 項目 | 値 |
|---|---|
| 1パートの長さ | 90秒（`PART_SEC`） |
| 分割する下限 | 135秒（`PART_MIN_TOTAL`） |
| パートの中身 | 映像のみMP4。OPFSへ書く |
| 結合 | `addVideoChunkRaw` で**デコードせず**再mux |
| 音声 | 結合後に全尺を1回（AACプライミングをパート間に持ち込まないため） |
| 台帳 | OPFS `mzjob.json`。ジョブidは素材キー+範囲+寸法+ビットレート+カット割+見た目のハッシュ |

パートごとに VideoPipe×3・VideoEncoder・muxer を**全部作り直す**。
これがメモリ蓄積のリセットになる。作り直しのコストはほぼゼロ
（実測: 600コマを5回作り直して109ms、連続で130ms）。

**結合時の不変条件**（破れたら例外を投げて止める）:
- パート間で avcC（デコーダ設定）が一致すること
- 各パートから読めたサンプル数 == そのパートにあるはずのコマ数
  （突き合わせないと「90秒まるごと欠けた500MBのMP4」が完走と見分けられない）
- 結合オフセットは「パート番号 × コマ数」から計算する
  （サンプル長の合算だと丸め誤差が後段へ累積する）

### 5.3 画質とビットレート

```js
QUALITIES = { full: { label:"高画質", scale:1 },      // 1080p
              light:{ label:"標準",  scale:2/3 } }    // 720p
```

| 経路 | 標準 | 高画質 |
|---|---|---|
| OPFS / ディスク直書き | 8 Mbps（1:1は6） | 12 Mbps（1:1は8） |
| 旧経路（どちらも使えない端末） | iOSは 3.8 Mbps 固定 | 同左 |

fps は **30固定**。4K出力は非対応。

### 5.4 デコードのスキップ

`SKIP_MIN = 4.0`。あるカメラへの要求が前回から4秒以上先へ跳んでいれば、
その間そのカメラは画面に出ていないので、間のフレームをデコードしない。
`?noskip` で無効化でき、切り分けに使う。

### 5.5 失敗したときの振る舞い

- **分割の入口で落ちたら**（まだ1コマも書けていない）、従来の一気書きで自動的にやり直す
  （`shouldFallbackToLegacy`。中止・1パート以上完了・完成済み は対象外）
- **足跡を残す。** `_markPhase()` が到達段階を sessionStorage に書く。
  タブごと落ちても残るので、「押した直後に落ちた」の中身が分かる
- 失敗時は `showErrorLog` が版番・UA・素材・レイアウト・直近ログを1枚にまとめる

### 5.6 「続きから再開」について（重要な方針変更）

分割の完了パートは端末に残り、条件が揃えば次回スキップされる。
**しかしこれをユーザーに案内しない。** 2026-08-01 に案内を全廃した。

撤去したもの（4か所）:

| 場所 | 文言 |
|---|---|
| 書き出し画面 | 「同期とカット割は保存済み（中断しても続きから再開できます）」＋手順3つ |
| 書き出し中の全画面 | 「前回のつづきから ─ ◯分ぶんは終わっています」 |
| 中断の案内 | 「選び直せば書き出しからやり直せます」「そこから続きます」 |
| 素材の画面 | 「前回のつづきがあります／この順番で選び直すと分析をやり直さずに書き出しへ進めます」 |

最後の1つは言い回しが違ったため 7/31 の撤去から漏れ、入口を作り替えた
2026-08-01 に**第一画面でいちばん目立つ塊**へ繰り上がっていた。

復元そのものは残す。実際に起きたときだけ `renderRestoreNote` が
「◯◯を復元しました」と**起きた事実**を報告する（約束ではなく結果）。

理由: 実際には戻れなかったという報告があり、戻れると書いておいて戻れないのは
黙っているより悪い。仕組みは残す（効いたときは黙って速くなる）。

> レビュー観点: 「動くかもしれない機能を、黙って持つ」ことの是非。
> 廃止すべきか、原因を突き止めて案内を復活させるべきか。

---

## 6. 自動カット割

`director.js` が統括し、`visual.js`（映像）と `sections.js`（音）を材料にする。

### 6.1 映像側の評価（`visual.js`、885行）

3秒ホップでサンプルし、各カメラについて次を測る。

| 指標 | 定数 | 意味 |
|---|---|---|
| グローバル動き | `TH_RAPID 0.28` | これ以上は急パン・激しいブレ |
| ブレ量 | `TH_SHAKE 0.11` | これ以上は手ブレ（単独で失格） |
| 意図的なパン | `TH_PAN 0.05` / `TH_PAN_RATIO 0.50` | 方向のそろった動きが半分超なら「振っている」 |
| カメラ残差 | `TH_CAM_RESID 0.55` | 全体動きを差し引いた残り |
| 被写体の有無 | `TH_EMPTY 0.12` | 顔が取れず動きも少なければ「人がいない」 |
| 近傍窓 | `NEAR_SEC 8` | 前後8秒も静止していて初めて「引き」と判定 |

グローバル動きは中央領域のSAD最小探索（±8px・1px格子）＋放物線サブピクセル補間。
**整数値は残差補償に、小数値はブレ量の判定に使う**（1px量子化でブレを取り逃していた）。

顔検出は YuNet（ONNX）を Worker で回す。2サンプルに1回。

### 6.2 見どころ候補（`highlight.js`）

開始位置の候補を最大5つ出す。

| key | 表示 | 選び方 |
|---|---|---|
| `start` | スタート | 位置で決まる（演奏の開始） |
| `climax` | 大盛り上がり | 全奏で最も大きい位置 |
| `ballad` | バラード | 静かでゆったりした位置 |
| `drumline` | ドラムライン | 打楽器が多く鳴っている位置 |
| `solo` | ソロ | 特定の奏者が抜かれている位置 |
| `finale` | フィナーレ | 位置で決まる（演奏の終わりまで） |
| `manual` | 自分で選ぶ | スライダー。拍にスナップする |

**席順は KINDS の定義順で固定する。z値は順位ではなく足切り（`Z_MIN = 0.8`）。**
z順に並べると「大盛り上がり」が落ちる曲が出る。

候補どうしの最小間隔 = `max(8秒, min(尺×0.45, 演奏長÷7))`。
選んだ位置は ±1.0秒以内の拍へスナップする。

### 6.3 レビューして欲しい点

- 閾値はすべて手調整で、実素材が数本しかない。過学習していないか
- 「引き」の失格条件（`noSubject` + 近傍の静止）は、
  マーチングの「全体像を撮る引きのカメラ」を誤って落とさないか
- ホップ3秒は、6/8拍子の速い曲で粗すぎないか

---

## 7. レイアウトと合成

- プリセット: 9:16 (1080×1920) / 16:9 (1920×1080) / 1:1 (1080×1080)
- モード3種: 縦型 / ワイプカメラ（メイン固定+小窓最大2） / 自動スイッチング
- 傾き補正は隅が出ないようズームする（layout.js の z と ui.js の `tiltCropPct` が同式）
- ウォーターマークは右上に薄く（`layout.js:170,181,210`）。
  **プレビューと書き出しで同じ関数を呼ぶ**ので、プレビューに見えているものが
  そのまま完成形になる。説明はプレビューの直上に1行置く ─
  以前は工程7の書き出し画面で初めて断っており、場所が違っていた。
  文言は「消せません」という禁止の言い方をやめ、事実の記述にした（2026-08-01）

## 8. 色合わせ（`colormatch.js`）

音声に使うカメラを基準に、他のカメラの色を寄せる。強さは0〜1で既定0.8。
2本以上のときだけ走る（1本では意味が無く、実装が例外を投げる）。

## 9. QA（`tools/qa/runner.html`）

ブラウザで開くだけで走る。node不要。素材は Python で合成した 3.6KB のMP4。
**100件**を24群に分けている。

主な群:

| 群 | 内容 |
|---|---|
| ⓪〜③ | 共有デマックスの一本化、「静かな黒抜け」の検知、変異テスト |
| ⑥ | 本物のH.264を実行時に生成して画素レベルで検証 |
| ⑧ | 分析前・作業中に押せてはいけないボタン |
| ⑪ | 傾き工程のゲートと画面遷移 |
| ⑭ | 上限（会員種別 × 端末） |
| ⑰ | 書き出し範囲とカット割の整合（不変条件） |
| ⑱ | 分割書き出しの結合（avcC一致・45コマの画素照合） |
| ⑲ | 表示と実態が食い違わないこと |
| ⑳ | 自動カット割の判定ロジック |
| ㉑ | 見えていないカメラを再生しない（平均本数を実測して固定） |
| ㉒ | 区間の色帯（音の性格を言い当てる・UIが独自判定しない） |
| ㉓ | ファーストビュー（種類を覚える・入口では聞かない） |

**運用ルール:** 新しい見張りを足したら、必ず「バグを戻すと落ちる」ことを
変異注入で確認する。落ちない見張りは撤去し、穴として明記する。

---

## 10. 既知の欠陥・未検証

### 10.1 直近で確定した重大欠陥（修正済み・v1.55.0で本番反映）

**書き出しが1秒たたずに落ちる（無限再帰）**

```
exportMP4()            _partsFallback = false   ← 1行目で必ず消す
  └ exportMP4Parts()
        └ 容量チェック失敗 → _partsFallback = true
          └ exportMP4() → _partsFallback = false  ← 呼ばれた側が即座に消す
            └ また分割へ …  無限
```

再帰を防ぐ印をモジュール変数に置き、それを**呼ばれた側の1行目でリセット**していた。
往復中は1コマも書かないので記録が残らず「ログも出ない」。

対照実験: 本番と同じ形は51回で打ち切り、修正版は exportMP4=2 / 分割=1 で着地。
発生条件は「書き出し135秒超」かつ「容量チェックが通らない」。

**教訓（レビューでも見て欲しい）:** 呼び出し側が立てた印を呼ばれた側が
初期化する形は、再入ガードとして必ず壊れる。状態は呼び出しの寿命に合わせる。

### 10.2 QAで守れていないところ（正直な穴）

- **本物の `exportMP4Parts` を runner から走らせられない。**
  iframe内でOPFSのwriter Workerが応答せず、スイート全体が固まる。
  分割からのフォールバックは「判定の表」と「戻り道に `noParts` が付いていること」で
  薄く見張っているだけで、経路の接続そのものは守れていない
- 「容量不足で分割から降りても無限再帰しない」の見張りを3度書き、3度とも
  変異が素通りした（`preflightFiles` / `_releaseAnalysisWorkers` / `exportDims` が
  両経路で共有されており、フォールバック経路だけを通す試験が組めない）。撤去済み

### 10.3 未検証

- **iPhone実機での8分素材の書き出し**（これが本丸。一度も成功を確認していない）
- iPhone実機での20分素材の取り込み
- 分割書き出しの再開が実際に働くか（働かないという報告がある）
- ReAngle の実機確認

### 10.4 保留中の設計判断

| 項目 | 現状 | 論点 |
|---|---|---|
| 既定画質 | 720p（標準） | 1080pを既定にすべきか |
| 冒頭の前振り | 8秒固定（UIは撤去） | 可変にすべきか |
| Privacy の動画対応 | 管理者限定 | 一般開放の可否 |
| MarchinZカラー | 既定ON | 押しつけになっていないか |
| fps | 30固定・4K非対応 | 需要があるか |

---

## 13. 再生（2026-08-01 新設）

製品改革により、再生は「書き出しの下見」ではなく**製品の中心**になった。

### 13.1 回すクリップを絞る

以前は `playClips()`（表示中の素材すべて）を**全部同時に `video.play()`** していた。
自動スイッチングでは画面に出るのは1台なのに、裏で3本が回り続けていた。

現在は `MC.preview.visibleClips()` が「いま回すべきもの」を決める。

```
回すもの = MC.neededIds(t)              いま画面に描かれるカメラ
         + 次のカットのカメラ            切替の PREROLL(2.0秒) 前から
         + 音声担当                      tick が currentTime を読む時計そのもの
```

- **何が映るかの正本は `MC.neededIds(t)`**。書き出しが
  「出番のないカメラをデコードしない」判定に使うのと同じ関数を使う。
  ここで独自に条件を書くと `drawComposite` と規則が2実装に割れ、黒コマになる
- `driftFix()`（0.5秒ごと）は、集合に居ないクリップを**止める**。
  以前は止まっている本を片っ端から起こしていたため、
  「見えていない本を止める」だけでは 0.5秒後に全部復活していた
- 先読みは必須。止まった状態から `seek` すると切替の瞬間にコマが飛ぶ

実測（3カメラ・6秒ごとの切替）: 常時3本 → **平均約2.0本 / 解析前は1本 / 最大3本**。
ワイプ・分割レイアウトは全員が映るので3本のまま（正しい）。

### 13.2 満たすべきこと

- 3本同時再生でコマ落ちしない（**iPhone実機で未計測 — §10.3**）
- カット切替が再生を止めない
- 区間色帯のタップで頭出しできる
- カット割が無くても再生できる（`cutAt` はカット割が空なら音声担当を返す）

---

## 11. 文章の方針（2026-08-01 に全面見直し）

- **常用漢字を使う。** ひらがな書きは読み手を子ども扱いする
- **言い切る。** 「〜してください」を乱発せず、見出しは名詞句
- **料理の比喩・擬人化をしない。**
  （廃止例: 下ごしらえ / 音楽を聴いています / 色を見比べています / 整いました）
- **内部事情を言い換えない。** 「内部の巻き戻しをやめます」ではなく
  「カメラを切り替えるたびに映像を読み直します」
- **できないことを、できると書かない。** これが最優先

---

## 12. 開発上の落とし穴（環境固有・共有のため記載）

1. **Google Drive 同期がコミットを巻き戻す。** push前に `git show HEAD:<file>` で
   実体を検証する。作業ツリーの grep を信用しない
2. **一括置換は途中で失敗すると後続が丸ごと未適用になる。** 件数を assert し、
   報告前に git blob で数える
3. **本番の挙動は本番の配信物で測る。** 手元の修正済みコードを測って
   「直っている」と誤認した実例がある（§10.1）
4. **`/tools/shared/*.js` に `?v=` が付いていない。** ローカル検証で
   ブラウザが旧版を掴む。DOMではなく配信物を `curl` で確かめる
5. **プレビューサーバは Drive 配下を配信できない**（macOSのTCC）。
   scratchpad へコピーして配信する

---

## 付録A: 状態オブジェクト `MC.S`（永続化される）

```
clips[]          Clipの配列（最大3）
mode             "vertical" | "wipeCam" | "switch"
preset           "9x16" | "16x9" | "1x1"
layoutId         レイアウトid
audioClipId      書き出し/再生に使う音声のクリップ
refClipId        同期の基準クリップ
slots[3]         スロットiに表示するクリップid
trimIn/trimOut   書き出し範囲（グローバル秒）
showIn/showOut   音で見つけた「演奏そのもの」の範囲
                 ★ trimとは別に持つ。混ぜると選び直すたびに範囲が縮む
exportPreset     "short" | "mid" | "full"
startKey         開始位置の候補キー
startAt          startKey==="manual" のときの絶対秒
lengthDecided    「この長さで進める」を押したか
cutList[]        [{t, clipId, trans:'cut'|'dissolve', dur}] 昇順
beatsPerBar      4
cutLevel         1:少なめ 2:おすすめ 3:多め
wipe*            ワイプの構成
colorOn/Strength カラーマッチ（既定ON / 0.8）
horizonOn        傾き補正（既定ON）
filterId         既定 "marchinz"
autoTrim         最初と最後の自動カット（既定ON・UIは撤去済み）
border*          境界線（既定ON / 白 / 2px）
```

クリップの同一性は **ファイル名 + サイズ + 更新日時** で判定する。
LINEで送り直した動画・別端末へコピーした動画は「別の動画」になる。

## 付録B: 用語

| 語 | 意味 |
|---|---|
| ランスルー | 本番と同じ通し演奏の練習 |
| サリュート | ドラムメジャーの敬礼。演奏開始の合図 |
| バッテリー | 動きながら演奏する打楽器隊 |
| ピット | 舞台前に固定配置する鍵盤打楽器群 |
| ソリ | 複数人による旋律の聴かせどころ |
| DM | ドラムメジャー。指揮者 |
