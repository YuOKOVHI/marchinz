# 010_MarchinZ 引き継ぎ書 2026-07-21

新しいセッションの最初の1通に「現在地」と「持ち越し」を貼ってください。
本書がこの日の正本です（前日は `引き継ぎ書_2026-07-20夜.md`）。

## 現在地

- 本番・ローカルとも **v1.39.38**（`31f29e0`）で一致。作業ツリークリーン
- この日 v1.39.25 → v1.39.38 まで進行。push は6回、すべて本番反映確認済み
- **push/デプロイは毎回かならず確認を取る**（1度の許可＝1度の実行）は継続

---

## この日やったこと

### 1. Vlog 自動編集エンジンを実装 → v1.39.27

前日まで planner.js すら無く「近日公開予定」と案内するだけだった本体機能を実装。

- `planner.js` … 構成テンプレ → 「ブロック→素材ID＋使う区間」の設計図を作る
- `compose.js` … プレビューと書き出しで**同じ描画関数**を通す（見たものが出る）
- `preview.js` … WebAudio 経由で再生（iOSは要素音量が効かないため）
- `exporter.js` … WebCodecs高速経路＋実時間経路。音はミックスバスで合成

### 2. Vlog 大改修「インタビュー構成ツール」へ → v1.39.32

優さんの「期待外れ、これはユーザー使ってくれない」を受けて設計転換。

- **主役は語り。インタビュー必須**、Bロールは飾り（無くても成立）
- **Bロールは語りの上に被せるのが基本**（音は語りが継続・Bロール側は無音）。
  頭4秒と締め3秒は必ず顔へ戻す。**同一素材は1シーンに1回まで**（validateで強制）
- 独立したBロールカットは「つかみ・場面転換・本番・余韻」だけ
- 3モードを語り比率59〜66%で再構築
- ロゴは右上7.5%へ小型化・余白を詰め、**エンドカード以外ずっと表示**

### 3. Switcher スマホUI大改修 → v1.39.32

- プレビューを**右上フロート**化（作業領域が下に見える）
- IN/OUT を「🔻IN（緑）／🔻OUT（赤）」に簡素化＋色分け
- **カット切替モード**新設（全画面・実フレームのサムネ一覧・1タップでカメラ変更）
- おまかせ完了で、同じボタンが緑の「動画を書き出す」に化ける

### 4. ヘッダー・フッターを本体と同じに（3回やり直し） → v1.39.30/34/35

**3回かかった。原因は私の検証方法の誤り。**
前2回は本体のCSSを読んで移植したが、**モバイルの実描画を測っていなかった**ため
かえって崩した（ロゴにアバターが重なる／ハンバーガーが2つ／フッターが2列＋1列）。

3回目に本体を375pxで開いて**実測**し、その数値に合わせて解決:

| 項目 | 本体(375px実測) | v1.39.34の誤り |
|---|---|---|
| タグライン | display:none | 表示していた |
| アカウントバーガー | 0x0(非表示) | 表示していた |
| ロゴ | 左寄せ 241×88 | 中央寄せ 88×32（height:32pxで潰れ） |
| フッター | grid 3列 | flex wrap |
| バナー | nowrap 1段 | wrap |

### 5. iPhone実機フィードバック5件 → v1.39.36〜38

- プレビューがジャーニーバーの下へ潜る → **バーの実高さを `--mz-journey-h` へ書き出し、
  その真下に配置**（z-indexで競わせず「重ねない」方針に）。横スクロールも防止
- 動画取り込み後、「おまかせで開始」まで自動スクロール（見えている時は動かさない）
- 進捗に**終了予定時刻**を併記（「残り約10分 / 7:42頃おわり」）＋ラベルを脈打たせる
- **「15分待って書き出せません」の根治**（下記）

### 6. その他

- YouTube「ICHIKASHI ALUMNI CGT」のリンク切れ修正＋**検出スクリプト新設** → v1.39.29
- ツールカードの安心バッジ/CTA削除、見てほしい人を5択に → v1.39.25
- Vlogを管理者以外クリック不可に（3経路すべて封鎖） → v1.39.30
- Vlogの表示順を4番目（最後）に → v1.39.31

---

## この日いちばん重要な学び

### 「8分30秒まで書き出せます」が嘘になっていた

**設定した2つの数字が噛み合っていなかった。**

```
会員の書き出し上限   8分30秒 (510秒)   ← v1.39.22で設定
iPhoneのメモリ上限   260MB
当時のビットレート    5Mbps
  → 260MB ÷ 5Mbps = 6分56秒しか出せない
```

案内どおり8分30秒を出せるよう、**iOSのビットレートを3.8Mbpsへ**
（510秒 × 3.8Mbps = 254MB < 260MB）。

さらに悪かったのは**書き出しボタンを押すまで黙っていた**こと。
→ **おまかせ完了の時点で警告**し、「INから8分30秒に詰める」ボタンを出すようにした。
判定は「端末の物理上限」と「会員種別の上限」の**厳しい方**で行う。

**教訓: 上限を決めたら、その値が実際に通ることを実測すること。**

---

## 持ち越し・未解決（優先順）

### 1. iPhoneでの実機確認（優さんの操作が必要）★最優先

私のブラウザペインはiOSではないため、以下は実機でしか確かめられない:

- v1.39.36〜38 のレイアウト修正（プレビューがバーの真下に来るか）
- **8分30秒の書き出しが実際に通るか**（今回の要）
- iOSの再生解錠（v1.39.29で実装、まだ実機未確認）
- Wake Lock が効くか／実時間書き出しの完走
- **iPhone標準のHEVC素材を読み込めるか**（未検証。最初に踏む可能性が高い関門）

### 2. ICHIKASHI ALUMNI CGT のデータ再取得

URLは修正済み（`@ikalumni_2022`・HTTP 200）だが**中身が空のまま**。
YouTube APIキーがこのMacに無く、`gh` CLIも未インストールのため私からは実行不可。

- **GitHub Actions から手動実行**（Actions → 「YouTube + note refresh」→ Run workflow）
- または `echo "APIキー" > ~/.mll_youtube_api_key && chmod 600 ~/.mll_youtube_api_key`

放置しても2日おきの自動更新で埋まる。

### 3. Vlogの実素材テスト

エンジンは合成素材でE2E検証済み（書き出し貫通・ピクセル検証）だが、
**実際のインタビュー素材で構成が良いかは未確認**。現在は管理者のみ開ける。

### 4. Switcher カット切替モードのサムネ

実フレーム生成は合成素材では検証できていない（手法は media.js のサムネと同一で実績あり）。

### 5. iPhoneで16分超を書き出す（未着手）

物理的に一度に出せない。`iPhone書き出し8分半_検討メモ_2026-07-20.md` の
A'案（OPFS直書き＋音声チャンク化）が設計済み・未実装。

---

## 教訓（この日の追加分）

- **「同じにする」はCSSの読み合わせでは足りない。** 対象を同じ幅で開いて
  実測し、数値で突き合わせる（ヘッダー・フッターで3回やり直した）
- **上限を決めたら実測する。** 案内と実際が食い違うと最悪の体験になる
- **制約は「後で弾く」のではなく「先に知らせて、その場で直せるように」。**
  15分待たせてから失敗を伝えるのは論外
- **z-indexで競わせるより「重ねない」設計の方が壊れにくい。**
  stacking contextが分かれると z-index は効かない
- **ブラウザペインのService Workerが旧版を配信し続けることがある。**
  検証で「直っていない」時は、まずSWの登録解除とキャッシュバスターを疑う
- **`©️` や `ver.`(U+202F) など、見た目が同じで実体が違う文字がある。**
  文字列置換が0件になったら `od -c` で実体を見る

---

## 運用ルール（継続）

- **push/デプロイは毎回かならず確認を取る**（1度の許可＝1度の実行）
- push前は deploy-guard 必須 ＋ `git show HEAD:<file>` で実体裏取り ＋ bundle バックアップ
- push はまとめて1回（Netlifyクレジット節約）
- Node無し。iOS Safari最優先（375px）。スクリプト優先編集
- 検証は「利用者が実際にする操作」で。プレビューサーバはDrive配下を配信できないので
  scratchpad へ cp

## 関連文書

- `ゲストとログインの違い_一覧.md` … 権限差の一覧（v1.39.38基準・プラン説明の下敷き）
- `Switcher書き出し問題_2026-07-20.md` … 3つのエラーの詳細分析
- `iPhone書き出し8分半_検討メモ_2026-07-20.md` … iOS対応の設計（A'案）
- `引き継ぎ書_2026-07-20.md` / `引き継ぎ書_2026-07-20夜.md` … 前日の経緯
