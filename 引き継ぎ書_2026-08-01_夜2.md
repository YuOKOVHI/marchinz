# MarchinZ 引き継ぎ書（2026-08-01 夜・その2）

前の版: [引き継ぎ書_2026-08-01_夜.md](引き継ぎ書_2026-08-01_夜.md)

## 現在地

```
本番   v1.64.0
手元   v1.66.0（未push・3コミット）
QA     tools/qa/runner.html を 122件（旧114 + 今回の見張り8件）全PASS
```

## 1. このセッションでやったこと

プロのUI/UXデザイナーとシニアエンジニアの2名にレビューさせ、
出てきた14件を**すべて**直した。

### 見つかった最大のもの: おまかせは一度も完成できていなかった

**本番 v1.64.0 でも同じ**。3つの門が、自分で掛けた鍵で自分を弾いていた。

```
runAuto()
  setBusy(true)                    _busy = true
    ↓                              （setBusy(false) は _autoRunning が立つ間
    ↓                                ずっと握り潰される＝返せない）
  runEasyFinish()   ← 引数なし
    if (!pIn && _busy) return;     ★ここで即return
                                     → カット割も色そろえも1行も走らない
  if (!easyDone) return;           ★無言で終了。全画面がふっと消えるだけ
```

仮に仕上げまで進めても、その先に同じ形の門がもう1つあった。

```
  autoStage.close()
  btn.click()  ──→  #exportBtn.onclick の1行目 if (_busy) return;  ★即return
  finally: _autoRunning = false    ← 鍵を返すのが click の14行あと
```

**引き継ぎ書の「iPhoneで書き出し成功が一度も未確認」は、実機の問題ではなかった。**
コード上で必ず落ちる。実機で試しても得られたのは「消えた」だけだったはず。

### 直した14件

| # | 何が壊れていたか | どこ |
|---|---|---|
| 1 | 自走が `_busy` の門で自分の鍵に弾かれる（上記・3箇所） | ui.js |
| 2 | 失敗も中止も**画面がふっと消えるだけ**。エラー表示先が全画面の下 | ui.js / index.html |
| 3 | 「中止」が何も止めない。中断点が director / colormatch に0個 | director.js / colormatch.js |
| 4 | 主役のプレビューが一度も動かない（play直後にpauseが同期実行） | ui.js |
| 5 | `_autoRunning = true` が try の外＝例外1つでアプリが永久ロック | ui.js |
| 6 | 0.5秒ごとに innerHTML 全置換＝アニメ痙攣・読み上げ喋りっぱなし | ui.js |
| 7 | iPhone横向きで画面上端へ**永久に到達できない**（実測 -141px） | style.css |
| 8 | 縦型プレビューの**64%が黒帯**（実測 343×222 の枠に絵が 125×222） | style.css |
| 9 | おまかせ中に「✅分析完了 — 書き出せます」の嘘のバイブとタブ題 | ui.js |
| 10 | 進捗が最長の段で **51%固定**、ETA超過後「まもなく」が永久ループ | ui.js |
| 11 | 段の補足が次の段へ居座る（「カメラを切り替える 音を読み込み中100%」） | ui.js |
| 12 | `finishAll()` が未使用＝**完成の瞬間が画面に存在しない** | ui.js |
| 13 | 常に false になる死にコード `!MC.S.showIn == null` | ui.js |
| 14 | `role="dialog"` なのにフォーカス移動もトラップも無し | ui.js |

### 足した見張り（8件・すべて変異で歯を確認）

同じ試験の中で**両側**を走らせている。直った状態で通り、
バグを戻した状態で落ちることを、その場で実際に確かめる形。

```
★書き出しは自走から本当に起動できる   setBusy(false)を握り潰す → 起動しない ✅
★仕上げと解析は自走なら弾かれない     自走でなければ弾かれる            ✅
★失敗しても畳まず理由と次の一手が出る  成功時は畳む・失敗の顔は出ない     ✅
★やめるで映像解析が実際に止まる       やめなければ解析へ進む            ✅
★段の補足は次の段へ居座らない
★再描画で段のDOMを作り直さない        ノード同一＋class据え置き
★中央寄せの溢れで上端へ戻れない       centerへ戻すと上端が消える        ✅
★おまかせ中は完了通知を出さない       こだわりでは今までどおり出る       ✅
```

既存の2件は、**旧い（壊れていた）挙動を正しいものとして固定していた**ので
新しい意図へ書き換えた。特に「自走は最後まで鍵が掛かったまま」は、
それ自体が書き出しを殺していた当のバグだった。

## 2. 次にやること

1. **push（未実施・許可待ち）** v1.66.0
2. **iPhone実機** — ここからが本番。おまかせが最後まで走ってMP4が出るか
3. 3本同時再生が10分持つか（「見る道具」化の前提）

## 3. このセッションで踏んだ落とし穴

1. **プレビューサーバが前セッションの別ディレクトリを配信していた。**
   `preview_start` が `reused: true` を返したら、それは**別のパスを配信する
   古いプロセス**かもしれない。`lsof -a -p <pid> -d cwd` で実体を見る。
   最初の md5 一致は偶然だった（どちらも同じ修正前ソースのコピーだったため）
2. **配信物が新しくてもブラウザは古いのを掴む。** `?cb=` を付けて開き直す。
   これを見落として「直っていない」と2度誤認した
3. **`rsync --exclude '*.mp4'` が QA の合成素材を落とす。** 4件が
   「MP4として解析できません」で落ち、コードの回帰に見えた
4. **assert が自分の説明コメントにヒットする。** 「`!MC.S.showIn == null` を
   撤去した」の検証が、撤去理由を書いたコメント自身を数えていた。
   コードの形（`if (...)` ごと）で数える
5. **版番は 23箇所で正しかった。** `?v=` だけを数えると 20 で、
   `tools/switcher/index.html` の2行目 `data-mz-app-v` を取り残す。
   root ×2 + switcher ×21（?v= 20 + data-mz-app-v 1）＝ 23

## 4. 注意（変わらず有効）

- **push と本番デプロイは毎回かならず確認を取る。1度の許可は1度の実行にしか使えない**
- Drive 同期がコミットを巻き戻す。push 前に `git show HEAD:<file>` で実体を見る
- 一括置換は件数を assert し、報告前に git blob で数える
- 本番の挙動は**本番の配信物**（curl）で測る。手元のコードで判断しない
- このMacに node/npm は無い。テストはブラウザで `tools/qa/runner.html`

## 5. QAの穴（正直な記録）

- 本物の `exportMP4Parts` は runner から走らせられない（iframe内でOPFSの
  writer Worker が応答せずスイート全体が固まる）
- おまかせの通しは重い処理をスタブに差し替えたもの。**実素材での通しは未実施**。
  今回直した3つの門は本物のコードを通しているが、
  その先の実際のデコード・書き出しは実機でしか分からない
