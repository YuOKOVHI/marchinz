# MarchinZ 引き継ぎ書 2026-07-18

## 現在地(このまま次セッションの前提にしてOK)

- **本番: v1.36.5 稼働中**。ローカル main = origin/main = 本番 = `c31cf22`(完全一致、未push・未コミット差分なし)
- 場所: `マイドライブ/CursorLogs/010_MarchinZ`(CloudeCode_u ではない)
- 本番: https://marchinz.netlify.app / GitHub: `YuOKOVHI/marchinz`
- firebase deploy(rules)は今回の一連の変更では不要(ルール変更なし)

## 2026-07-18 に実施した内容(v1.35.8 → v1.36.5)

1. **メトロノーム拡張**: BPM上限 260→400、アクセントを「1拍目固定」→拍ドットタップで各拍自由設定
2. **クリエイターツール3種をライト統一+KV背景**: ReAngleをダーク→ライト化、Switcher/Privacy/ReAngle共通でキービジュアル(hero-show.png)に白88%重ねの背景
3. **Switcher水平補正の永続化バグ修正**: horizonOnがsaveStateに含まれておらず再読込で補正が消えていたのを修正
4. **練習ログ・楽器メンテのワンタップ記録**: 「前回と同じで今日を記録」ボタンを追加
5. **毎日データ更新をビルドレスに**: netlify.tomlのignoreルールでbotのデータのみpushはビルドをスキップ。marchinz-data-refresh.jsが実行時にGitHub rawから最新取得(Netlifyクレジット節約の本命)
6. **イベント登録「URLから仮入力」**: netlify/functions/event-scrape.js(Netlify Functions初導入)。JSON-LD→OGP→本文の順でタイトル/日付/都道府県/種別を抽出
7. **UGC「ツール利用」タブに使用状況サマリー**: ツール別のべ回数・今日/7日/30日・登録者数を棒グラフで表示
8. **TOP「3つの入口」ブロック+クリエイターツールのビジュアルカード**: ファン/プレイヤー/クリエイター別の特設導線。ツールカードに自作SVG(Before/After等)
9. **14名レビュー反映**: 未ログイン時にメトロノーム(本来ログイン不要)がログイン必須画面に飛ぶP0バグを修正、Days/メトロノームの遷移先重複解消、ブランド3色統一、安心バッジ追加
10. **収益化Tier1-2**: メンテ記録時に消耗品のAmazonアフィリンク(tag=hamamasu-22)を表示。楽器台帳の空状態メッセージをメリット訴求に強化
11. **文言・運用調整**: 推奨環境を端末別表記(iPhone/Android/Mac)に、YouTube+note自動更新を毎日→2日おきに(cron変更、Actions/APIクォータ節約)

## 未着手・次にやるなら

- **収益化Tier2本丸(統計レポート)は方式判断待ち**: 全ユーザー集計には「匿名カウンタ」方式(推奨、Firestoreルール変更+firebase deploy要承認)か「管理者エクスポート」方式かの選択が必要。ヤマハは仮の第一目標
- **A8アフィリの実装は無し**: 優さんは「A8とAmazonで実装済み」と認識されていたが、コード全体を確認したところAmazonのみ(tag=hamamasu-22)。A8を使うなら管理画面のバナーコードが必要
- Web図鑑のAmazon検索リンク(マーチングエクスプレス21等)に`tag=hamamasu-22`が付いておらず機会損失(未修正)
- ReAngleは実機iPhone未検証のままベータ表記
- 今回まとめて出た機能(メトロノーム拡張、楽器メンテのAmazonリンク、イベントURL仮入力、TOP入口ブロック)は実機(iOS Safari)未確認

## 運用ルール(継続)

- push は手動でこまめに(自動push不採用)。優さんが明示的に「pushして」と言うまでコミットのみ積む
- push前は必ず `git ls-remote origin refs/heads/main` で実体確認 → `~/Movies`へbundle保全 → `git pull --rebase origin main` → `git push`
- **スクリプト優先編集(2026-07-18指示・最重要)**: 複数箇所の一括編集・調査はPython/sedスクリプト1本で完結させ、Read/Editの往復でトークンを消費しない
- Drive巻き戻り罠が活発。編集はscratchpadのgit-blobコピーで行うか、cp→即commit→`git show HEAD:<file>`で検証
- UIアイコンはFont Awesomeモノクロ統一(カラー絵文字禁止)
- スマホ・iOS Safari最優先(375px)で検証

## 参考

- 詳細な過去の運用・ハマりどころは `引き継ぎ_2026-07-13.md` を参照
- メモリ(`~/.claude/.../memory/marchinz-*.md`)に技術詳細・マネタイズ方針・デプロイ運用を記録済み
