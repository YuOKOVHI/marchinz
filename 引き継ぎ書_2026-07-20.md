# 010_MarchinZ 引き継ぎ書 2026-07-20

## 現在地

- 作業ツリー: クリーン(未追跡は `引き継ぎ書_*.md` のみ)
- **未pushは解消済み**。v1.39.1〜1.39.12 を push 済み
- **本番は v1.39.12**
- 前日の引き継ぎ書は `引き継ぎ書_2026-07-19c.md`

```
未pushの4件(いずれもコミット済み・実体裏取り済み)
fa3987b fix(switcher): カメラマン判定を「背景ごと動いたか」で行う+判定の表示 → v1.39.4
612eec8 feat(switcher): ソロ/ソリを強く優先・操作カメラの据わった絵を活かす → v1.39.3
31f6ce3 fix(switcher): ルック作り直し・おまかせ表記・小窓レイアウト・振り/空舞台の除外 → v1.39.2
2f92626 feat(vlog): 6者レビューの反映 — 環境音を既定に・尺の自由化・縦横選択 → v1.39.1
--- ここまで未push ---
2b5c3fd feat(vlog): MarchinZ Vlog の骨組み+iPhone最適化を恒久ルール化 → v1.39.0  ← 本番はここ
```

---

## 今日やったこと

### 1. 本番デプロイが6コミット分止まっていた(解決済み)

朝の時点で**本番が `fd79d88`(v1.38.0)のまま**で、v1.38.1〜1.38.5 が反映されていなかった。
`[hidden]` のセキュリティ修正(MarchinZ Daysタブが他人に見える)も本番に出ていなかった。

- 原因は **GitHub の障害**(Actions → API へ波及)。Netlifyのビルドが `preparing repo` で
  `Unable to access repository` を出して失敗し続けていた
- **私の初期診断は2回外した**: ①「Netlify側の障害」②「GitHub連携の権限が失われた」。
  ②の誤診で優さんにリポジトリの再リンク作業をさせてしまった(無害だが不要だった)
- 教訓: Netlifyステータスの「GitHub API Requests: partial_outage」は**GitHub側の不調を
  Netlifyの監視が映しているだけ**。Netlify自身のincidentが無いときはGitHub側を先に見る
- 障害復旧後にpushして **v1.39.0 が本番反映済み**。今日の未確認事項だった
  「Daysタブの露出」「書き出しのバッジ焼き込み」の修正はこれで本番に乗った

### 2. MarchinZ Vlog(4本目のクリエイターツール)に着手 — Phase 0 完了

**場所** `tools/vlog/` / **名前空間** `window.MV` / 出力 1920x1080 または 1080x1920・30fps

素材を枠に入れると構成テンプレに沿って自動で並び、MP4で書き出すエディタ。
設計プランは `~/.claude/plans/4-marchinz-vlog-vlog-prancy-firefly.md`(承認済み)。

実装済み: `state / demux / audio / templates-data / logo / media / ui / app`
未実装: `planner / preview / exporter / bgm / quality / show / xmeml / guide / testgen`

**モードは3つ**(構成テンプレのブレンド比率)
- おすすめ = 大石2 : THE FOCUS+アメリカ8
- エモーショナル = 大石7 : 3
- アクティブ = 大石0 : 10

**確定要件(優さん決定)**
| 項目 | 決定 |
|---|---|
| 枠 | インタビュー3・インサート10・ロゴ1・BGM3(ゲスト 1/5) |
| 音 | **「演奏をそのまま聴かせる」が既定**。BGMは必須ではない |
| 尺 | アルゴリズムが素材に合わせて決める。**3分より短くてもよい** |
| 縦横 | **横(1920x1080)/縦(1080x1920)を選べる**。モード=表現 とは独立した「出し先」設定 |
| ロゴ | PNG推奨、非PNGは自動透過。右上に小表示+最後に白or黒背景で大きく |
| Artlist | リファラル `https://artlist.io/referral/9e958a5d-8272-4c71-95d9-12ce5704a7dc`。**BGM枠と書き出し後の両方**に置く。同梱BGMは無し |
| 責任 | 「できあがった映像についてMarchinZは責任を負わない」旨を明記済み |

### 3. Vlogを6者でレビュー → 反映(v1.39.1)

原口さん(THE FOCUS VLOG制作者)/ 大石健弘さん / UI-UXデザイナー / シニアエンジニア /
3団体の広報(中学28名・高校85名・社会人120名)。**独立に一致した指摘ほど確度が高い**。

**構成テンプレの誤りが元データで裏取りできた**
- 「今日は何の日」ブロックを丸ごと落としていた(`VlogWorks/data.js` の `DNA.skeleton` 5-15%)
- 締めの語りを12%と最短にしていたが、元データは「70-90%=クライマックス、長回しを恐れない」→ 16%へ
- エモのカット秒を5.0〜7.0にしていたが、大石作品13本の実測は**1.58〜3.69秒**。
  長回しは**人の言葉だけ**で情景は普通に切る → 3.5〜4.5秒へ
- 本番ハイライト2.5秒は「認識される前に切れる」→ 3.5秒へ

**3団体全員が同じ場所で離脱していた**
- **BGM必須**(私が勝手に必須にしていた。要件は「3つまで入れられる」)。
  → 「演奏をそのまま聴かせる」を既定の第一選択肢に。マーチング団体は演奏という音を
     持っているので、現場音は妥協案ではなくこの分野の正解

**私の設計ミス(3者が別ルートで指摘)**
- 空き枠を上限数ぶん先に並べていたため、10枠に5ブロックのガイドが2周し、しかも
  planner は到着順で割り当てるので**守られない約束**になっていた
  → 空き枠の先出しを廃止し、撮ってほしいものは重複を除いたチェックリスト1枚に集約
  → ページ全高 3331px → 2925px、映像セクションは 884px → 240px

### 4. Switcher の修正(v1.39.2〜1.39.4)

**MarchinZカラーをプロのカラリストのレビューで作り直し**(3つの不満はすべて数式に原因)
| 不満 | 原因 | 実測 |
|---|---|---|
| シャドウが効きすぎ | 黒締めが**しきい値なしの全域アフィン変換** | 入力34→13(−62%)、コード9以下は全部0 |
| ハイライトの抜けが悪い | 肩の `u/(1+u)` は**入力1.0でも0.5が数学的上限** | **純白が230コードで頭打ち** |
| 全体的に黄色すぎ | ハイライトのティントに **G:+0.012** | Gを足すとオレンジではなく黄色 |

修正後の実測: 白 230→255到達 / 白のR-B +21→+3 / 中間調は中性 / 金管の彩度は維持(R-B 207)
/ フィルタなしは完全素通し。**8項目中7項目PASS**(残る gymFloor は素材自体がR-B+20の暖色)

**その他**
- 「おすすめ」→「**おまかせ**」に表記変更。**おまかせに自動トリム(開始/終了)を組み込んだ**
  (同期ボタン側にはあったのに、おまかせ経路だけ通っていなかった)。
  カット割より**先**に実行すること(director は `trimRange()` の中だけを割る)
- 小窓1/2のレイアウト崩れを修正。大きさスライダーが小窓1の行で折り返し、行高が76/36pxで
  食い違っていた。grid で列を揃え、大きさは両方に効く設定なので独立した行へ
- **カメラを振っている絵を除外**(ディレクター指示「絶対に入れない」)
- **人が写っていない画を除外**(誰もいないピット等)
- **ソロ・ソリで抜いているカメラを強く優先**。音声から `soli` を新設し `feature` に束ねた
  (実測: ソロ0.70 / ソリ0.77 / 全奏0.15 / バッテリー0.07)
- **操作カメラ(手持ち・ジンバル・カメラマン付き三脚)を自動判定**し、移動中は失格・
  据わった絵はソロ/ソリで大きく加点。クリップカードに判定バッジを出し手動上書きも可

### 5. 全ページ・全コードのレビュー(UI/UXデザイナー + シニアエンジニア)→ v1.39.5 / v1.39.6

自作コード 107ファイル / 77,906行を機械スキャン → 異常箇所だけ精読。
全5ページ(TOP + ツール4本)を **375px の実測**で確認した。

**コードは想定より堅かった。機械が挙げた危険パターンは大半が誤検知**
- XSS 18件 → **全滅**。`innerHTML` は静的文字列か `""` のクリアのみ。
  UGC(community / profile / app)は `createElement` + `textContent` で組んでおり安全
- リスナ漏れ 7件 → `showPage` が `hidden` で DOM を保持するので漏れない
- `http://` 28件 → SVG名前空間と YouTube リンク。混在コンテンツではない
- APIキー露出 → Firebase Web API キーは公開前提

**UI/UX は AA(4.5:1)未達が 56件。原因は 2 つの色トークンに集約されていた**
| 対象 | 変更 | 比率 |
|---|---|---|
| ミュート文字(最多) | `--muted` α 0.55→0.6 | 4.24 → 5.04 |
| eyebrow・小見出し | `--amber-bright` → **既存の** `--amber` | 3.19 → 5.02 |
| ボトムナビ非アクティブ | `#8a94a6` → `#6b7684` | 3.05 → 4.62 |
| ヒーロー eyebrow | α 0.55→0.72 | 3.66 → 6.24 |
| NEWバッジ | 背景を `--amber` へ | 3.19 → 5.02 |
| ツール共通(4本) | バッジ・安全表記・フッター | 4.2〜4.5 → 4.6〜5.8 |

**新トークンはほぼ足していない。`--amber: #b45309` は元から定義されており、
テキストに装飾用の `--amber-bright` を使っていたのが誤用だった。**
最終実測: 全5ページで AA 未達 **0件**(Leaflet の帰属表示3件のみ残置。ライブラリ既定)

**v1.39.6 で入れたもの**
- **トースト基盤を新設**(`marchinz-toast.js` + CSS)。`alert()` 22箇所を置換。
  置換先が無いことが根本だった。`confirm()` 4件は戻り値が要るので今回は据え置き
- タップ標的 44px: フッター13リンク(**実測16px**)/ 「すべて見る →」3件 / X ロゴ /
  ツール側の共有フッター。文中のインラインリンクは WCAG 対象外なので触らない
- フッターバナー4箇所に `width`/`height` を明示。**未読込時に幅2pxへ潰れていた**
  (`loading="lazy"` のまま。属性があればボックスが先に確保される)
- 本体にも `[hidden] { display: none !important; }`。実測の漏れは0だが再発防止
- Switcher のカード説明を実装に合わせて書き直し。「縦型動画作成」の説明が
  "自動で同期"(両モード共通)で、縦型ならではの価値(**写真も使える**・9:16/1:1)が
  伝わっていなかった

### 6. 実素材テストで見つかった書き出しバグ(v1.39.9)— 最重要

優さんが Timeline 1-3.mov(1920x1080 / 18〜23分)で初めて実素材テストを実施。
2件の症状が出たが、**原因は1つ**だった。

| 症状 | モード |
|---|---|
| 「デコードが進みません: Timeline 3.mov」で書き出せない | 縦型 |
| 書き出しが真っ黒(プレビューは正常) | スイッチング |

**原因**: `VideoPipe.frameAt` の時刻基準の食い違い。
呼び出し側は `local = t - clip.offset`(クリップ先頭からの絶対秒)を渡すが、
frameAt は最初に届いたフレームの timestamp を `firstTs` として引き、そこからの
相対時間で比較していた。`init(tIn - c.offset)` で途中から始めるため firstTs は
その開始地点になり、**要求位置からさらに同じだけ先へ進もうとしていた**。

同条件の模擬で裏取り(実素材が無くても再現できる):
- 修正前: 1コマ目の要求で **762.3秒地点**を返す(381秒ズレ)。11,448枚を無駄デコード
- 修正後: 381.3秒ちょうど。デコードは40枚

`demux.samples()` が返す `cts` はトラック内の絶対時刻なので、`firstTs` を引く
正規化そのものが不要だった。**トリム開始が 0 のときだけ偶然動く**バグで、
v1.39.2 の「おまかせに自動トリムを組み込んだ」変更で表面化した。
真っ黒は、offset を持つクリップが二重進行で素材の終端を超え、eof 後の
最後のフレームを保持し続けていたため。

**この修正はまだ実素材で未検証**。次のセッションで最初に確認すること。

### 7. 実素材テストを受けた UI/ロジックの調整(v1.39.8・1.39.10〜1.39.12)

- **音声の初期選択を「おすすめ」に**(v1.39.8)。おすすめ印は出しているのに
  選ばれているのは最初の動画のままで、表示と選択が食い違っていた。
  `recommend()` は `clip.stats` が要るため読み込み直後は確定できない。
  同期後の `renderAll → renderAudio` で追従させ、手で選んだら止める
- **「3つまとめて取り込めない」は誤解だった**(v1.39.10)。機能は元から
  multiple 対応。UI が「素材1/2/3」の独立した枠だけを見せており、
  1枠ずつ入れるものだと読めていた。ステップ1の説明を
  「3つまでまとめて選べます」に変更(375px では主導線の真上に出る)。
  一度側パネルに一括ボタンを足したが、下部固定ボタンと導線が重複するため撤去
- **手ブレを単独条件で失格に**(v1.39.11)。従来は
  `shakeP75 > TH_SHAKE && flipRatio > 0.45` の AND で、flipRatio が低い
  (ゆっくり同方向に揺れる)手ブレがすり抜けていた。挙動が変わる組み合わせを
  機械で12通り洗い出し済み。**「絶対使わない」は director の既存の仕組みで担保**
  (全カメラ失格の区間は妥協せず直前カットを延長)。
  失格理由を `MC.visual.dqReason()` で文字列化し、延長ログに内訳を出すようにした
- **カメラ切替を「いま映っているカット」に統一**(v1.39.12)。タップ選択→操作の
  二段階をやめ、操作対象を常にプレビューと一致させた。順送りボタンも廃止し
  C1/C2/C3 の直接選択へ。カット⇄ディゾルブ / 前と結合は削除
- 解析中のプレビュー表示を「映像分析中」に統一。縦型では「おまかせ」の説明から
  「カット割」を外した

### 8. MarchinZ Vlog を管理者限定で公開(v1.39.7)

クリエイターツール一覧の Switcher 直下に `data-admin-only="true" hidden` で追加。
`enforceAdminOnlyVisibility()` が hidden と style.display の両方を設定する既存の
仕組みに乗せた。未ログイン/管理者の**両方向を実測**(display:none 0x0 ↔ flex 346x371)。

`tools/shared/sitechrome.js` にあった Vlog リンクは削除した。ツールページには
auth.js / firebase が読み込まれておらず管理者判定ができないため。
**注意: これは導線を隠すだけで `/tools/vlog/` の URL 直打ちは通る**(静的サイト)。

---

## 未着手・持ち越し(優先順)

1. ~~未push 4件を push~~ → **完了**(v1.39.5 まで本番反映済み)
2. **実素材での確認が全て未実施**(最優先で残っているのはこれ)
   - Switcher: `~/Desktop/自動整理/Timeline 1.mov`(2.2GB)/`2.mov`(2.8GB)/`3.mov`(2.9GB)。
     **ブラウザペインは大容量fetchを遮断するので優さんの実機で通す必要がある**
   - 特に今回ルックを大きく変えたので、実写での見え方の確認が要る
   - `TH_CAM_RESID = 0.55` 等のしきい値は合成条件での妥当値。実素材で調整の可能性。
     クリップカードのバッジに「画面全体が動いている区間が◯%」と根拠が出るので切り分け可
3. **Vlog Phase 1.5「書き出し貫通スパイク」**(シニアエンジニアの指摘)
   - 15秒の手書きタイムラインを WebCodecs / 実時間の両経路で書き出し、**planner を書く前に**
     データ構造(合成関数の引数・セグメント順序・音声ミックス)を確定させる
   - ここを飛ばすと Phase 4 で planner 作り直しになる
4. **Vlog planner の割当モデル**(大石さん・原口さん・エンジニアが別々の動機で同じ要求)
   - 「インタビューi番目 → i番目ブロック」の1対1をやめ、**「ブロック → 素材ID + 使う区間」**へ。
     語りを2つに割る/Bロールを被せる/セグメント順序制約(VideoPipeは前方専用)の全てに必要
5. Vlog の未実装(planner / preview / exporter / bgm / quality / show / xmeml / guide)

### レビューで出た未反映の提案(価値が高い順)

- **テロップ**(名前・学年/パート・日付) — 原口さん「誰が喋っているか分からないインタビューは
  身内以外10秒で離脱」。インタビュー枠に2行入力を足すだけ
- **Bロール被せ**(音は継続・絵だけ差替) — 素人素材で1人25秒の据え置きは物理的に持たない。
  テンプレに `bRoll` フィールドは定義済み、planner が未実装
- **末尾トリム→頭トリム** — 原口さん・大石さん一致。「本音は最後に来る」。
  現在プランは「超過は末尾トリム」だが、既定を頭から切るに変え、末尾を切ったら画面で知らせる
- **「この動画、いちばん見てほしい人は？」** — 実装済み(入力欄)。ただし**ガイド文の主語に
  差し込む**部分は未実装(大石さん提案)
- **書き出し前に1カットだけ手で選ばせる** — 大石さん「全自動で終わらせない儀式」
- **映ってはいけない人の除外** — 3団体全員が不安に挙げ、社会人団体は「これが無いと決裁できない」。
  優さん判断は「責任を負わない旨を記載」で対応済みだが、機能としては未実装
- iOS実時間書き出しの中断対策(画面ロックで**壊れた短いMP4が無言で保存される**経路がある)

---

## 教訓・ハマりどころ

- **assert の判定文字列がコメントに載ると誤検知する。2026-07-20 に3回踏んだ**。
  「rAF が残っている」「flipRatio の AND が残っている」等、いずれも自分で書いた
  変更理由コメントに同じ語が入っていたため。**実コードだけを見るには
  コメントを剥がしてから検証する**(`re.sub(r"/\*.*?\*/", "", js, flags=re.S)`)。
  件数 assert も同様で、想定件数を思い込みで書くと落ちる。まず数えてから書く
- **同じ画像・同じ文言が複数箇所にある前提で置換する**。バナーの width/height
  付与で正規表現が最初の1件にだけ当たり、**肝心のフッター側が未処理**のまま
  「反映した」と報告しかけた。置換後は必ず件数を assert する
- **CSS を足す前に既存ルールの詳細度と衝突を見る**。`.footer-nav-link` の既存は
  `.mz-footer-grid .footer-nav-link` で詳細度が上。`.footer-banner img` に
  `height` を足していたら既存の `max-width:120px` と衝突して崩れていた
- **UI の文言は「切り方」まで確かめる**。カメラ切替ボタンで名前を先頭9文字に
  切ったら、実素材の `Timeline 1/2/3.mov` が全部「Timeline …」になり区別
  できなくなった。**末尾で見分ける命名は中間省略にする**

- **計測スクリプトは、既知の正常値で較正してから使う**。2026-07-20 のレビューで
  **2回続けて誤報を出した**。①「収益バナーが潰れている」→ `scrollIntoView` が
  効いておらず(`top:6964, inViewport:false`)lazy 未読込だっただけ。
  ②「コントラスト1.3で実質不可視」→ 背景 `rgba(...,0.08)` を**アルファ合成せず**
  計算していた(実際は 6.5:1 で合格)。
  引き継ぎ書の「計測する仕組みを入れたのに判定式が古いまま」と同じ系統で、
  **計測器そのものを疑う視点**が抜けていた。まず正解の分かっている要素で試すこと
- **`document.hidden = true` は CSS アニメーションのクロックも止める**。
  トーストの入場を `requestAnimationFrame` でやったら透明のまま出なかった
  (`playState: running` なのに `currentTime: 0`)。CSS animation に変えても
  ペイン上では同じ。**実機の実害は別にあった**: バックグラウンドタブに出た通知が
  `setTimeout` で消え、戻った時には無い = alert からの置き換えとして機能後退。
  → 表示時間は `visibilitychange` を見て**見えている間だけ数える**ように実装した
- **同じ画像・同じ文言が複数箇所にある前提で置換する**。バナーの `width/height`
  付与で、正規表現が最初の1件だけに当たり**肝心のフッター側が未処理**のまま
  「反映した」と報告しかけた。置換後は件数を assert すること
- **CSS を足す前に既存ルールの詳細度を見る**。`.footer-nav-link` に
  `min-height` を足そうとしたが、既存は `.mz-footer-grid .footer-nav-link` で
  詳細度が上。また `.footer-banner img` に `height` を足すと既存の
  `max-width:120px` と衝突して崩れるところだった

- **「計測する仕組みを入れたのに、判定式が古いまま」を2回踏んだ**。panRatio を追加しながら
  `isPanning` が旧式のままで固定カメラが全部失格になるところだった。
  **新しい指標を足したら、それを消費する側を必ず確認する**
- **ブラウザペインは `document.hidden = true`**。エンジニアの助言どおり
  `if (document.hidden) return;` を入れたら**ボトムバーが一度も出なくなった**。
  rAF単独依存(v1.37.2)と同じ型の罠。コードに注意コメントを残してある
- **`MZJourney` の `autoState()` は `{current, done[]}` を返す**。文字列を返すと無反応
- **役割の異なる指標を組み合わせるときは、前提条件を検算する**。
  `flipRatio` は `|dx|<2px` のサンプルを除外して数えるため、ノイズで1px揺れる固定カメラは
  `flipRatio=0`(=向きが一定)と評価される。これに気づかず shakeP75 と組み合わせると
  固定カメラがパン扱いになる
- **ブロックマッチングは大きな被写体に食いつく**。「カメラが動いたか」は
  **補正後の残差が小さいか**で確かめる(背景ごと動いた=揃う / 被写体だけ=揃わない)
- **常時加点は効きすぎる**。操作カメラへの `+0.35` で全奏でも寄りが1位になっていた。
  「その分ソロ・ソリで使う」という指示は、**加点を集中させる**という形で実装するのが正しい
- **テンプレの pct 合計はスクリプトで検算する**。active が95%になっていたのを機械で検出した
- **別セッションと `.claude/launch.json` を取り合う**。上書きせず両方の設定を残すこと
- Netlifyステータスの「GitHub API Requests」は**GitHub側**の不調。切り分けの順番に注意

---

## 運用ルール(継続)

- push前は **deploy-guard スキル必須** + `git show HEAD:<file>` で実体裏取り + bundle バックアップ
- push はまとめて1回(Netlifyクレジット節約)
- **Node無し**(`node --check` は使えない)。構文確認はブラウザで実読み込みする
- スクリプト優先編集。Pythonで日本語を扱うときは `# -*- coding: utf-8 -*-` と `io.open(encoding)`
- **iOS Safari最優先(375px)**。2026-07-20 に **CLAUDE.md 必須ルール8** を新設し、
  「UI/UXの設計そのものをiPhoneで使う前提で最適化する」を恒久ルール化した
  (ルール7=「動く・崩れない」、ルール8=「設計思想」。迷ったら「電車の中で片手で操作できるか」)
- **完成尺の下限181秒は本体のYouTube掲載条件**(`MIN_VIDEO_DURATION_SEC = 181`)由来。
  Vlogでは下限を外したが、3分未満は「MarchinZに載せられません」と画面で知らせている
- 検証は「ユーザーが実際にする操作」で行う。プレビューサーバはDrive配下を配信できないので
  scratchpad へ cp して `functools.partial` で配信する

## 関連メモリ

`marchinz-project` / `switcher-director-update` / `marchinz-mobile-safari-first` /
`marchinz-deploy-flow` / `css-hidden-attribute-trap` / `drive-sync-commit-revert`
