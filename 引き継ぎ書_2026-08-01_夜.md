# MarchinZ 引き継ぎ書（2026-08-01 夜）

前の版: [引き継ぎ書_2026-08-01.md](引き継ぎ書_2026-08-01.md)
関連: [映像ツール仕様書_2026-08-01.md](映像ツール仕様書_2026-08-01.md) / [映像ツール_製品改革提案_2026-08-01.md](映像ツール_製品改革提案_2026-08-01.md)

## 現在地

```
本番   v1.64.0（2026-08-01 に push 済み）
手元   v1.65.0（おまかせ専用画面。未push）
QA     tools/qa/runner.html を 114件
```

**本番に出ているもの**: 書き出しの無限再帰の修正 / 文言の全面書き直し /
工程7→5 / 傾きを飛ばせる / 区間の色帯 / 2段選択（種類→おまかせ・こだわり） /
おまかせの自走

**まだ本番に出ていないもの**: おまかせ専用の全画面（v1.65.0）と、
その後の「音の読み込み%」

---

## 1. この製品がいま目指しているもの

3人協議（PM+シニアエンジニア+UI/UXデザイナー）の結論は
**「書き出す道具」から「見る道具」へ**。
最頻の用事（練習直後の反省会）にMP4は要らないのに、iPhoneで最短3分・
通常9分待たせていた。詳細は提案書。

USPは **カチンコ不要のマルチカム同期 + マーチング区間の自動判別**
（`sections.js` のバッテリー/ピット/ソリ判別は汎用ツールに無い）。

## 2. 画面の構造（優さんの指示どおり）

```
1段目             2段目                 その先
作る動画を選ぶ  →  どちらで作りますか  →  おまかせ: 専用の全画面（1枚だけ）
 縦型               おまかせ               こだわり: 5工程
 ワイプカメラ        こだわり
 自動スイッチング
```

**順序に意味がある。** 何を作るかが決まらないと、おまかせが何を
おまかせされるのかも決まらない。

### おまかせ（優さんの指示: 2026-08-01）

> できるだけタップやクリックせずに最後までやってほしい。
> 完成60秒、盛り上がるシーンを入れる。音はこちらが選んだ一番いいやつ。
> 傾き補正も自動。色はMarchinZカラー。取り込んだら書き出しまで。

> 完全にUIを分けて。どんどん自動で進んでるのがわかるように。1つの画面だけに。
> 何をしてるかがわかる。プレビューがわかるように。
> 「できていってる！」がわかる、楽しいUIとUX

実装: `MC.ui.runAuto()` と `MC.ui.autoStage`（`#autoStage`）

```
傾き(自動) → 同期 → 音声(おすすめ) → 見どころ → カメラ割り → 書き出し
```

決め打ちは `MC.ui.AUTO`（60秒 / climax / marchinz）。
専用画面には4つだけ出す: 大きなプレビュー / 進み具合 / いま何をしているか / あと何分。
段が済むたびに「傾きを直しました」と**成果の言い方**でチェックが点く。

### こだわり（5工程）

`mat（動画）→ tilt（傾き）→ sync（同期）→ length（長さと開始位置）→ export（書き出し）`

---

## 3. 次にやること

### 実機でしか分からない（最優先）

1. **iPhoneで実際に書き出しが成功するか** — 一度も確認できていない
2. **3本同時再生が10分持つか** — 改革の前提。崩れたら「見る道具」化は白紙
3. 20分素材の取り込み

### 未着手

- 提案書の段2「同期後すぐ再生画面へ」
- `ui.js` は 3,500行超。責務の分割は未着手（重複排除だけ実施）

---

## 4. このセッションで踏んだ落とし穴（同じ轍を踏まないために）

1. **本番の挙動は本番の配信物で測る。** 手元の修正済みコードを測って
   「直っている」と誤認し、書き出し即死の原因究明を一度後回しにした。
   `curl <本番URL>/js/xxx.js` で実物を読むのが最短
2. **消した工程名が条件に残ると静かに壊れる。** `rank.polish` が undefined に
   なり `数値 < undefined` が常に false ─ タブが最初の画面から出っぱなしだった
3. **`Function.length` は既定値つき引数を数えない。** `onProg = null` は 0 と
   数えられる。引数の有無を見るならソースの形で見る。
   さらに **`toString()` は関数名を含まない** ─ `async (onProg = null) => {` から
   始まるので `/detect\s*=\s*async/` のような正規表現は当たらない。
   仮引数だけを取る形が確実:
   `const argsOf = fn => (String(fn).match(/^[^(]*\(([^)]*)\)/) || [,""])[1];`
4. **`toString()` はコメントも返す。** 「なぜ直したか」の注記に正規表現が
   反応して試験が落ちる。コメントを除いてから当てる
5. **`body { overflow:hidden }` だけでは背後が止まらない。** `html` にも要る
6. **`play()` の中の `applyMute` が後から上書きする。** 消音は play() の後で
7. **QAの `compReset` が状態を戻していないと、試験どうしが汚染し合う。**
   `showIn/showOut/lengthDecided` 等を戻すよう修正済み
8. **試験データの雑音が雑音でないことがある。** 素朴なLCGは鋸波になり
   調波性が上がって「打楽器」が「静か」と判定された。xorshift32 を使う

## 5. 注意（変わらず有効）

- **push と本番デプロイは毎回かならず確認を取る。1度の許可は1度の実行にしか使えない**
- Drive 同期がコミットを巻き戻す。push 前に `git show HEAD:<file>` で実体を見る
- 一括置換は途中で失敗すると後続が丸ごと未適用になる。件数を assert し、
  報告前に git blob で数える
- このMacに node/npm は無い。テストはブラウザで `tools/qa/runner.html`
- **版番の書き換えは計23箇所**（root `index.html` ×2 + `tools/switcher/index.html` ×21）
  ※ horizon.js を読み込みへ戻したので 22 → 23 に増えた
- プレビューサーバは Drive 配下を配信できない。scratchpad へ rsync して配信
  （配信元は `scratchpad/mz`。`.claude/launch.json` の serve.sh が指すパス）

## 6. QAの穴（正直な記録）

- 本物の `exportMP4Parts` を runner から走らせられない
  （iframe内でOPFSのwriter Workerが応答せずスイート全体が固まる）
- おまかせ専用画面の試験は、重い処理をスタブに差し替えた通し。
  実素材での通しは未実施
