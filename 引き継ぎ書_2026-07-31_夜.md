# MarchinZ 引き継ぎ書（2026-07-31 夜）

## 現在地

- 版番 **v1.55.0**。**本番へ反映ずみ**（2026-07-31 夜に push、配信物で確認）
- 本番の実物を curl で取り、原因だった `_partsFallback` が 0 件・`{noParts:true}` が 5 箇所あることを確認
- QA: `tools/qa/runner.html` を **89件**（傾き3件を新設計向けに書き直し、分割フォールバック判定を1件追加）

## この回でやったこと（優さんの5点）

| # | 依頼 | 状態 |
|---|------|------|
| ① | 管理者ラベルの不具合を直す | 済（d595b37） |
| ② | 文章が不快。常用漢字・Apple のような文章に | 済（81件を書き直し） |
| ③ | 傾きは単独ページに。素材ごとに1ページ | 済（0ff6565） |
| ④ | 下に書いてあることを上にも書かない。その分プレビューを上へ | 済（46px 上がった） |
| ⑤ | 書き出しで即死する（1秒ない） | **原因確定・本番反映ずみ**。実機確認待ち。下記 |

### ③ 傾きの単独ページ化
- 工程が7つに戻った（動画 → 傾き → 同期 → 音声 → 長さ → 設定 → 書出）
- `#tiltSec` の `hidden` 属性は廃止。出し分けは `applySteps` に一本化
- `closeTilt` → `unpinTilt`（パネルの表示には触らず、固定プレビューだけ解く）
- 1画面に1本ぶん。動画1/3 → 動画2/3 → 動画3/3 と送る（実測で確認）

### ④ 上部バー
- `.mzj-activity`（活動テキストの行）を廃止。バー 106px → **60px**
- プレビューの上端 208px → **162px**

### ② 文章
- ひらがな書きを常用漢字へ（かたむき→傾き、そろえる→合わせる、はじまり→開始 ほか）
- 工程名の呼び分けを解消（動画を入れる / 動画を読み込む / 動画を選ぶ → **動画を選ぶ**）
- 画質の名前を **標準 / 高画質** に（「はやい / きれい」は速度と画質が混ざっていた）
- 残っている かな 表記はコード内コメントのみ（画面文言は 0 件・実体確認済み）

## ⑤ 書き出しの即死 — 原因確定（本番はまだ壊れている）

**原因は分割書き出しの無限再帰。** 本番 v1.54.0 の配信物を取って確認した。

```
exportMP4()            _partsFallback = false   ← 1行目で必ず消す
  └ 分割へ  exportMP4Parts()
        └ 容量チェックが失敗
          └ _partsFallback = true               ← 印を立てて
            └ exportMP4() を呼ぶ
                  └ _partsFallback = false      ← 呼ばれた側が即座に消す
                    └ また分割へ …  無限
```

再帰を防ぐための印を、呼ばれた側の1行目が毎回消している。**この防止策は
原理的に一度も効かない。** 往復中は1コマも書かないので `mz_switcher_export_at_v1`
に何も残らず、「ログが出ない」のはこのため。

対照実験（ブラウザ実測）:

| コード | exportMP4 の回数 | 分割の回数 | 結果 |
|---|---|---|---|
| 本番と同じ書き方 | 51 | 51 | **止まらない**（50回で打ち切り） |
| 手元の修正版 | 2 | 1 | 1回だけ降りて従来経路へ着地 |

条件は「書き出しが135秒超（＝3分・まるごと）」かつ「容量チェックが通らない」。
59秒なら分割へ入らないので起きない。

### 修正（本番 v1.55.0 に反映ずみ）

- `d595b37` 印をモジュール変数から**引数**へ。呼ばれた側が消せない
- `a38702e` 分割の入口で落ちたら従来方式で自動的にやり直す
  （`shouldFallbackToLegacy`。中止・1パート以上書けている・完成済みは対象外）
- `a38702e` どこまで進んだかの足跡（`_markPhase` → sessionStorage）。
  1コマも書けずに落ちた回でも、次に開いたときの案内に段が出る
- 5巡目レビューで、降りた理由が**成功すると誰にも届かない**穴も塞いだ

### 私の誤り（記録）

一度「無限再帰ではなかった」と訂正したが、**その訂正が誤り**だった。
測ったのは既に修正済みの手元コードで、本番の話だと思い込んでいた。
本番の挙動は本番の配信物（`curl`）を取って確かめること。

## 5巡目レビュー（UI/UXデザイナー + シニアエンジニア）で直した4件

- **P0** 「1つ前の動画」が押せるのに1つも戻らない（`ui.js` の `tiltPrevBtn`）。
  順送りになったため1つ前は必ず確認ずみで、`renderTiltSec` の
  「未確認から始める」再探索に上書きされていた。`_tiltPick` で明示指定を優先
- **P1** 分割から降りた理由が、書き出しが成功すると誰にも届かない
  （詳しいログは失敗時しか出ない。`lastPartsError` は未参照だった）
- **P2** 傾き画面で「動画N / 3」の変化が読み上げに乗らない → `role="status"`
- **P2** 「別の場面」は名詞 → 「別の場面を見る」（ボタンは動詞で終える）

却下（裏を取って実害なし）: 差し戻しの隙の `running=false`（オーバーレイの下で
押せない）／傾き画面の余白の積み上がり（5回連続 144px で固定）

## 次にやること（優さんへのお願い）

3分または「まるごと」を選んで書き出す。これまで1秒で落ちていた条件。
別の形で止まったら、**同じタブでもう一度開く**と上部に
「書き出しが○%で止まりました…」が出て、行末に「分割: パート2/6を開始」の
ように**どこで消えたか**が載る（v1.55.0 から、1コマも書けていなくても残る）。
その1行が次の手掛かりになる。

## 注意（変わらず有効）

- **push と本番デプロイは毎回かならず確認を取る。1度の許可は1度の実行にしか使えない**
- Drive 同期がコミットを巻き戻す。push 前に `git show HEAD:<file>` で実体を見る
- 一括置換は途中で失敗すると後続が丸ごと未適用になる。件数を assert し、
  報告前に git blob で数える
- このMacに node/npm は無い。テストはブラウザで `tools/qa/runner.html`
- 版番の書き換えは計22箇所（root `index.html` ×2 + `tools/switcher/index.html` ×20）
- レビュワーの指摘は file:line とコードで裏を取ってから採否を決める

## 既知の穴（QAで守れていないところ）

- 本物の `exportMP4Parts` を runner から走らせるのは断念
  （iframe内でOPFSのwriter Workerが応答せずスイート全体が固まる）
  → フォールバックは「判定の表」と「戻り道に noParts が付いていること」で
    薄く見張っているだけ。経路の接続そのものは守れていない
- iPhone 実機での 8分素材の書き出し / 20分素材の取り込みは未検証

## 優さん判断の保留

既定画質 720 vs 1080 / 冒頭8秒の無音 / ハイライト切り出し /
Privacy 60秒上限 / MarchinZカラー既定ON / 30fps固定・4K非対応
