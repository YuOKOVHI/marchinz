# MarchinZ 引き継ぎ 2026-07-13

## いちばん大事なこと(結論)

**本番は 1.35.5 まで反映済み・稼働中。ローカルに 1.35.7 相当の 5 コミットが未 push。**
次のセッションの最初の仕事は「この 5 コミットを push して本番へ出す」こと。firebase deploy は今回不要(ルール/インデックス変更なし)。

- 本番: https://marchinz.netlify.app(現在 data-mz-version 1.35.5 が稼働)
- **GitHub main(origin) = `9412ed1`** / **ローカル HEAD = `3788c6c`(data-mz-version 1.35.7)**
- リポジトリ: `マイドライブ/CursorLogs/010_MarchinZ`(GitHub: `YuOKOVHI/marchinz`)
- **push状況は必ず `git ls-remote origin refs/heads/main` を正とする**(Drive 撹乱で `git rev-parse` 等ローカル追跡が古い値を返す。今回も HEAD を誤って別ハッシュと表示した)

---

## ▼ 未 push の 5 コミット(古い順)

```
a4867ee chore: data-mz-version 1.35.0→1.35.5(note機能/TOP調整/FOUC修正の区切り)
be1b1da feat(journey): MarchinZ Log動画を移動カメラに(プレビューと同じ寄り+パン)
fb2ccf1 feat(switcher): 自動水平補正をマスターON/OFFトグル化 + 両ツールにベータ免責
bde377d feat(reangle): ReAngle移植① 生ファイルを tools/reangle/ へ配置
3788c6c feat(reangle): ReAngle移植②〜⑥ ヘッダ/カード/UGCログ/netlify/SPEC(data-mz-version 1.35.7)
```

いずれもローカル検証済み。push すれば Netlify 自動デプロイで本番反映される。

---

## ▼ 次のセッションにそのまま貼るテキスト

```
010_MarchinZ の続き。本番は 1.35.5 稼働中で、ローカルに 1.35.7 相当の未push 5コミットがある
(journey移動カメラ / switcher自動水平トグル+両ツール免責 / ReAngle移植①②)。
まずこれを本番へ出したい。

push手順(順番厳守):
1. git ls-remote origin refs/heads/main で本当のorigin/mainを確認(Drive撹乱でローカル追跡は当てにしない)
2. git pull --rebase origin main(毎朝5時のbotがデータを push しているため。data系のみで通常衝突しない。
   念のため git bundle を ~/Movies 等 Drive外へ取ってから)
3. git push origin main(5コミットまとめて1回)
4. 反映確認: curl -s https://marchinz.netlify.app/index.html | grep data-mz-version が 1.35.7 になるか、
   /tools/reangle/ が開くか

firebase deploy は今回不要(ルール/インデックス変更なし)。

注意:
- push は手動でこまめに(自動保存/自動pushは不採用)。
- Drive巻き戻り罠が活発。編集は scratchpad の git-blob コピー(git show HEAD:<file> や cp -R)で行い、
  cp→即commit→git show HEAD:<file> で実体検証。push判定は git ls-remote を正とする。
- 検証はスマホ・iOS Safari最優先(375px)、UIアイコンは Font Awesome モノクロ統一。
- 自動ブラウザはページが hidden 扱いで requestAnimationFrame が発火しない。録画系(journey/reangle)は
  内部関数を手動で叩くか実機で確認する。ローカル配信は Service Worker/キャッシュ回避のため新ポートで。
```

---

## ▼ 今セッション(2026-07-12〜13)でやったこと

### 1. MarchinZ Log 動画を「移動カメラ」に作り替え(be1b1da)
- 従来: 全停留が収まる 1 ズーム固定で地図 1 枚だけ描画=引いた絵。カメラ不動でアバターだけ移動。
- 変更後: プレビュー(journey.js)と同じく、各停留にズームして寄り→移動中は 2 点が入るまで引いてパン→最後に全体へ引く**移動カメラ**。毎フレーム、カメラ位置/ズームに応じてタイルを描き直す(タイルはカメラ経路を先読みして一括ロード、上限 420 枚で自動調整)。`marchinz-journey-export.js`、`?v` 1.35.6。
- 検証: 東京→横浜→名古屋→大阪で、各停留が画面中央に来てズーム 8↔10.5 で変化、タイル 412/412、最後に全体へ引く、を計測確認。

### 2. Switcher「仕上げ」に自動水平補正のマスター ON/OFF トグル(fb2ccf1)
- 「自動水平補正」チェックボックス(既定 OFF)。ON=全スロット一括自動検出+スライダー表示、OFF=補正を適用しない(`layout.js` で rot をゲート)。`state.js`/`layout.js`/`ui.js`/`index.html`。

### 3. Switcher・Privacy 両方にベータ免責(fb2ccf1)
- ヘッダー直下にアンバー×クリームの注記「⚠️ 本ツールはベータ版です。動作は保証されません。ご利用は自己責任で…」。

### 4. ReAngle を MarchinZ クリエイターツールへ移植(bde377d + 3788c6c)
- `CloudeCode_u/ReAngle/` → `tools/reangle/`(Switcher/Privacy と同枠組み・SPA 本体の重量増ゼロ)。斜め撮り動画を正面視点へ透視補正(WebGL 逆マッピング/床 4 隅ホモグラフィ)。
- ヘッダを MarchinZ 標準(ロゴ→/#top、ReAngle+ベータ版バッジ、statusPill 維持で caps-badge 化、戻る→/#creators)。本文はダークテーマのまま(指示どおり)。
- TOP・#creators のクリエイターツールカードを **ReAngle → Switcher → Privacy** の順に(ReAngle カード fa-vector-square を追加)。
- 利用ログ(`marchinz-admin-ugc-log.js`)に reangle を配線。netlify.toml に reangle vendor の 7 日キャッシュ。
- **本文は元のダーク。実機 iPhone Safari での保存(navigator.share)・実演技動画(4K HEVC/回転付き)の書き出し速度・iOS16 の MediaRecorder フォールバックは未検証**=ベータ表記のまま。

### ※ 前セッション分(既に push・本番反映済み・1.35.5)
note 新着フィード(TOP の LATEST NOTE + 新着 note バンド、メディアの note カード)、公開ログ permission-denied 修正、TOP レイアウト調整(ブロック順・件数・スマホ 2×2)、YouTube+note の毎朝 5 時自動更新(GitHub Actions)、未ログイン時の管理者ナビ FOUC 修正。詳細は前版 `引き継ぎ_2026-07-12.md`。

---

## ▼ 残タスク

- **未 push 5 コミットの push**(最優先。上の手順)。
- ReAngle 実機 iPhone 確認(保存/書き出し速度/発熱/向き/ドラッグ操作感)。
- Switcher/Privacy 実機の保存系(前からの持ち越し)。
- YouTube 新着のスマホ表示を 2×2 に揃えるか(今は 1 列。要判断)。
- `section_vis_mll` バックフィル(pc20 機。公開 feed に旧ログを出すため。急がない)。
- プロフィールへの練習バッジ任意表示(公開範囲設計から)。

---

## ▼ 作業の注意(このリポジトリ特有)

- **Drive 巻き戻り罠が活発**。commit 直前/読み取り中に Google Drive がファイルや `.git` 参照を旧版へ戻す。ファイル名列挙の cp が「偽成功」することもある(今回 tools/reangle が一度消えた→`cp -R` でディレクトリごと写し直し直後に実在確認で解決)。**編集は scratchpad の git-blob コピーで行い、cp→即 commit→`git show HEAD:<file>` 検証。push 判定は `git ls-remote`。要所で `git bundle` を Drive 外へ。**
- **毎朝 5 時に bot(GitHub Actions)が YouTube+note データを main へ push**。優さんの push 前は必ず `git pull --rebase origin main`。
- **push は手動でこまめに**(自動保存 launchd は不採用。スクリプト `~/bin/marchinz_autosave.sh` は残置だが未有効化)。
- **firebase deploy はルール/インデックス変更時のみ**、push より先。CLI は `~/bin/firebase`(marchinz-app ログイン済み)。
- ローカル確認は Drive を直接配信できない(TCC)ため `git archive HEAD | tar -x -C <scratchpad>` して `python3 -m http.server`。PWA の Service Worker/キャッシュ回避に**新しいポート**で配信するのが確実。
- 課金は Netlify($9 Personal)だけ。YouTube API/GitHub Actions/note RSS/Firestore はいずれも無料枠内。
