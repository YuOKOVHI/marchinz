# 010_MarchinZ 引き継ぎ書 2026-07-19c（夜・セッションリセット版）

## 現在地

- 作業ツリー: クリーン（未追跡は `引き継ぎ書_*.md` のみ）
- **未push 0件**。ローカル = `origin/main` = `24bfab6`
- 本番版: **v1.38.4**（Netlify自動デプロイ済み。反映確認は未）
- 朝の引き継ぎ書は `引き継ぎ書_2026-07-19b.md`（この書はその続き）

```
本日push済み（朝の未push9件 + 今日の分、計14コミット）
5117f99 …（朝の時点の最新。ここまでが前セッション分）
cd85683 fix(reangle): 床自動検出v2
3a2347b feat(reangle): 「見る位置（左右）」スライダー
4f9f3ee fix(reangle): プロレビュー指摘の反映
46385d0 chore: メジャーアップデート 1.37.0
992afb6 fix(tools): ウォーターマーク透過化+フィルター/境界線の初期値
6e3fe44 feat(switcher): IN/OUT視覚化+MarchinZカラー改名+TOPカード全幅
d9afd04 fix(switcher): プレビューが止まって切り替わらない問題 → v1.37.2
fd79d88 feat(tools): 使える条件+TOPと同じヘッダー/フッター → v1.38.0
b1b7627 chore(data): botの日次データ更新（自動）
48c0d6d feat(switcher): エラーログ+おすすめ/こだわりタブ+sticky+横型のみ → v1.38.1
45e7c2b perf(profile): マイページ先行ペイント+タブ帯の揺れ解消 → v1.38.2
c7aab51 feat: ハンバーガーにSwitcher+マイページのスマホ最適化 → v1.38.3
24bfab6 fix: プロレビュー指摘の反映 → v1.38.4
```

## 今日やったこと（時系列）

### 1. ReAngle 床自動検出v2（cd85683）
旧検出は「床＝画面下部の大面積」前提で、**アリーナ撮影（画面下部＝観客席・床＝画面中段）で観客込みの歪んだ四角を返し、既定90%補正が映像全体を回転・シアーさせていた**。複数バンド探索（y=0.48〜0.83）＋p60散らばり判定＋**エッジ実在性検証**（辺の内外プローブ）＋信頼度後処理（実在しない辺は補正が効かない形へ均す）で根治。

### 2. ReAngle「見る位置（左右）」スライダー（3a2347b）
視点の横移動を出力空間の水平シアー `x' = x + k*(cy-y)` で再現。±25、tiltの外側に合成、縦動画はアスペクト比で減衰。

### 3. Switcher 各種（992afb6 / 6e3fe44）
- ウォーターマークPNGが**JPEG由来で白背景付き**だった → 透過化（520×173）＋右上端へ
- フィルター初期値を常に「MarchinZカラー」に（保存値の復元を廃止・名称も変更）
- 自動スイッチングは境界線を初期オフ
- IN/OUTの視覚化（旗ボタン・範囲バンド・範囲外はプレビュー暗転）
- TOPのSwitcherカードをウィジェット幅いっぱいに

### 4. Switcher「プレビューで切り替わらない」の根治（d9afd04）★重要
**真因: 描画ループが `requestAnimationFrame` 単独依存**。rAFが発火しない環境では再生しても `MC.S.t` が0のまま進まず、画面は黒で一切切り替わらない（実測で再現）。ReAngleと同じ `setInterval` 保険を追加。あわせてカット切替時の `ensurePlaying`（切替直後の最大0.5秒静止を解消）、プレビュー左下に「カメラN」バッジ。

**この件で最初「機能は正常」と誤報告した。** 検証がシーク＋1コマ描画だけで、優さんが見ている「再生中」を試していなかった。さらに平均RGBでカメラ判別しており、カラーマッチが色を揃えると判別不能だった。

### 5. ツール3本をサイトの一部に（fd79d88 / c7aab51 / 24bfab6）
`tools/shared/sitechrome.{js,css}` を新設し、ブランドヘッダー＋グローバルナビ＋サイト標準フッターを3ツールへ注入（`<body data-mz-tool="xxx">` で自動マウント）。
- 「使える条件」を1行で（ゲスト「ゲストは5分まで、登録ユーザーは12分まで」／ログイン時「12分まで使えます」）
- フッターは「できること」「つづけて使う」を削除しYouTubeページと同じ構成に。「無料・すべて端末内」はページ下部へ
- **719px以下は本体と同じくハンバーガー＋ドロワー**（ドロワーに「映像ツール」節を置きツール間の導線を確保）

### 6. Switcher 初見導線（48c0d6d）
- **エラーログを画面に表示**（版番/UA/素材/スタック込み・ワンタップコピー）。未捕捉エラーとPromise拒否も収集
- **「おすすめ／こだわり」タブ**。初期はおすすめで「おすすめで開始」1つ（同期→カット割→カラーマッチを一括）
- プレビューをsticky化、自動スイッチングは横型のみ

### 7. マイページ高速化（45e7c2b / c7aab51）
`loadAndRenderCore` が**ヘッダー描画前に8つの一覧クエリ全部を待っていた**のが遅さの主因。プロフィール文書到着時点でヘッダー/カバー/練習記録(Days)を即描画・即マウントする**先行ペイント**に変更。カレンダー照合表（500件）を5分キャッシュ。タブは横スクロール帯→**アイコン付きグリッドチップ**（`auto-fit`・最低96px）に刷新し、上下揺れも構造的に解消。

### 8. プロレビュー2回実施 → 全反映（4f9f3ee / 24bfab6）
サブエージェント＝シニアエンジニア役、私＝UI/UXデザイナー役。2回目で**[高]3件**を検出：

- **`[hidden]` がサイト本体で効いていなかった** — `.user-profile-tab{display:inline-flex}` がUAの `[hidden]{display:none}` に勝ち、`el.hidden=true` が全て無効。**本人限定の「MarchinZ Days」タブが他人・ゲストにも見えていた**（実測確認）
- **プレビュー専用の重ね描きが実時間録画に焼き込まれていた** — `exportRealtime` はプレビューcanvasをそのまま録画するため、**iOS Safari（最優先ターゲット）でカメラバッジ入りMP4**が出ていた。`MC.preview.overlayOn` で解決
- 先行ペイントが上記①の露出時間を延ばす問題

## 未着手・持ち越し（優先順）

1. **本番での実機確認（最優先）** — v1.38.4は全てpush済みだが**本番での動作確認は一切していない**。特に:
   - マイページ: 「MarchinZ Days」タブが他人から見えなくなったか
   - マイページ: 表示速度の体感改善、タブグリッドの見た目（実データで）
   - Switcher: iPhoneでの書き出しにバッジが焼き込まれないか
2. **Switcher書き出しエラーの正体が未特定** — 優さん報告の「書き出しができない/エラーになる」は**本番ゲスト状態の合成素材では再現せず**（正常に3.6MB生成）。実素材17〜23分は5分/12分制限を超えるため読み込み時点で弾かれていた可能性。**次に出たらエラーログのコピーを送ってもらう**（v1.38.1で表示機能を実装済み）
3. **実素材でのSwitcherカット割の「画のセンス」確認** — Timeline 1-3.mov（~/Desktop/自動整理/）でまだ未実施
4. Privacy/ReAngleの書き出し音声PCM対応（さらに前から持ち越し）

## 教訓・ハマりどころ

- **`[hidden]` の罠は本体でも踏んだ**。`display` 指定のあるクラスは `[hidden]` に詳細度で勝つ。**対症療法（個別セレクタ）ではなくCSS先頭に `[hidden]{display:none!important}` を1行**。共有CSSモジュールは自前でガードを持つこと（利用側依存は4本目で破綻）。検出法: hidden属性つき要素で `getComputedStyle(el).display` を実測
- **rAF単独依存は禁止**（Switcher/ReAngleとも）。発火しない環境で全機能が止まる。`setInterval` 保険を必ず併設し、再生中だけ回す
- **検証は「ユーザーが実際にする操作」で行う**。シーク＋1コマ描画で「正常」と判断して誤報告した
- **色での判別は当てにならない**（カラーマッチが色を揃える）。合成素材はカメラごとに**形状**を変え（バー3/8/14本）、明暗遷移をカウントして判別する
- **ブラウザペインは大容量fetch（>128MB級）を遮断**するため実MOV（2GB級）のE2E不可 → WebCodecsでページ内合成素材を作る（`scratchpad/site/tools/switcher/testgen.js`）
- **ペインはsetTimeoutも強く絞る** → エンコーダのバックプレッシャ待ちは `MC.yield()`（MessageChannel）必須
- **本体JSを変えたら `index.html` の `?v=` 更新を忘れない**（user-profile-page.js が1.34.1のまま放置されていた）
- push直前にbotの日次データ更新が入ることがある → `git pull --rebase` で解決（データファイルのみなので無衝突）

## 運用ルール（継続）

- push前は deploy-guard スキル必須＋`git show HEAD:<file>` で実体裏取り
- スクリプト優先編集、Node無し（Python+venv）。**Pythonで日本語を扱うときは `# -*- coding: utf-8 -*-` と `io.open(..., encoding='utf-8')`**
- iOS Safari最優先（375px）、UIアイコンはFont Awesomeモノクロ
- push はまとめて1回（Netlifyクレジット節約）

## 関連メモリ

`marchinz-project.md` / `switcher-director-update.md` / `reangle-tool.md` / `css-hidden-attribute-trap.md` / `marchinz-deploy-flow.md` に本日分を追記済み。
