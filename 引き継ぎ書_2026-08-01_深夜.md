# MarchinZ 引き継ぎ書（2026-08-01 深夜）

前の版: [引き継ぎ書_2026-08-01_夜2.md](引き継ぎ書_2026-08-01_夜2.md)

## 現在地

```
本番   v1.71.0（2026-08-01 深夜 push済み・配信物検証済み）
QA     tools/qa/runner.html を 145件 全PASS
```

**この日の大きな出来事: iPhoneで初めて書き出しが完走した**（v1.70.0、52.3秒/1770コマ/56MB）。
「一度も成功していない」は終わった。

## 1. v1.69.0 → v1.71.0 で入ったもの

### 0%で無言に固まる問題（解決）
真因は mp4box の seek が**全トラックの最小オフセット**を返すこと。
業務用カメラ/Resolve の .mov はタイムコード(tmcd)トラックの1サンプルが
mdat 先頭にあり、どの時刻を頼んでもファイル先頭から読み直していた。
`tools/shared/mp4source.js` の `seekOffsetFor()`（対象トラックだけ見る）で修正。
全ツール（Switcher/Vlog/Privacy/ReAngle）に効く。

### 見張り（runWatched）
書き出しが進まなくなったら「最後に通ったところ」を名指しして止まる。
`beat()` が進んだ印（コマループ等10箇所）。止まれば失敗の顔+記録コピー。

### 音（優さんの決定 2026-08-01）
**書き出しの整音は「音量をそろえる+音割れ防止リミッタ」の2つだけ。**
70Hzハイパスと下方エキスパンダ（雑音を沈める）は削除
（実機で音が消えた最有力容疑。真因は未確定のまま容疑を排除した形）。
音の**選定**（雑音が少なく割れていないカメラを選ぶ）は変更なし。
QAに「lpcm MOV→AAC→往復デコードで音が鳴る」実測試験あり（audio=true でも
無音、という壊れ方を常時見張る）。

### 1080p統一（優さんの指示）
720p系の分岐を削除。9x16=1080x1920。標準/高画質はビットレート差のみ(8/12Mbps)。
**書き出しは約2倍（100秒前後）になる見込み。落ちる場合は解像度でなく
PART_SEC を絞る方針**（コードに注記あり）。

### 別のシーンも作る（優さんの要望）
完成画面に候補1〜5個（演奏の長さで増減、直前の窓と半分以上重なるものは除外、
大盛り上がり/バラード等の既存語彙）。押すと解析を使い回して自走。
2回目は約2.5〜3分（1回目の約半分）。ui.js: sceneOffers / makeAnotherScene。

### 保存まわり（優さんの指示）
スマホ=「動画を保存」1ボタンのみ（Web Share）/ PC=ダウンロードのみ。
判定は canShare({files}) の機能検出。share失敗(AbortError以外)はDLへフォールバック。
成功時だけ「✓保存しました（サイズ）」。取り消しは区別して何も出さない。

### そのほか
- 円形の進捗（canvas直描き・preview.js）を12時始点・終点に
- Privacy案内を一般ユーザーから撤去（Privacyは画像のみ対応のため）。管理者には残す
- こだわりの #syncBtn/#autocutBtn/#colorMatchBtn に作業中の鍵
  （同期の最中に書き出しを押せた「黙って壊れる」経路を閉鎖）
- おまかせ全画面・最初の2択・完了画面を375px/320px/横向きで実測して作り直し
- 失敗の顔: 進行中の見た目を全部止める/失敗ごとの手がかり/記録コピー1本化

## 2. 次にやること（実機で確認）

1. **音が入るか**（整音は音量調整だけになった。それでも無音なら真因は別=muxing等）
2. **1080x1920で完走するか**（約100秒が正常。落ちたら PART_SEC を絞る）
3. **別のシーンで2本目が出るか**（約2.5〜3分の見込み）
4. 3本同時再生が10分持つか（「見る道具」化の前提。ずっと未確認）

## 3. 落とし穴（このセッションで新たに学んだもの）

- **mp4box の seek は使わない**。seekOffsetFor で対象トラックだけ見る
- **見張りは「進んだ印」をループの中で打つ**。v1.69.0 の私の見張りは stage() でしか
  打っておらず、健全な書き出しを120秒で殺す状態だった（assertは個数しか見ていなかった）
- **Promise.race は負けた側を止めない**。見張りが鳴ったら cancelFlag を立てる
- 整音のゲイン根拠に 8kHz 統計のピークを使わない（打楽器のアタックを拾えず過大増幅）
- Web Share の resolve は共有シート完了までで、実際の保存の保証ではない（コード注記あり）
- 版番は **switcher 31 + root 2 = 33箇所**（共有モジュールにも ?v= を付けた）
- プレビューサーバの reused:true は**別セッションの別ディレクトリ**を配信していることがある。
  `lsof -a -p <pid> -d cwd` で実体を見る

## 4. 注意（変わらず有効）

- push と本番デプロイは毎回かならず確認。1度の許可は1度の実行にしか使えない
- 作業完了・許可待ちのタイミングで PushNotification を送る（優さん指示 2026-08-01）
- Drive巻き戻り: push前に git show HEAD:<file> で実体を見る
- 一括置換は件数assert、本番の挙動は本番の配信物（curl）で測る
- node/npm無し。テストはブラウザで tools/qa/runner.html（scratchpad配信 + ?cb=）
