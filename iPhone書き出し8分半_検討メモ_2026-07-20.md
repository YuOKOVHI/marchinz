# iPhone で 8分30秒を書き出す — 検討メモ(2026-07-20)

上位モデルでの再検討用。**結論は出していない。** 選択肢と計算根拠、
検証済み/未検証の境界を残す。

## 要件

- **マーチングのショウは8分。最低でも 8分30秒(510秒)は書き出せるようにしたい**(優さん)
- 保存先は iPhone のカメラロール(共有シート経由)
- 対象は Switcher の書き出し。Mac は既に制限なしで動く想定(v1.39.15 の fastStart 修正済み)

## なぜ iPhone だけ制限があるのか

| | Mac (Chrome) | iPhone (Safari) |
|---|---|---|
| `showSaveFilePicker` | ある | **ない** |
| 書き出し先 | ディスクへ直接(`FileSystemWritableFileStreamTarget`) | **メモリに全部載せるしかない** |
| 尺の制限 | 実質なし | メモリ上限で決まる |

iOS Safari はタブのメモリ上限が厳しく、超えると**警告なくタブごと落ちる**
(エラーも出ない)。v1.39.20 で「始める前に断る」ガードを入れた。

## 現状(v1.39.21 時点)

```
MC.exporter.MEM_HARD_LIMIT = MC.isIOS ? 260e6 : 1.6e9;
MC.exporter.videoBitrate()  = MC.isIOS ? 5e6   : (preset==="1x1" ? 8e6 : 12e6);
→ iPhone で書き出せるのは 6.7分 まで。8分30秒は断られる
```

**`260MB` は私の推測値で、実測に基づかない。** ここが最大の不確実性(後述)。

## 計算: 510秒を収めるのに必要なビットレート

```
260MB - 音声12.2MB(192kbps×510s) = 247.8MB(映像)
247.8e6 × 8 ÷ 510 = 3.89 Mbps
```

これが**どの程度の画質か**を bpp(1ピクセル1フレームあたりのビット数)で見る。

| 解像度 | 3.89Mbps での bpp | 評価 |
|---|---|---|
| 1080x1920 | **0.063** | 破綻域。動きの激しいマーチングでは確実にブロックノイズ |
| 900x1600 | 0.090 | 厳しい |
| **720x1280** | **0.141** | 実用域。SNS なら十分見られる |
| 540x960 | 0.250 | 余裕だが解像感が落ちる |

> H.264 の目安: 0.1 を下回ると厳しい / 0.15 前後で良好。
> マーチングは大人数の細かい動き+パンで、圧縮に最も厳しい部類の素材。

**結論: 260MB という枠を前提にする限り、1080p のまま8分半は物理的に無理。720p なら現実的。**

---

## 選択肢

### A. OPFS 経由でメモリ制限そのものを外す(本命候補・要実機検証)

iOS Safari も **OPFS(Origin Private File System)** に対応しており、
そこには `FileSystemWritableFileStream` がある。
= **Mac で使っているディスク直接書き込みと同じ仕組みが iPhone でも使える可能性**。

```js
const root = await navigator.storage.getDirectory();
const fh = await root.getFileHandle("export.mp4", { create: true });
const writable = await fh.createWritable();
const target = new Mp4Muxer.FileSystemWritableFileStreamTarget(writable);
// … muxer で書き出し …
await writable.close();
const file = await fh.getFile();     // ← ここが懸念(下記)
await navigator.share({ files: [file] });
```

**検証済み(Chrome)**: 実際の Mp4Muxer + VideoEncoder で30フレームを OPFS の
本物の `FileSystemWritableFileStream` へ書き、finalize まで通り、
生成MP4が `<video>` で再生できることを確認済み(2026-07-20)。

**未検証・最大の懸念**: 最後の `getFile()` → `navigator.share({files})`。
共有シートへ渡す時点で iOS がファイル全体をメモリへ読み込むなら、
結局同じ上限に当たる。**遅延読み込みされるかは実機でしか分からない。**

- 通れば: **1080p・12Mbps のまま尺の制限が消える**(最良)
- 通らなければ: B へフォールバック
- 実装規模: 中(exporter の target 選択とiOS分岐、共有経路の変更)

補足: OPFS は書き出し後にゴミが残るので、成功・失敗どちらでも
`root.removeEntry()` で後始末が要る。ストレージ容量の確認(`navigator.storage.estimate()`)も。

### B. iOS だけ 720x1280 / 4Mbps に落とす(確実)

```
720x1280 / 4Mbps → 8分30秒 = 約268MB / 0.141 bpp
```

- 確実に収まり、実装は小さい(`PRESETS` と `videoBitrate()` の iOS 分岐だけ)
- SNS 投稿なら実用上問題ない画質だが、**Mac 書き出しより明らかに粗くなる**
- Instagram/TikTok は結局再圧縮するので、実害は見た目ほど大きくない可能性

### C. iPhone は長尺をあきらめる(現状維持)

- iPhone はハイライト(数分)専用、ショウ全体は Mac で書き出す運用
- 画質を一切妥協せず、実装も現状のまま
- 優さんの要件「8分30秒」を満たさない

### D. 実時間録画(MediaRecorder)経路を使う

iOS Safari は MediaRecorder 対応。チャンクを逐次受け取れるため、
理屈の上ではメモリに溜めずに済む。ただし:
- **書き出しに実時間かかる**(8分の動画に8分)。現在の fast 経路は数分
- 既存の `exportRealtime` があるが、これも最後に Blob 化している実装なら同じ問題
- 画質・音質の制御が効きにくい

---

## 先に潰すべき不確実性

### ★ `MEM_HARD_LIMIT = 260MB` の根拠がない

私が保守的に置いた推測値。実際の iOS Safari のタブ上限は端末の RAM に依存し、
iPhone 15 Pro(8GB)と iPhone 12(4GB)では大きく違う。
**もし実際に 500MB 使えるなら、B の 720p すら不要で、1080p/5Mbps で 8分半が収まる**
(510秒 × 5.19Mbps ÷ 8 = 331MB)。

**測る方法**: 書き出し前に段階的な `ArrayBuffer` 確保を試すプローブを入れ、
実機で通る最大値をログに出す。ただし「確保できる」と「その後の処理も通る」は別なので、
実測値の 6〜7割を上限に採るのが安全。

```js
for (const mb of [1200, 900, 700, 500, 350, 250, 150]) {
  try { const b = new ArrayBuffer(mb * 1e6); b.byteLength; return mb; } catch (e) {}
}
```

### 音声側のメモリも効いている

`encodeAudioPcm` は書き出し尺ぶんの Float32 を確保する。
510秒 × 48000Hz × 2ch × 4byte = **約196MB**。映像とは別に載る。
iPhone で 8分半を狙うなら、ここもチャンク処理へ変える必要がある(現状は未改修)。
**これは A・B どちらを選んでも効いてくる。**

---

## 進め方(提案)

1. **プローブで iPhone の実メモリ上限を測る**(小さい実装・情報価値が高い)
2. その値を見て A / B を決める
   - 500MB 以上使える → 1080p/5Mbps でそのまま 8分半が入る可能性
   - 300MB 前後 → A(OPFS)を試す。ダメなら B
3. どちらにせよ**音声のチャンク化**は必要
4. 実機検証は優さんの iPhone でしかできない(私は実素材・実機を扱えない)

## シニアエンジニア協議の結論(同日追記)

コードの実測(caps 判定・音声確保・realtime 経路)を踏まえて再検討した結果、
**選択肢の図が塗り替わった**。

### 決定打: fast 経路に入れる iPhone は、定義上 OPFS 直書きも使える

`caps.aac` は `AudioEncoder` の有無で決まる(exporter.js probeCaps)。
Safari のバージョン系譜は:

```
iOS 16.4  WebCodecs 映像のみ(AudioEncoder なし)→ caps.aac=false → realtime 経路
iOS 18.2  OPFS の createWritable が入る
iOS 18.4+ AudioEncoder が入る → ここで初めて fast 経路に入る
```

つまり **fast 経路(=メモリ問題が起きる経路)に入れる iPhone は、
必ず createWritable(Mac と同じディスク直書き)を持っている**。
- Worker + SyncAccessHandle の実装は不要
- 720p への解像度退避も初手では不要
- 古い iOS は realtime 経路で、そこは MediaRecorder のチャンク→Blob 方式
  (WebKit が大きな Blob をディスクへ退避できる)なので**今日すでにメモリ安全**

### 前メモの評価の訂正

- **「共有シートで結局メモリに載る」懸念は過大評価だった可能性が高い。**
  OPFS の `getFile()` はディスク裏付けの File(遅延参照)で、Blob/File は
  設計上 lazy。share はネイティブ側のストリームコピーで JS ヒープを通らない見込み。
  ただし実機未検証であることは変わらない(検証項目 a)
- **音声196MB は「出力バッファ」で、ソース読みは既にストリーミングだった**
  (`pcmChunks` + ストリーム型リサンプラ)。チャンク化は AAC を1024フレーム窓で
  回すだけで済み、見た目より小さい改修
- 260MB プローブは優先度が下がった(OPFS でヒープの大物が消えるため)。
  OPFS 不可時のフォールバックガードとしてだけ残す

### 採用案(A' = OPFS 直書き+音声チャンク化)

```
iPhone の書き出し分岐(実装後)
├ fast 経路(iOS 18.4+)
│   ├ OPFS 使える → createWritable でディスク直書き
│   │               1080p / 8Mbps / 尺の制限なし ← 8分半OK
│   │               finalize → getFile() → 共有シート → カメラロール
│   └ OPFS 不可  → 現行のメモリ経由 + 260MB ガード(5Mbps・6.7分)
└ realtime 経路(iOS 16.4〜18.3) → 現状のまま(既にメモリ安全・実時間かかる)
```

ビットレートは 12→8Mbps(iOS のみ)。サイズ上限は消えるが、
510秒×12Mbps=765MB はカメラロールへのコピーと端末容量に重い。
8Mbps(523MB)が画質と取り回しの均衡点。YouTube の 1080p30 推奨と同じ。

実装項目:
1. exporter: iOS fast 経路で OPFS ターゲットを選ぶ(feature detect)
2. 共有フロー: **finalize 直後に getFile() して保持**し、保存ボタンの
   ハンドラで await を挟まず `navigator.share({files})` を直接呼ぶ
   (share は user gesture 必須。ボタン押下→await→share だと Safari が拒否する)
3. 後始末: 成否どちらでも `removeEntry()`。`storage.estimate()` で容量事前確認
4. encodeAudioPcm: chL/chR の全長確保をやめ 1024フレーム窓の逐次エンコードへ
5. iOS のみデコーダキュー水位を下げる(frames 8→4 等。IOSurface の節約)

### 実機でしか確かめられないこと(優さんの iPhone で)

- a. `share({files:[OPFSのFile]})` がメモリで死なないか ← 唯一の本質的リスク
- b. OPFS の quota(数百MBは通る見込み。estimate をログに出す)
- c. 8:30 のエンコード実時間(HW エンコードで2〜4分想定)
- d. 3本同時デコードのフットプリント(水位下げで保険)

a が実機で死んだ場合のみ、B(720p/4Mbps)を退避先として使う。

## 現在のコード位置

| 何 | どこ |
|---|---|
| メモリ上限 | `tools/switcher/js/exporter.js` `MC.exporter.MEM_HARD_LIMIT` |
| ビットレート | 同 `MC.exporter.videoBitrate()` |
| 書き出し前ガード | `tools/switcher/js/ui.js` `estBytes > MEM_HARD_LIMIT` の分岐 |
| 案内文 | 同 `MC.ui.showLongExportHelp()` |
| 解像度プリセット | `tools/switcher/js/state.js` `MC.PRESETS` |
| 音声(メモリ確保) | `tools/switcher/js/exporter.js` `encodeAudioPcm` |
| 共有シート保存 | 同 `MC.exporter.shareMode()` / `ui.js` の `#saveBtn` |

## 関連

`Switcher書き出し問題_2026-07-20.md`(Mac側の経緯)/ メモリ `ios-safari-file-save`
