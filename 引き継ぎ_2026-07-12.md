# MarchinZ 引き継ぎ 2026-07-12(最終更新版 v3)

## いちばん大事なこと（結論）

**すべて push・デプロイ済み。本番は最新で稼働中。実機確認・UI追改修・Firestore権限バグ修正・UGCツール利用記録まで完了。**

- 本番: https://marchinz.netlify.app （`data-mz-version` は `1.35.0` のまま。バージョン文字列は未更新）
- ローカル HEAD = origin/main = 本番、すべて一致（最新コミット `2c1d7d1`）
- リポジトリ: `マイドライブ/CursorLogs/010_MarchinZ`
- 残タスクは**1つだけ**（section_vis_mll バックフィル。稼働上の問題なし＝急がない）

---

## ▼ 次のセッションにそのまま貼るテキスト

```
010_MarchinZ の続き。v1.35.0系、直近コミット 2c1d7d1 まで本番push・デプロイ済み。
コード側は一区切りついているので、今回は実機での最終確認からスタート。
marchinz.netlify.app で以下を確認して、問題があれば修正して
(修正したらコミット、pushはまとめて最後に1回)。

確認項目:
1. マイページを開閉して半透明が残らないか(要ログイン。前回セッションから持ち越し・未確認)
2. チューナーの実マイク動作(iPhone等の実機で許可ダイアログ→音程判定まで)
3. TOPの「ツール利用」UGC記録が実際に増えるか
   (メトロノーム/チューナーを使う、Privacy/Switcherカードを押す→
    #ugc/tool_use に反映されるか、管理者アカウントで確認)
4. Switcherの選択フェーズ(縦型動画作成/自動スイッチング動画)がiPhone Safariで
   問題なく動くか(自動化ブラウザでは合成clickが通らずタップ検証が甘い箇所)

注意:
- 本番は最新コミット 2c1d7d1。詳細は リポジトリの 引き継ぎ_2026-07-12.md
- MarchinZ Switcher(/tools/switcher/)・MarchinZ Privacy(/tools/privacy/)を正本として育てる方針
  (優さん承認済み)。MarchCut/MzMosaic(CloudeCode_u側)は開発元アーカイブとして凍結済み、更新しない
- pushはまとめて1回(Netlifyクレジット節約)。push前は git show HEAD:<file> で実体検証(Drive巻き戻り対策)
- ルール変更時のみ firebase deploy(pushより先に)。CLIは ~/bin/firebase の単体バイナリでログイン済み
- 検証はスマホ・iOS Safari最優先(375px)、UIアイコンはFAモノクロ統一
- 残懸案: プロフィールへの練習バッジ任意表示(公開範囲設計から)、
  section_vis_mllバックフィル(pc20機など認証情報のある環境で。急がない)
```

---

## ▼ このセッション(2026-07-12 通し)でやったこと

### 1. 前回持ち越しの実機確認 6項目 → 検証完了
1. `/tools/privacy/` 写真1枚でモザイク → **OK**(実顔写真で顔検出→モザイク確認)
2. `/tools/switcher/` 動画2本→同期→縦/スイッチング/ワイプ→MP4書き出し → **OK**(179BPM・12カット・7.27MB出力まで通し確認)
3. チューナーのマイク → `microphone=(self)` ヘッダーの本番配信を確認。getUserMedia 呼び出しも確認(自動化ブラウザに実マイク無く音程判定の実動作のみ未確認)
4. マイページ半透明 → Google認証必須のため私からは代行不可(**要 優さん実機・未確認のまま**)
5. iPhone Safari 左右ブレ → `overscroll-behavior-x: none` の本番配信・適用を確認
6. マップのボトムシート → バルーンタップ→シート表示、開いたまま地図パン可を確認

### 2. TOP の追加UI改修
- 見出しを **PRACTICE TOOLS / CREATOR TOOLS** の2ブロック構成に。表記は TODAY'S MARCHINZ と同じアンバーの英字ラベル(eyebrow)へ統一
- CREATOR TOOLS ブロックに Privacy / Switcher へのカードを追加
- 「登録して練習記録をつける」の外側ネイビー箱を撤去(ボタンのみ残す)

### 3. ツール2種を MarchinZ トンマナに統一 + ヘッダー刷新
- ダークテーマ → 本体の白基調(ネイビー/ブルー/アンバー3色、クリーム地ヘッダー)へ
- 両ツールのヘッダーを **MarchinZ ロゴ先頭**の構成に(ロゴ→区切り線→ツール名。あくまで MarchinZ ページ内であることを明示)
- **Switcher に最初の選択フェーズを新設**: ①縦型動画作成(9:16/2分割縦積み) ②自動スイッチング動画(16:9/スイッチング)。選び直しリンク+モード表示付き

### 4. プロ2名レビュー(UI/UX + エンジニア)の指摘を反映
- select/数値入力をモバイル16px化(iOS Safari のフォーカスズーム跳ね防止)
- 両ツールに OGP・description・favicon・theme-color を追加(SNSシェア対応)
- Switcher 書き出しファイル名 `MarchCut_` → `MarchinZ_Switcher_`
- ファイル名の innerHTML 挿入を `MC.ui.esc()` でエスケープ(自己XSS防止)
- ヘッダーのツール名を h1 化(a11y/SEO、見た目不変)
- 「選び直す」で `MC.preview.pause()`(選択画面の裏で音が鳴り続けるのを防止)
- 保留: eyebrowアンバーのコントラスト濃色化(サイト全体に波及するため未着手)

### 5. Firestore `mll_logs` 公開フィードの permission-denied を根治
- **当初診断(get()10回上限超過)は誤りだった**。真因は「`user_id` の型/サイズ検証(ガード)を OR 全体の外側に置いていたため、Firestore が list クエリ(`where visibility==public`)を『クエリのフィルタだけでは規則を満たせる保証がない』と判定して拒否」していた
- 修正: ガードを **self-read/admin 分岐の内側限定**にし、public 分岐(`visibility==public` かつ `section_vis_mll==public`)はクエリフィルタのみで証明可能な形に。アクセス制御ロジック自体は不変
- 併せて `section_vis_mll` を mll_profiles からの非正規化フィールドとして mll_logs に複製(mll-role.js=作成時、user-profile-page.js=トグル時に全ログ同期)
- 本番で段階検証(優さん承認のもとルールを一時緩めて実証)→ firebase deploy 済み → 直接クエリ20件取得を確認

### 6. UGC に「ツール利用」種別を新設(メトロノーム/チューナー/Privacy/Switcher)
- `mll_admin_ugc_feed` に `tool_use` kind を追加。`tool_id` を4値にホワイトリスト、ログイン不要ツールのためゲスト書き込みも許可(検索系と同型のルール)
- 記録タイミング: メトロノーム=スタート時、チューナー=マイクON時、Privacy/Switcher=TOP・クリエイターのカードから開いた時(通常クリックは書き込み完了を最大0.5秒待ってから遷移。同一ツールはページ滞在中1回だけ記録)
- UGC管理ページ(`#ugc/tool_use`)にタブ追加
- **MarchinZ Promo(CloudeCode_u/MzPromo/)にもUGCタブを新設**: UGC種別12個への直リンク+ツール利用の説明+企画ネタ3本。投稿メーカーの機能紹介セレクトに4ツール追加(計8機能)。Macアプリ版(`~/Applications/MarchinZ Promo.app`)のindex.htmlも同時更新済み
- ルールはfirebase deploy済み・本番E2Eで正規書き込み成功/不正tool_id拒否/ゲスト名偽装拒否を確認済み

### push 履歴(このセッション全体)
`eadcfaf`(TOP2ブロック化+トンマナ) → `d15b96a`(ロゴヘッダー+選択フェーズ+eyebrow) → `b5a3e75`(レビュー修正6件) → `45caff7`(mll_logs修正) → `99f896a`(引き継ぎ書更新) → `2c1d7d1`(UGC tool_use追加) すべて実配信確認済み

---

## ▼ 残タスク

### section_vis_mll バックフィル（急がない・唯一のコード面の宿題）
**現状: 実行しなくても稼働上の問題なし。** 本番の公開ログ(6ユーザー分)を確認したところ、MLLタブを非公開にしているユーザーは**ゼロ**。全員「未設定=公開」で、ルールのフォールバックが実際の意図と一致しているためプライバシー漏れは発生していない。将来ユーザーが非公開に切り替えても、トグル処理(`syncMllLogsSectionVis`)が本人の全ログへ自動反映する。バックフィルは「そのバッチ更新が万一失敗した場合の保険」。

**このMacでは実行不可の理由**:
- `scripts/.local-env` のサービスアカウントJSONパスが `/Users/pc20/Downloads/...`＝**別マシン(pc20)**のもの。yuokocchi機には認証情報が無い
- このMacに Node.js が無い(Homebrew無し方針)

**実行する場合(pc20 機など認証情報がある環境で)**:
```bash
cd scripts && npm install        # 未インストールなら
node backfill-mll-section-vis.mjs --dry-run
node backfill-mll-section-vis.mjs --dry-run --user-id=対象UID  # 本人だけ試すとき
node backfill-mll-section-vis.mjs                              # 本番実行
```
Firestoreルールは既にデプロイ済みなので、バックフィルは単独で実行してよい。

### 実機でしか確認できていない項目
1. **マイページの半透明**(要ログイン) — Google認証が必要で私からは代行不可
2. **チューナーの実マイク動作** — 自動化ブラウザに実マイクが無く、許可ダイアログ〜音程判定は未確認
3. **UGCフィードのテスト記録** — E2E検証でゲスト名義の「メトロノーム/チューナーを使いました」が2件、本番UGCフィードに残っている。既読にすれば消える

---

## 作業の注意（このリポジトリ特有・変わらず）

- **Drive同期の巻き戻し罠**: commit直前にGoogle Driveがファイルを旧版へ戻すことがある。**push前に必ず `git show HEAD:<file>` でコミット内の実体を検証**する。
- **pushはまとめて1回**（Netlifyがクレジット制。過去にクレジット上限到達→サイト一時停止の実績あり。優さんがPersonalプラン$9にアップグレード済み）。
- **ルール変更時のみ firebase deploy**（`firebase use marchinz-app` → `firebase deploy --only firestore:rules`。CLIは `~/bin/firebase` の単体バイナリでログイン済み）。
- 検証は **スマホ・iOS Safari 最優先（375px幅）**。UIアイコンはFont Awesomeモノクロ統一（カラー絵文字禁止）。ツールページはFA非導入のため絵文字/インラインSVGを使用。
- ローカル確認は Drive配下を直接配信できない（TCC）ため、scratchpadへrsync同期して python http.server で配信する。JSを更新したらブラウザキャッシュ回避のためポートを変えるのが確実。
- `tools/privacy/vendor/` に約22MBのWASMファイルがある(git管理下)。
- 自動化ブラウザ(Claude_Browser)は合成clickイベントが実際のUI操作と食い違うことがある(座標クリックが素通りする等)。JS経由の直接呼び出し(`el.click()`やAPI直叩き)で機能自体は検証できるが、「実際にタップして反応するか」は最終的に実機確認が要る。

---

## MarchCut / MarchinZ Switcher の関係（重要・合意済み）

- 中身は完全に同一コード（コピー起源で、機能の重複ではない）
- **今後は MarchinZ Switcher 側(`tools/switcher/`)を正本として育てる方針**
- MarchCut(CloudeCode_u側)は開発元アーカイブとして凍結、今後の機能追加・バグ修正はSwitcher側に対して行う
- 同様に MzMosaic → MarchinZ Privacy も同じ関係(MzMosaicは凍結)

---

## 積み残しの懸案(コード外・任意)

- プロフィールへの練習バッジ任意表示(公開範囲設計から) ※前々回からの持ち越し
- `data-mz-version` を 1.35.0 のまま据え置き中。UI刷新+バグ修正+UGC拡張がまとまったので区切りとして 1.35.x へ上げてもよい(未実施)
- eyebrowアンバー(#d97706)のコントラストが小文字で AA 未達。直すならサイト共通の `--amber`(#b45309)へ一括変更
