# MarchinZ 引き継ぎ 2026-07-12(最終更新版 v4)

## いちばん大事なこと(結論)

**すべて push・デプロイ済み。本番は最新で稼働中。** 今セッションで note新着機能・TOPレイアウト調整・毎日自動更新(YouTube+note)・重大バグ(FOUC)修正まで完了し、実機でも確認済み。

- 本番: https://marchinz.netlify.app
- **GitHub main = ローカルHEAD = `86d2b90`(完全一致・全push済み)**
- リポジトリ: `マイドライブ/CursorLogs/010_MarchinZ`(GitHub: `YuOKOVHI/marchinz`)
- 次セッションの主目的 → **YouTube と note の「表示のされ方」を実際のデータ更新後に見て、自然かどうか確認する**(下の「次にやること」参照)

---

## ▼ 次のセッションにそのまま貼るテキスト

```
010_MarchinZ の続き。本番 main = 86d2b90 まで push・デプロイ済みで最新稼働中。
今回はまず、YouTube新着・新着大会動画・新着note の「表示のされ方」を実データで見て、
自然かどうか(件数・並び・レイアウト・スマホ2×2の見た目)を確認したい。

前提:
- YouTube と note は毎朝 JST 5:00 に GitHub Actions が自動更新 → bot が main へ push → Netlify 自動デプロイ。
  なので朝以降は本番の新着データが入れ替わっているはず。
- 表示箇所は3つ: ①TOPの TODAY'S 内「LATEST NOTE」1枠 ②TOPの「新着note」バンド(4件) ③メディアページのnoteカード。
  YouTubeは ①TODAY'Sの「LATEST VIDEO」 ②「YouTube 新着」バンド ③「新着の大会動画」バンド。

注意:
- **push 前に必ず `git pull --rebase origin main`**(bot が毎朝データファイルを push するため。data系のみなので通常衝突しない)。
- push は手動でこまめに(自動保存/自動pushは使わない方針)。
- Drive巻き戻り罠が今も活発。編集は scratchpad の git-blob コピーで行い cp→即commit、確認は `git show HEAD:<file>`/`git ls-remote` を使う(作業ツリーの grep/rev-parse はキャッシュが古いことがある)。
- ルール/インデックス変更時のみ firebase deploy(pushより先)。CLI は ~/bin/firebase でログイン済み。
- 検証はスマホ・iOS Safari最優先(375px)、UIアイコンはFAモノクロ統一。
```

---

## ▼ 今セッション(2026-07-12 通し)でやったこと

### 1. 前回持ち越しの実機確認 → 完了
- マイページ半透明・チューナー実マイク・Switcher選択フェーズ・UGC tool_use記録 → 優さん実機で確認済み。

### 2. 公開ログフィードの permission-denied 根治(コミット 64de9e0)
- TOPの公開ログ(calendar-events / highlights / mll-role)が全て permission-denied で失敗していた。
- 真因: `queryMllLogsForFeed`(mll-role.js)のクエリが `visibility==public` だけで、ルールが要求する `section_vis_mll==public` を欠いていた。
- 修正: クエリに `section_vis_mll` フィルタ追加 + 複合インデックス追加(firestore.indexes.json)→ **firebase deploy 済み**。
- ※残: `section_vis_mll` バックフィル(未設定の旧ログは公開feedに出ない。pc20機で。急がない)。

### 3. note 新着フィード機能(新規)
- **データ層**: `export_notes_feed.py`(標準ライブラリのみ)が `marching_story`(ちゃみ｜マーチング♪)と `marching_matsuri` の公式RSSを取得→ `marchinz-notes.inline.js`(`window.__MARCHINZ_NOTES`)生成。全滅時は既存温存。
- **表示層(TOP・優さん承認済みの形)**:
  - TODAY'S MARCHINZ 内に「LATEST NOTE」1枠(最新1件、`renderDashStatic`)。
  - TOPに「新着note」バンド(最新4件、`renderTopNotes`、新着の大会動画の下)。※noteはTOPに2箇所出す方針。
  - メディアページ: noteセクションに marching_story(ちゃみ)カードを追加。**ちゃみのサムネイルも設定済み**(note公式ページの og:image)。
  - ※メディアの「新着記事(自動更新)グリッド」は一度作ったが**優さん指示で削除済み**(renderMediaNotes ごと撤去)。メディアは案内カードのみ。

### 4. TOPレイアウト調整
- TODAY'S 内ツールブロック順を **today → CREATOR → PRACTICE**(CREATOR先)に。
- 近日開催予定: `EVENTS_LIMIT` 6→8(PC最大8件)。スマホは CSS で3件表示(`#mz-top-events-grid > *:nth-child(n+4){display:none}`)。
- 新着の大会動画・新着note: スマホ縦2×横2(`@media max-width:640px`)。PCは4列。**YouTube新着(mz-top-yt-grid)はスコープ外=スマホ1列のまま**(指示が2つだったため。揃えたければ拡張可)。
- CSS変更は marchinz-brushup.css、`?v=` は brushup.css/top-highlights.js とも 1.35.3。

### 5. YouTube + note の毎日自動更新(コミット 9bd5092)★今回の重要調査
- **判明**: YouTubeの毎日自動更新は既に GitHub Actions で数ヶ月稼働していた(`.github/workflows/youtube-api-daily.yml`。origin/main に毎日 bot コミットあり)。
- **やったこと**: そのワークフローに note ステップを2行追加して相乗り。名前を「YouTube + note daily refresh」に。**実行を毎朝 JST 5:00 に変更**(cron 20:00 UTC)。差分があれば bot が main へ push → Netlify 自動デプロイ。
- **サイトコードは bot が触らない**ので「作業中コードが勝手に本番へ出る」ことは構造上ない(優さんの要望どおり)。
- **コスト**: 有料は Netlify($9 Personal)だけ。YouTube API は無料枠1万u/日に対し1更新~60〜130u(search不使用)。note RSS はキー不要。GitHub Actions は無料枠内。→ 詳細は下「コスト早見表」。

### 6. 重大バグ: 未ログインで管理者ナビ(管理/UGC)が一瞬表示される FOUC 修正(コミット 86d2b90)
- 真因: `.site-nav-link{display:inline-block}`(作者CSS)が UA の `[hidden]{display:none}` を打ち消し、JSが inline display:none を付けるまで一瞬表示されていた。既存の同種対策(`.site-brand-actions[hidden]`等)が data-admin-only 系だけ抜けていた。
- 修正: `styles.css` に `[data-admin-only][hidden]{display:none!important}` を1行追加(styles.css `?v=` 1.33.3→1.35.4)。対象4要素(PC/モバイルドロワー×管理/UGC)を初回描画から確実に隠す。admin確定時はJSが hidden 属性を外すので表示される。**実機で消失を確認済み**。

### 7. 自動保存スクリプト(作ったが不採用・手動push運用に決定)
- `~/bin/marchinz_autosave.sh` は完成・テスト済み(変更あれば `[auto]` commit + Drive外bundle + 変更履歴.txt。pushはしない)。
- **優さんの決定: 自動保存/自動化はオフ。Drive巻き戻り対策は「手動でこまめに commit / こまめに push」で運用する。** launchd は登録していない(スクリプトは残置。使いたくなったら `launchctl load` で有効化可)。

### push 履歴(このセッションで main へ出た11コミット)
`b5ddfcc`→`8d2cab5`→`cdc39b0`→`64de9e0`→`c0ba715`→`b970f89`→`6d47901`→`42c041e`→`9bd5092`→`7c84868`→`86d2b90`。全て実配信確認済み。

---

## ▼ 次にやること(優さんの希望)

### YouTube と note の「表示のされ方」が自然か確認する(最優先)
朝5時の自動更新後に、本番で以下を実データで見て自然かチェックする:

```
TOP:
  TODAY'S MARCHINZ … LATEST VIDEO(最新動画1枠)/ LATEST NOTE(最新note1枠)
  「YouTube 新着」バンド … 4件
  「新着の大会動画」バンド … PC4件 / スマホ2×2
  「新着note」バンド … 最新4件 / スマホ2×2
メディアページ(#webmagazine):
  note セクション … 検索カード / マーチング祭 / ちゃみ(サムネ付き)
```

見るポイント: 件数・新しい順に並んでいるか・サムネが出るか・タイトル切れ・スマホ2×2の窮屈感・YouTube新着だけスマホ1列で不揃いに見えないか、等。違和感があれば件数やレイアウトを調整する。

### 残タスク(急がない)
- `section_vis_mll` バックフィル(pc20機。公開feedに旧ログを出すため)。
- プロフィールへの練習バッジ任意表示(公開範囲設計から)。
- YouTube新着のスマホ表示を2×2に揃えるか(今は1列。要判断)。

---

## ▼ 作業の注意(このリポジトリ特有・変わらず+今回の学び)

- **Drive巻き戻り罠が今も活発**。commit直前/読み取り中に Google Drive がファイル(や `.git` 参照)を旧版へ戻す。今回は `git rev-parse origin/main` が古い値(8464450)を返し、`git ls-remote origin`(GitHub直問い合わせ)で真の値(86d2b90)を確認できた。**push状況の判定は `git ls-remote` を正とする**。編集は scratchpad の git-blob コピーで行い、cp→即commit→`git show HEAD:<file>` 検証。要所で `git bundle` を Drive外へ。
- 対策の運用方針: **手動でこまめに commit / こまめに push**(自動保存は不採用)。未コミットの時間を短くして巻き戻りの実害を減らす。
- **bot が毎朝 main へ push する**ので、優さんの push 前は必ず `git pull --rebase origin main`(data系のみ・通常衝突しない)。
- **firebase deploy はルール/インデックス変更時のみ**、pushより先。CLI は `~/bin/firebase`(marchinz-app にログイン済み)。
- 検証は**スマホ・iOS Safari 最優先(375px)**。UIアイコンはFAモノクロ統一。ローカル確認は Drive を直接配信できない(TCC)ため `git archive HEAD | tar -x -C <scratchpad>` して `python3 -m http.server` で配信。
- 自動化ブラウザはこの SPA でスクロールがリセットされる癖がある。表示検証は DOM/computed-style 計測(`getComputedStyle().display` 等)で確実に取れる。

---

## ▼ コスト早見表(今回調査)

課金されるのは **Netlify だけ**($9 Personal、加入済み)。残りは全て無料枠。

| サービス | 用途 | 課金 | 消費 |
|---|---|---|---|
| **Netlify** | ホスティング/ビルド/デプロイ | **$9/月** | push・bot毎日pushごとにビルド。**ここだけ有料** |
| YouTube Data API v3 | 動画メタ取得 | 無料(1万u/日) | 1更新~60〜130u(search不使用) |
| GitHub Actions | 毎朝5時の自動更新 | 無料枠内 | 数分/日 |
| note RSS | note新着取得 | 無料(キー不要) | 毎朝 |
| Firestore | ログ/イベント/UGC/プロフィール | 無料枠(読5万/書2万 per日) | 利用者数次第(将来ここが最初に伸びうる) |
| Firebase Auth/Storage/AppCheck | ログイン/画像/Bot対策 | 無料枠 | 少量 |
| 地図(OSM/CARTO)/CDN | 地図・SDK配信 | 無料 | — |

**手動でこまめに push する運用**にしたので、Netlifyのビルド回数は push 頻度に比例する点だけ意識(bot の毎朝1回+手動分)。将来コストが動くとしたら「Netlifyビルド頻度」と「Firestore読み取り数」の2つ。
