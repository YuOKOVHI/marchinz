# 引き継ぎ書 2026-07-31

前の引き継ぎ書（`引き継ぎ書_2026-07-26.md`）の内容は**この1枚に畳んである**。
そちらを開かなくても続きから着手できる。

---

## 状態（最重要）

- 本番: **v1.53.0**（commit `e87f9c4`）。**push済み・作業ツリークリーン**
- 本番URL: https://marchinz.netlify.app （※ `marchinz.com` は404）
- origin に bot の日次コミット `642cdca`（YouTubeリスト更新）が1つ載っている。
  次に作業するとき `git pull --rebase origin main` で取り込むこと
- Firestore ルール: 変更なし（前回のまま本番反映済み）
- **壊れているものは無い。ただし iPhone実機で書き出しが完走しない**（下記A）

QAランナー **47件**。`tools/qa/runner.html` をブラウザで開くだけ。Node/npm不要。

---

## いま最優先で確かめてほしいこと（優さんの実機1回で決まる）

### A. 書き出しが途中で止まる ─ 原因の切り分け

**実機での症状**: iPhone Safari、8分12秒（14772コマ）を 720x1280 で書き出し、
**62%（9120コマ＝5分04秒ぶん）で止まる**。エラー画面は出ず、タブごと落ちて自動再読込。
序盤の即死ではなく「5分は書けてから死ぬ」。

**やってほしい操作**（これだけ）

1. 書き出しの画面で `▸ 途中で止まってしまうとき` を開く
2. `☑ ゆっくり書き出す（原因さがし用）` にチェック
3. 画面上部に「**原因さがしモード（ゆっくり書き出します）**」の帯が出るのを確認
4. いつもどおり書き出す（**時間はかかる**）

※ URL に `?noskip` を付けても同じ（`https://marchinz.netlify.app/tools/switcher/?noskip`）

**結果の読み方**

| 結果 | 意味 | 次の手 |
|---|---|---|
| 完走した | 犯人は「カメラ切替のたびのデコーダ巻き戻し」（H1） | 分割書き出しが根治策になる |
| また落ちた | H1は無罪 | 帯に出る数字（下記）で次を絞る |

落ちたら再読込すると、こういう帯が出る。**スクショを取ること**。

> 書き出しが 62% で止まりました。同期もカット割も残っています。同じ動画を・前と同じ順番で
> 選び直せば、書き出しからやり直せます。
> `9120/14772コマ・720x1280・8Mbps・カメラ3本・端末内の保存領域(OPFS)・312秒で約304MBまで・音声直列・再シーク34回・空き12400MB`

判定表（シニアエンジニアとの協議結果）

| 見るところ | 結果 | 確定/棄却 |
|---|---|---|
| 再シーク回数 | 数十回以上 | H1を強く支持 |
| 同上 | 数回以下 | H1を棄却 |
| ゆっくりモードで完走 | する | **H1がほぼ確定** |
| 同上 | 同じ304秒付近で停止 | H1棄却 → H2/H5へ |
| 空き容量 | 数百MB未満 | H4（容量）を再考 |
| 同上 | 潤沢 | H4を完全除外 |
| 解像度を半分にして再実行 | 停止コマ数が比例して伸びる | H2（GPUメモリ）を支持 |
| 充電＋涼しい環境で変わるか | 大きく変わる | H5（発熱）を支持 |

### B. 仮説の一覧（確信度順・すべてコードで裏取り済み）

**大前提（これが分かったのが今回の収穫）**:
**62%の時点で音声コーデックは1本も動いていない。** iOSは音声直列
（`exporter.js:1054` `audioParallel && !MC.isIOS`）なので `runAudio()` は映像93%の後。
つまり前回の「iOS音声直列化」は的外れではなく、**落ちている区間の対象外**だった。
落ちた瞬間に生きているのは VideoDecoder×3 + VideoEncoder + Canvas2D + WebGL +
mp4-muxer + OPFS Worker だけ。

| | 仮説 | 確信度 | 根拠 |
|---|---|---|---|
| H1 | `skipTo()` の flush+reseek の蓄積 | 60% | カットが3〜8秒で回るので5分で数十回。時間比例で階段状に積む形が症状と最も整合（`exporter.js:36-42,108-125`） |
| H2 | `new VideoFrame(canvas)` の IOSurface 解放遅延 | 45% | iOS固有。計測APIが無い。解像度を下げて延命するならこれ |
| H5 | サーマル → OS watchdog | 30% | コードでは切り分け不可。対照実験のみ |
| H3 | mp4-muxer の非fragmentedサンプルテーブル | 存在は確定 | `vendor/mp4-muxer.min.js:7` で全サンプルmetaをfinalizeまで保持。ただし9120個で約1MB → 単体では説明不可 |
| H4 | OPFS容量 | 15% | 容量超過なら `_writeFatal` の日本語エラーで止まるはずで「タブごと落ちる」と矛盾 |

**構造的に否定済み**（調べて却下したので再調査不要）:
WebGLコンテキスト増殖（寸法キーで頭打ち）/ VideoPipe の close漏れ（全経路で対）/
書き込み背圧の滞留（16MB毎・最大20回）

**効かないと分かっていること**: ビットレート低減はフレーム数もシーク回数も変えないので
H1/H5に効かない。解像度低減は H2 にのみ延命効果（根治ではない）。

### C. 分割書き出し（根治策の候補）

8分を2〜3分ずつのパートに分け、1本ずつ保存する。
- 実装コスト **中**。`exportMP4` は既に `MC.trimRange()` を尊重するので、
  区間を区切って複数回呼ぶだけ。muxer/パイプの再設計は不要
- **H1が真因なら根治**（パートごとにデコーダを作り直すため蓄積がリセットされる）
- H2が真因なら効果は不確実（合計フレーム数は変わらない）
- どちらにせよ「1本失敗しても他は残る」保険にはなる
- リスク: 2〜3本のファイルになる（つなぐ手間）/ 起動コストがパート数だけ増える
- カラーマッチ統計は `colorStats` キャッシュを使うのでパート跨ぎでも色は連続する

**A の判定を待ってから着手判断するのが正しい順序。**

---

## このセッションでやったこと（v1.47.0 → v1.53.0、22コミット）

### 1画面1操作への作り替え（優さん指示）

**きっかけ**: 「今、UIUXがすごく悪い。1画面で1操作を徹底。文言を徹底的に見直し。
これでダメなら codex 移行を検討」

実測で分かった元の姿:
- 第1画面に**押せるものが40個**、そのうち24個がツールと無関係なサイトナビ
- 文言36個・396文字。「3本まで」と「上限なし」が**同じ画面に同時に出ていた**
- **現在地の画面が存在しない工程が2つあった**（`#syncSec`/`#layoutSec` が
  `<div id="proPane" hidden>` の中にあり、既定のおまかせでは1枚も表示されない）

やったこと:
- `#proPane` ラッパを廃止。工程の出し分けは `applySteps` に一本化し、
  おまかせ/こだわりは `body.tab-easy` / `.tab-pro` の別軸へ
- `applySteps` を「畳む/ロックする」から「**いまの工程だけ出す**」へ
- `body.mz-focus` で作業中はサイト共通の外枠（ブランド帯・下部タブ・フッター）を下げる
- 素材0本のうちは `.stage` / `#easyPane` を出さない

結果（375x700実測）:

```
工程        出ている枠      押せるもの   高さ
動画        dropSec             9       848px
傾き        tiltSec             8       945px
同期        easyPane            7       787px
音声        audioSec           16      1069px
設定        placeSec           13      1055px
書出        exportSec          19      1388px
```

**すべての工程で枠が1つだけ**になり、現在地の画面が無い工程はゼロ。

### 傾き修正フェーズの新設（優さん指示・自動検出は全廃）

工程が5→6段に。`動画 → まっすぐにする → 同期 → 音声 → 仕上げ設定 → 書き出し`

- 1本ずつのカルーセル。`−/＋`（0.5度）とスライダー（0.1度・±3度）
- **三分割グリッド＋中央の黄色い水平線**を描く（これが無いと判断できないという指摘）
- 素材の35%地点から表示、`べつの場面を見る`で5か所を送れる（0秒目は三脚を触っている）
- 傾き確認中だけ**素材そのものの向き**でプレビュー（横動画を縦枠に切ると水平の手がかりが外へ出る）
- サムネ＋ファイル名＋「まわりが約4.4%切れます」
- `horizon.js` は読み込みごと撤去（ファイルは残置）。`c.tiltOk` を保存・復元

### 文言の全面見直し

| 前 | 後 |
|---|---|
| 音量-14dB・歪みあり⚠・未解析 | 音が大きい／音がわれています⚠／まだ聞いていません |
| 選択肢が IMG_0001.MOV（プレビューは「カメラ1」） | 「カメラ1」に統一、ファイル名は薄字 |
| ライトモード(720p)／リッチモード(1080p) | はやい／きれい |
| ⚠この端末は実時間録画になります | 動画とおなじ長さの時間がかかります（3分なら約3分） |
| 演奏区間を検出（サリュート考慮） | 演奏の範囲をさがす |
| 解析前／カメラマン付き／定点固定 | まだ見ていません／手でもって撮影／置いて撮影 |
| 「**下の**「分析を開始」を押してください」 | 位置を指す語を全廃 |
| 912.4秒で書き出しました | 15分12秒で書き出しました |

### レビューで見つかった事実の誤り（すべてコードで裏取り）

- **「音を整えています…」が嘘**だった。`gain|normalize|compress|loudness` は
  `exporter.js`・`audio.js` のどちらにも0件。音は素通し（それが正しい設計）→
  「音を書き出しています…」へ
- **書き出しの映像だけ elst（エディットリスト）を適用していなかった**。
  音声（`exporter.js:235`）と解析（`visual.js:507`）は補正済みなのに映像だけ生の cts。
  Bフレームを持つH.264では1〜3フレーム音とずれる
- **手ブレ判定にだけ「被写体の動き」ガードが無かった**。`panRatio`(:722)と
  `operated`(:439)は `act < TH_CAM_RESID` で除外しているのに `dqReason` だけ素の
  `shakeP75`。カンパニーフロントやドラムラインが横切ると**三脚カメラが「手ブレ」で失格**
  していた（＝マーチングで一番見せたい瞬間が捨てられていた）→ `shakeCamP75` を追加
- **色シェーダが mediump**（iOSでは fp16）。Lab（L=0..100/ab=±127）を経由するので
  空・体育館の壁に縞。**Macでは fp32 に昇格されるので手元では再現しない** → highp へ
- **2Dの縮小品質が既定の "low"**。GL側はミップマップを作っているのに最後の
  1920→1080でバイリニア単独になり台無しに → `imageSmoothingQuality = "high"`
- **顔の注意が画面に一言も無く、Privacy への導線が消えていた**。
  `index.html:398` の `<footer>` 内リンクは `sitechrome.js:240` の
  `old.replaceWith(foot)` で丸ごと差し替えられ、差し替え後に privacy は0件
  → 完成画面に注意とリンクを追加
- `?test` で完成動画をサーバへ PUT する経路が本番コードに残っていた → localhost 限定に

### 書き出しクラッシュ対策（v1.50.0〜）

- **書き出し中もプレビューのrAFが60fpsで合成し続けていた**。
  `cancelAnimationFrame` が全コードに0件。書き出しの全画面に覆われて誰にも見えないのに、
  exporter と別の canvas+GL でGPUとメモリを二重に使っていた → `exporter.running` ガード
- 顔検出Worker+ONNX RuntimeのWASMを書き出し開始時に解放（`_releaseAnalysisWorkers`）
- OPFS書き込みの滞留を数えて背圧＋中止を見る
- iOSでは音声を直列に戻した（→ 後に「対象外の区間だった」と判明）

---

## 罠と教訓（次に同じ轍を踏まないために）

### ★ 一括置換スクリプトは途中で失敗すると後続が丸ごと未適用になる

**2026-07-30 に実際にやらかした。** python の replace 連打で7件直すつもりが、
4件目の `assert` で失敗してスクリプトが中断。その後ろの3件が未適用のまま
「全部直した」と報告し、コミットメッセージにも書いた。優さんはそれを信じて
実機で確認していた。**P0として報告した3件が入っていなかった。**

未適用だったもの: ①分析後に書き出し画面へ行けない ②分析中に進捗が消える
③「最初からやり直す」の確認ダイアログ

**対策（必ず守る）**
- スクリプト末尾で `assert n == 期待件数`。出力の「適用 N 件」を目視で突き合わせる
- 報告・コミットの前に、主張ごとに `git show HEAD:<file> | grep -c <特徴文字列>`
  で**実体**を確認する（作業ツリーではなく blob）
- UI/挙動の主張は、コードの存在確認だけでなく**実際に動かして測る**
- 不変条件は QA へ入れて機械に見張らせる。**そのテストが修正前の状態で
  本当に FAIL するかを必ず確認する**（FAILしないテストは見張りとして無意味）

### 「直した」と言う前に、自分の修正が壊していないか測る

このセッションで、自分の修正が原因の回帰を **5件**作って自分で見つけた。

1. デッキ化で `#dropSec` が消え、**動画2台目を追加も削除もできなくなった**
2. デッキ化で `canSelect` が可視判定のため、**ジャーニーの戻る導線が全滅**（6枠中5枠disabled）
3. `data-mzj-phase` 依存のCSSとデッキが二重に効き、**同期の画面が空**になった
4. 傾きの余白計算で `padding-top` が要素の `top` を動かさないことを見落とし、
   **素材を送るたびに111pxずつ下がった**
5. `saveState` が clips 空のときに保存を上書きし、**再読込直後に前回の続きを自分で消していた**
   （「再開できない」の直接原因。案内した1タップ前に壊していた）

### `requestAnimationFrame` は非表示タブで発火しない

傾きフェーズの余白計算を rAF に入れたら、非表示タブで当たらなかった。
実機で別アプリに切り替えて戻ると同じ事故になる。`getBoundingClientRect` が
レイアウトを確定させるので**同期に測ってよい**。

### QAランナーの土台にも欠陥があった

- `compReset` がモードを選んでおらず `#workspace` が hidden のまま。
  **DOMの見え方を見るテストが全部素通り**していた
- ダミークリップに `syncMethod` が無く、実物（`media.js:57` が必ず入れる）と条件が違った
- `renderAll` を呼ばないと `body.mz-has-clips` が付かず、実物と違う条件になる

### 状態の保存と再開（優さんに聞かれて調べた）

再開には**2つの条件**があり、どちらも画面に書かれていなかった（→ 書いた）。

1. `clipKey = ファイル名|バイト数|更新日時` の3点照合（`state.js:73`）。
   LINEで送り直したもの・別端末へコピーしたもの・書き出し直したものは**別の動画**
2. **`slots`（どのカメラがどの枠か）は保存されていない**（`state.js:126` の保存項目に無い）。
   読み込み順で先頭の空き枠へ入る（`media.js:181`）ので、**選ぶ順番も前と同じ**にする必要がある

案内は3か所に出した（素材の画面／書き出しの画面／中断の帯）。

### 既存の罠（前回から継続）

- **Drive同期がコミットを巻き戻す。** push前に `git show HEAD:<file>` で実体検証
- 映像エンコーダは同じ入力でも実行ごとに揺れる。「2回エンコードして比べる」テストは書かない
- 非表示タブの `setTimeout` は1秒に絞られる。計測ループは `MC.yield`（MessageChannel）で
- `getProc` は寸法ごとにコンテキストを作る。処理解像度は `[1, 1/2, 1/4]` の3段に丸める
- `isContextLost()` を見ないと `preserveDrawingBuffer:true` で直前のコマを返し続ける
- プレビューサーバは Drive 配下を配信できない（TCC）→ scratchpad へ cp して配信

---

## 6名レビューの結果（2026-07-29）

マーチング初級/上級・広報初級/プロ・映像初級/プロ の6名。判定:

| | 判定 | 理由 |
|---|---|---|
| 中1プレイヤー（歴4か月） | 使えるが一人では無理 | 「書き出し」の意味が分からない／傾きを自分で判断できない |
| マーチング歴14年・DCI経験 | **Yes**（同期+分割表示として） | 自動カット割は誤判定が直るまで使えない |
| 顧問2年目 | **No** | 顔の注意が無い／Privacyに渡しても60秒しか返らない |
| SNSマーケ12年 | **No** | 8分48秒は Reels/Shorts に投稿できない |
| 趣味1年半 | 練習用は即使う／記録用は No | 13分上限・30fps固定・4K非対応 |
| プロ18年・DIT | 納品はNo／当日共有はYes | **P0は無し**。①elst ②同期の妥当性検証 ⑥mediump ⑦縮小品質 を直せば「Yes、オフライン確認用」に変わる |

**3名が「現状ではNo」で、理由が全部ツールの外側（出口の設計）にある。**

---

## 優さん判断保留の6件（触っていない）

指示があるまで手を付けないこと。

1. **既定画質 720 vs 1080** — SNSに出すなら1080が要る、という指摘（コードのコメントは逆の判断）
2. **冒頭8秒の無音** — `preRoll` 既定8秒 + `salute` が「5秒以上の静けさ」を探すので
   設計上ほぼ確実に冒頭が無音になる
3. **ハイライト切り出し** — 8分48秒のままでは Reels/Shorts に出せない
4. **Privacy の60秒上限**（`limits.js:73`）— Switcher→Privacy のワークフローが成立していない
5. **MarchinZカラー既定ON** — 「記録」用途では味付けが邪魔という指摘
6. **30fps固定・4K非対応**（`exporter.js:853` に `const fps = 30;` ベタ書き）

---

## 積み残し（着手していない）

### マーチング指導者の指摘（自動カット割の判定ロジック）

裏取り済みだが未修正。マーチング映像で一番価値のある瞬間を捨てている疑い。

- **ブレ判定が1px量子化**（128px幅・±8px整数格子・dt=0.12 → 1px=0.065）。
  静止から失格まで実質2px。手持ちカメラが構造的に全滅する
- **引きが「人が写っていない」で失格**する。顔検出は640x640入力で全景の顔は約5px→検出不能、
  残る条件が `act < 0.12` だが**隊形が決まって静止した瞬間**は act がほぼ0
- **拍グリッドが一定テンポ・整数ラグ固定**。120BPMで480秒に約3.8秒ドリフト。
  ショーは1曲1テンポではない（`sections.js` は局所BPMを持っているのに使っていない）
- **解析サンプル間隔（最大5秒）がショット長より長い**。半分以上の区間はサンプル0点
- `cutLevel` の既定は2だが、現場感覚ではレベル1が「普通のマーチング映像」
- `role="pit"` は事実上の封印スイッチ（`cls.pit > 0.45` が3値の積で届かない）
- `director._salute` のキャッシュが更新されない（`director.js:47-49`）

### UI/UXの積み残し

- おまかせ/こだわりタブの廃止（構造衝突は解消済みなので緊急性は下がった）
- 「書き出す」→「動画をつくる」の全面言い換え（影響範囲が広い）
- メインSPA側（モーダル14個に focus trap なし／親切な日本語エラーが一度も出ない／
  `sw.js` が0バイトなのに register／netlify function に認証・レート制限なし 等）

---

## 環境・道具

- **このMacに node / npm は無い**（Homebrew無し）。firebase CLI は単体バイナリなので動く
- テストの全部:
  - `tools/qa/runner.html` … ブラウザで開くだけ。**47件**。本番でも開ける
  - `python3 scripts/rules_test.py` … firestore.rules の挙動テスト（本番に触れない）
  - `python3 scripts/count_visibility.py` … Firestore REST で全件走査（read-only）
- プレビューサーバは Drive 配下を配信できない（TCC）→ scratchpad へ cp して配信。
  `http.server` の `-m` 起動は cwd が Drive だと落ちるので chdir してから起動する

---

## 運用ルール（厳守）

- **push と本番デプロイは実行前に毎回かならず確認を取る。1度の許可は1度の実行にしか使えない**
- コミットは都度作ってよい。**止めるのは push だけ**
- 版番: **push命令 → マイナー++・パッチ0** / **ローカルの積み上げ → パッチ++**。
  書き換えるのは ルート `index.html` の `data-mz-version` と
  `tools/switcher/index.html` の `data-mz-app-v` + 全 `?v=`（**計21箇所**）
- `firestore.rules` を変更したら `firebase deploy --only firestore:rules` を忘れない
- 新ツール構築・既存ツール改修では **tool-builder スキル**を適用し、完成したら
  `BentoBox/tools.json` へ登録する
- Drive同期対策で、push前に必ず `git show HEAD:<file>` で実体検証
