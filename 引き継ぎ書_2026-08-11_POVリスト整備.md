# 引き継ぎ書 2026-08-11 — POVリスト整備と日次自動更新

**本番 v2.22.0 稼働中。ローカルは v2.22.2（未push 2コミット・別セッションのぶん）。**

このセッションでやったことは大きく2つ。
**① POV動画の取りこぼしを直した ② 毎日ひとりでに保たれる仕組みを作った。**

---

## いちばん大事なこと

### POV動画は 30件 → 982件 になった

発端は優さんの指摘。`0_eeiZoTLYY`（Bluecoats 2026 Victory Run / Lead Mellophone Cam）が
リストに無い、というものだった。調べたら **収録作業の11時間半前には公開されていた**ので、
「新着だったから」では説明できない取りこぼしだった。

**真因**: POVだけ取り込みスクリプトが無く、母集団が「YouTube全体」のまま
手作業の検索に頼っていた。他の3ソースは特定チャンネルを全件なめるので漏れない。

**さらに悪いことに、投稿者のほとんどが個人チャンネル**だった。
同じ Bluecoats 2026 Victory Run のヘッドカムが
**Curzy / Noah Arst / jack zirkelbach という3つの別々の個人チャンネル**から
上がっていて、1本しか拾えていなかった。
つまり「既知チャンネルを列挙する」方式では原理的に届かない。

### 毎日06:00 JSTに自動で回るようになった

`.github/workflows/videos-daily-update.yml`（**macOSランナー**）

| 対象 | 反映 |
|---|---|
| **海外**（FloMarching） | **全自動** |
| **POV（確実な線）** | **自動** |
| **POV（それ以外）** | Issue を立てて人が決める |
| マーチング等（マーチング祭・DCJ）・MIX3 | **手動のまま。触らない** |

**なぜ macOSランナーなのか**: 海外の採用条件は「10分以上 かつ タイトル/サムネイルに
FULL SHOW」。採用15件の内訳を実測すると **10件(67%)はサムネイルOCRでしか判定できず**、
OCRは macOS Vision を使う。Linuxで走らせるとルールが黙って弱くなり、
**「更新なし」に見えてしまう**。

**クレジットは減らない**: コミットするのは CSV と派生データだけで、
`netlify.toml` の `ignore` に入れてあるのでビルドは走らない。
使い捨てクローンで両方向を検算済み（CSV+派生だけ→スキップ / index.html含む→走る）。

---

## ★ 踏んだ罠（同じ轍を踏まないために）

### 1. 団体名が他分野と衝突する（18件の混入）

団体名の部分一致だけで自動採用したら、**同じ語を持つ無関係な動画**が混ざった。

```
Pacific Crest → Pacific Crest Trail（ロングトレイル徒歩）  4件
Troopers      → eスポーツ / アイスホッケー / マウンテンバイク  5件
Mandarins     → ゲーム "Bleeding Mandarins" / 個人vlog      3件
Cadets        → 陸軍幼年学校・英軍士官学校の訓練            2件
Cavaliers     → NBA クリーブランド・キャバリアーズ           2件
Jersey Surf   → サーフィン                                1件
Blue Knights  → アイスホッケーのゴーリー                    1件
Genesis       → ヒュンダイの車種（GV80 POV drive など）
```

`"POV"` や `"GoPro"` は車載・ゲーム・アウトドアでも日常的に使われる。
**楽器・パート語があることを必ず確かめる**こと。
除去した18件は `pov_ledger.json` へ却下登録済み（入れ忘れていて再び候補に出た）。

### 2. 自動生成される大会名が全角縦棒のままだった

CSV側だけ `【POV/YYYY】` へ統一したのに、`apply_accepted()` は `【POV｜YYYY】` を
作り続けていた。**毎日の自動反映が動き出すと、統一した表記が日々崩れるところだった。**
生成側も試験で見張るようにした。

### 3. 試験が骨抜きになる

`test_scout_pov_videos.py` の初版には穴が2つあった。どちらも**変異試験で発覚**した。

- 「落とすべき」の試験文に楽器語が無く、除外規則を踏む前に落ちていた
  → `multicam` の除外を消しても PASS してしまう
- 自動採用の「題名だけでPOVと読める」の門を踏む例が無かった
  → その門を消しても PASS してしまう

**落とすべき試験文には必ず楽器語を入れること。**
試験が落ちたときに試験の方を書き換えない。

### 4. Service Worker が古いデータを握る

push・デプロイ後、CDNもGitHub rawも新しいのに**ブラウザだけ旧データ(POV 192件)**を
表示し続けた。キャッシュ回避リロードでも直らず、**SWを解除して初めて936件**になった。
本番の検証でDOMを見るときは、この層を疑うこと。

### 5. 終了コードの測り方

`python3 script.py | tail -2; echo $?` は **tail の終了コード**を見てしまう。
異常系の試験をするときは、パイプせずに測ること（これで一度、誤って「正常」と読んだ）。

---

## 作ったもの

| ファイル | 役割 |
|---|---|
| `scout_pov_videos.py` | 探索と判定。3軸（横断検索/雪だるま/再訪）+ 見張り台帳 |
| `test_scout_pov_videos.py` | **46件**。判定の見張り。変異試験で歯を確認済み |
| `check_pov_updates.py` | 日次チェックの本体。`--auto-apply` で確実な線だけ入れる |
| `.github/workflows/videos-daily-update.yml` | 毎日06:00 JST（macOSランナー） |
| **`docs/POV_LIST_GUIDE.md`** | **他AIと共同作業するための指示書。POVを触るなら必読** |
| `docs/POV_MISS_2026-08-10.md` | 取りこぼしの経緯と再発予防 |

`AGENTS.md` / `CLAUDE.md` / `docs/OPS_GUIDE.md` / 日次Issueの本文 の4箇所から
`POV_LIST_GUIDE.md` を必読として参照させてある。

### よく使うコマンド

```bash
python3 scout_pov_videos.py --coverage                          # 年別の収録状況
python3 scout_pov_videos.py --year-from 2012 --year-to 2014 --skip-revisit
python3 scout_pov_videos.py --accept ID / --reject ID / --apply
python3 check_pov_updates.py --auto-apply                       # 日次と同じ処理
python3 test_scout_pov_videos.py                                # 判定を変えたら必ず
python3 sync_csv_to_json.py && python3 check_data.py
```

---

## いまの状態（2026-08-11）

```
POV 982件 / 2003〜2026 の24年分
台帳 180件（採用49 / 却下131）
薄い年 … 2012・2013・2014
2020年が少ないのはコロナでDCIシーズン中止のため。正しい空白
2012年より前はヘッドカム自体がほとんど無い
```

- 本番 **v2.22.0** / ローカル **v2.22.2**（**未push 2コミット**、別セッションのぶん）
- 試験 46件 PASS / `check_data.py` OK

---

## 次にやるとよいこと

1. **日次ワークフローの初回を見届ける**。翌朝06:00 JSTが初回。
   `gh run list --workflow videos-daily-update.yml` で結果を見る。
   macOSランナーは初回だけ挙動が読めないので、1回は目視したい
2. **薄い年（2012〜2014）を埋める**。指示書の依頼文をそのまま他AIへ渡せる
3. **役割が `【POV】` のまま残っている行**を埋める（題名に楽器が無いもの。動画を見ないと分からない）
4. `pov_団体未解決_claude.tsv` の96件の扱いを決める
   - 日本語24件: 京都橘・精華女子・出雲商業など。**GoProが奏者装着か撮影者の手持ちかが
     題名で判別できない**ので、採用基準に照らして目視が要る
   - 英語72件: 高校/大学マーチングバンド・WGI屋内団体。**DCI外なので範囲に入れるかの判断**
5. QAの既存FAIL 2件（**本番にも元からある**もので、今回の変更による回帰ではない）
   - `投稿画面を開いたあと本文へフォーカスしない`
   - `説明的な3分類カードがTOPに残っている` … `.mz-gate-band` はログイン時のみ隠れる要素で、
     QAは未ログインで走るため必ず出る。**試験側の前提ずれ**

---

## この環境の鉄則

- **`git push` と本番デプロイは毎回、優さんの明示の許可を取る。1度の許可＝1度の実行**
  （Netlifyクレジット制。過去に上限到達でサイトが一時停止した）
- **コミットは都度作ってよい。止めるのは push だけ**
- **Google Drive がコミットを巻き戻す**。push前に `python3 push_check.py`
- **別のAIが同じファイルを同時に触っていることがある**。
  作業前に `git log --oneline -5` と `git status` を見て、相手の作業中ファイルを巻き込まない
- 一括編集は **Pythonスクリプト1本 + assert**
- **`body[data-mz-tool]` を書き換えると登録特典が黙って消える**
